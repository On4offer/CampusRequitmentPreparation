# 开发：Vite 将前缀 /api 的请求代理到此后端根地址（与 uvicorn --port 一致）
# 若后端不是 8076，改成实际地址即可
VITE_DEV_API_TARGET=http://127.0.0.1:8076

# 生产构建（npm run build）时：填写后端 API 根 URL，不要尾斜杠
# 开发环境留空即可，由上方代理转发
# VITE_API_BASE=https://your-api.example.com
