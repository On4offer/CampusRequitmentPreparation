# 开发记录（总览）

按时间倒序，每次开发一条。

---

## 2026-03-19 V2 第一功能：最小向量检索层（RAG Core）起步

### 完成内容
- 新增 `app/rag/` 模块：
  - `embedder.py`：最小 embedding（英文 token + 中文单字，L2 归一化稀疏向量）
  - `index.py`：`InMemoryVectorIndex`（按 `user_id` 分桶，支持 `upsert/remove/query_top_k`）
  - `retriever.py`：`LTMRetriever`（可对 `LTMItem` 建索引并按 query 检索，返回 `RetrievedMemory(item, score)`）
  - `__init__.py`：统一导出
- 配置预留（`app/core/settings.py`）：
  - `rag_enabled`、`rag_top_k`、`rag_min_score`、`rag_max_chars`
- `.env.example` 增加 V2 可选配置注释（默认关闭，便于后续灰度）
- 测试：新增 `tests/test_v2_rag_retriever.py`
  - 相似 query 命中正确记忆且按 score 倒序
  - 不同 user 之间检索隔离

### 涉及文件
- 新增：`app/rag/embedder.py`、`app/rag/index.py`、`app/rag/retriever.py`、`app/rag/__init__.py`、`tests/test_v2_rag_retriever.py`
- 修改：`app/core/settings.py`、`.env.example`

### 验收
- `pytest tests/test_v2_rag_retriever.py tests/test_day7_regression.py` 全过
- 说明：本次先完成“检索层本体”，尚未接入 `/chat` 主链路（下一步接入 retrieve + trace + citations）

---

## 2026-03-19 V1 可观测与配置外置 + 演示更新

### 完成内容
- **配置外置**：`safety_force_listen`、`watch_tier_force_listen`（默认 True），通过环境变量可关闭「高风险强制倾听」或「关注档优先倾听」，无需改代码；情绪落盘、策略路径此前已可配
- **结构化日志**：/chat 成功时打一条 `logger.info`（trace_id、latency_ms、mode、risk_tier、user_id），便于本地或日志系统查看
- **演示脚本**：`demo_script.md` 增加「5. 工具占位演示」、原「5」改为「6. 切 session + 重置」，验收项增加「工具向输入 mode=工具、trace 含 intended_tool」

### 涉及文件
- 修改：`app/core/settings.py`（safety_force_listen、watch_tier_force_listen）、`app/api/routes.py`（策略分支读配置、chat_done 日志）、`开发记录/demo_script.md`

### 验收
- 回归用例全过；通过改 `.env` 中 `SAFETY_FORCE_LISTEN=false` / `WATCH_TIER_FORCE_LISTEN=false` 可切换策略；演示脚本 2～3 分钟含工具占位步骤

---

## 2026-03-19 V1 长期记忆（LTM）数据模型与接口占位

### 完成内容
- **数据模型**（`app/memory/ltm.py`）：`LTMItem`（id、user_id、type、content、created_at、source、confidence、tags），类型 `Preference` / `Profile` / `Event` / `Constraint`
- **占位存储**：`InMemoryLTMStore`，`put(user_id, item)`、`get_by_id(id)`、`list_by_user(user_id, type=None, limit=N)`；单例 `ltm_store`
- **API**：`POST /memory/ltm`（body：user_id、type、content、source、confidence、tags）返回 `{ id }`；`GET /memory/ltm?user_id=xxx&type=Preference&limit=10` 返回 `{ items: LTMItemOut[] }`；不在 /chat 主链路调用
- **测试**：`tests/test_v1_ltm.py` 写入后列表查询、按 type 过滤

### 涉及文件
- 新增：`app/memory/ltm.py`、`tests/test_v1_ltm.py`
- 修改：`app/api/schemas.py`（LTMWriteRequest、LTMItemOut、LTMListResponse）、`app/api/routes.py`（POST/GET /memory/ltm）

### 验收
- `pytest tests/test_v1_ltm.py` 通过；能通过 API 写入并查出 LTM，为 V2 RAG 预留扩展点

---

## 2026-03-19 V1 情绪结果持久化占位

### 完成内容
- **配置**：`emotion_log_enabled`（默认 False）、`emotion_log_path`（默认 `data/emotion_log.jsonl`），由环境变量/`.env` 控制
- **写入模块**（`app/emotion/log.py`）：`append_emotion_record(file_path, record)`，向 JSONL 追加一行；record 含 user_id、session_id、timestamp_ms、label、intensity、risk_tier、trace_id；目录不存在则创建，写失败静默忽略
- **/chat 链路**：当 `emotion_log_enabled` 为 True 时，在得到 emotion_for_trace 后调用 `append_emotion_record` 写一条
- **测试**：`tests/test_v1_emotion_log.py` 开启开关并指定 tmp 路径，POST /chat 后断言文件存在且含一条合法 JSON、字段与 trace 一致

### 涉及文件
- 新增：`app/emotion/log.py`、`tests/test_v1_emotion_log.py`
- 修改：`app/core/settings.py`（emotion_log_enabled、emotion_log_path）、`app/api/routes.py`（调用 append_emotion_record）

### 验收
- 默认不落盘，现有回归全过；设置 `EMOTION_LOG_ENABLED=true` 且 `EMOTION_LOG_PATH=data/emotion_log.jsonl` 后对话若干轮，该文件出现对应条数记录且字段与 trace 中 emotion 一致；`pytest tests/test_v1_emotion_log.py` 通过

---

## 2026-03-19 V1 状态机「工具」分支与占位

### 完成内容
- **Mode 扩展**：状态机输出增加「工具」，共五类（闲聊/倾听/安慰/工作/工具）；工具意图优先于显式指令（关键词命中即 mode=工具）
- **工具关键词规则**（代码内）：天气/查天气 → weather；记一下/备忘/提醒/记个事 → memo；几点了/现在几点/时间 → time
- **Trace**：`TraceDecision` 增加 `intended_tool`、`tool_params_placeholder`；mode=工具时写入，安全/关注档强制倾听时清空
- **占位 prompt**：`get_system_prompt_for_mode("工具")` 返回工具占位说明；`config/policy_config.json` 增加「工具」模板
- **回归**：`data/regression_v0.jsonl` 增加 3 条工具用例（帮我查天气、记一下明天开会、现在几点了）；`test_day7_regression` 断言 `expect_intended_tool`

### 涉及文件
- 修改：`app/policy/state_machine.py`（Mode、_TOOL_KEYWORDS、decide_mode 返回 4 元组）、`app/trace/models.py`（TraceDecision）、`app/api/routes.py`（接入 4 元组与 trace 字段）、`config/policy_config.json`、`tests/test_day5_policy.py`、`tests/test_day7_regression.py`、`data/regression_v0.jsonl`

### 验收
- `pytest tests/test_day5_policy.py tests/test_day7_regression.py` 全过；「帮我查天气」等输入 trace 中 mode=工具、intended_tool=weather/memo/time

---

## 2026-03-19 V1 共情策略可配置化

### 完成内容
- **配置文件**（`config/policy_config.json`）：mode_prompts（四类 mode 的 system 模板）、explicit_rules（显式指令关键词→mode）、emotion_to_mode（情绪标签→mode，含 min_intensity）
- **配置加载**：`app/policy/state_machine.py` 增加 `reload_policy_config(config_path)`，支持从 JSON 加载；空路径或文件不存在时使用代码内默认（与 V0 一致）
- **Settings**：`policy_config_path`（默认空）；启动时在 `create_app()` 中解析项目根路径并调用 `reload_policy_config`
- **导出**：`app.policy` 增加 `reload_policy_config`

### 涉及文件
- 新增：`config/policy_config.json`
- 修改：`app/core/settings.py`（policy_config_path）、`app/policy/state_machine.py`（可配置逻辑与默认常量）、`app/policy/__init__.py`（导出）、`app/main.py`（启动时加载）

### 验收
- 不设置 `POLICY_CONFIG_PATH` 时行为与 V0 一致；`pytest tests/test_day5_policy.py tests/test_day7_regression.py` 全过
- 设置 `POLICY_CONFIG_PATH=config/policy_config.json` 后重启，修改 JSON 中某条 mode_prompts 或规则即可生效

---

## 2026-03-19 Day7：演示脚本 + 回归集 + badcase 占位

### 完成内容
- **演示脚本**（`demo_script.md`）：约 2 分钟可复现流程（正常对话 → 多轮 → trace 回放 → 安全模式 → 切 session 重置）
- **回归集**（`data/regression_v0.jsonl`）：约 20 条用例，覆盖情绪稳定性、mode、安全触发、关注档；字段：`message`、`expect_risk_tier` / `expect_mode` / `expect_emotion_label`（可选）
- **回归测试**（`tests/test_day7_regression.py`）：读取 jsonl，mock LLM 使情绪走规则兜底，POST /chat + GET /trace，断言上述期望字段
- **策略补充**：高风险强制 mode=倾听、关注档优先 mode=倾听（`app/api/routes.py`）
- **badcase 占位**（`开发记录/badcase_week1.md`）：本周可在此记录 badcase 供 V2/V4 用

### 涉及文件
- 新增：`demo_script.md`、`data/regression_v0.jsonl`、`tests/test_day7_regression.py`、`开发记录/badcase_week1.md`
- 修改：`app/api/routes.py`（安全/关注档 mode 覆盖）

### 验收
- 按 `demo_script.md` 跑一遍约 2 分钟闭环
- `pytest tests/test_day7_regression.py` 全过

---

## 2026-03-19 Day6：UI 最小可演示（Gradio Chat + 诊断面板）

### 完成内容
- **Gradio 页面**（`ui/gradio_app.py`）
  - 左侧：对话区（Chatbot + 输入框）、清空对话
  - 右侧：诊断面板（user_id / session_id 输入、重置会话、本轮 emotion / mode / trace_id / 耗时 / token、Trace JSON 展示）
- **接口调用**：POST /chat → 再 GET /trace/{trace_id} 拉取诊断；POST /sessions/reset 清空会话记忆
- **依赖**：requirements.txt 增加 `gradio`
- **常用命令**：`常用命令.md` 增加「启动 Gradio UI」一节（先启后端，再 `python -m ui.gradio_app`，浏览器 7860）

### 涉及文件
- 新增：`ui/__init__.py`、`ui/gradio_app.py`
- 修改：`requirements.txt`、`常用命令.md`

### 验收
- 先启动 uvicorn（如 8076），再启动 `python -m ui.gradio_app`，打开 http://127.0.0.1:7860
- 多轮对话 → 右侧显示诊断（情绪、模式、trace_id、耗时、token、Trace JSON）→ 可切换 session_id 并重置会话

---

## 2026-03-19 Day5：状态机（Policy v0）与共情策略骨架

### 完成内容
- **状态机**（`app/policy/state_machine.py`）
  - `decide_mode(message, emotion_label, emotion_intensity)` → (mode, mode_reason)
  - mode：闲聊 / 倾听 / 安慰 / 工作（工具占位）
  - 判定优先级：用户显式指令（如「别安慰我，只想吐槽」）> 情绪规则；默认 倾听
- **共情策略**：四个 mode 各一套 system prompt 模板（`MODE_PROMPTS`），`get_system_prompt_for_mode(mode)` 返回对应模板
- **/chat**：在情绪识别后调用 `decide_mode`，非安全模式下用 `get_system_prompt_for_mode(mode)` 作为 system；trace 中写入 `decision.mode`、`decision.mode_reason`
- **测试**：`tests/test_day5_policy.py`（trace 含 mode/mode_reason、显式指令覆盖情绪、四套 prompt 存在）

### 涉及文件
- 新增：`app/policy/__init__.py`、`app/policy/state_machine.py`、`tests/test_day5_policy.py`
- 修改：`app/api/routes.py`（接入 decide_mode、get_system_prompt_for_mode，trace 写入 mode/mode_reason）

### 验收
- pytest 10 条全过
- 相同输入 mode 稳定；trace 可解释「为什么进入该模式」（mode_reason）

---

## 2026-03-19 Swagger 文档字体调大（/docs 注入 CSS）

### 完成内容
- 关闭 FastAPI 默认 Swagger：`docs_url=None`
- 自定义 `GET /docs`：使用 `get_swagger_ui_html()` 生成页面后，在 HTML 的 `</head>` 前注入自定义 CSS
- 放大 Swagger UI 常用区域字号（标题、接口路径、说明文案、参数表格、输入框/下拉框、响应区等），提高可读性

### 涉及文件
- 修改：`app/main.py`

### 验收
- 访问 `http://127.0.0.1:<port>/docs` 字体明显变大，阅读舒适

---

## 2026-03-18 V2 /chat 接入 RAG（检索 + 证据注入 + citations + 降级）

### 完成内容
- **RAG 单例与写入同步**：`app/rag` 提供单例 `ltm_retriever`；`POST /memory/ltm` 写入成功后对带 id 的 item 调用 `ltm_retriever.index_item()`，保证新 LTM 进入检索索引。
- **Trace 与响应结构**：`TraceDecision` 增加 `memory_hits`（id/type/score）；`ChatResponse` 增加 `citations`。
- **/chat 接入 RAG**：当 `settings.rag_enabled=True` 时，在情绪落盘后调用 `ltm_retriever.retrieve()`，按 `rag_max_chars` 拼证据块注入 system 前部，写 trace 步骤 `retrieve_ltm`，并填充 `memory_hits`/`citations`；检索异常时只打带 error 的步骤、不改 system，主流程继续（降级）。
- **测试**：`tests/test_v2_rag_chat.py` 集成测试（开启 RAG → 写 LTM → /chat → 断言 citations 非空、trace 含 retrieve_ltm 与 decision.memory_hits）。

### 涉及文件
- 修改：`app/rag/__init__.py`、`app/api/routes.py`、`app/trace/models.py`、`app/api/schemas.py`
- 新增：`tests/test_v2_rag_chat.py`

### 验收
- 默认 `rag_enabled=False` 时行为与 V1 一致；`pytest tests/test_day7_regression.py tests/test_v1_ltm.py tests/test_v2_rag_retriever.py tests/test_v2_rag_chat.py` 全过；开启 RAG 时 /chat 返回 citations，trace 含 retrieve_ltm 与 memory_hits。

---

## 2026-03-18 V2 收尾：LTM 管理增强 + UI 证据展示 + 回归集升级

### 完成内容
- **模块一（关键项）**：LTM 模型新增 `is_active`、`updated_at`、`embedding_status`；支持局部更新与软删除（`PATCH/DELETE /memory/ltm/{id}`）。
- **模块五（API/UI）**：
  - `GET /memory/ltm` 增加关键词过滤参数 `q`；
  - 新增 `GET /memory/ltm/{id}`；
  - Gradio 诊断区展示“本轮命中记忆数 + citations 列表（id/type/score）”。
- **模块六（回归与数据）**：
  - 新增 `data/regression_v2.jsonl`；
  - 新增 `tests/test_v2_rag_memory.py`（预加载记忆后断言 citations 与 trace.memory_hits）；
  - 新增 `tests/test_v2_ltm_api.py`（按 id 查询、关键词查询、patch、软删除）。

### 涉及文件
- 修改：`app/memory/ltm.py`、`app/api/schemas.py`、`app/api/routes.py`、`ui/gradio_app.py`
- 新增：`data/regression_v2.jsonl`、`tests/test_v2_rag_memory.py`、`tests/test_v2_ltm_api.py`

### 验收
- `pytest tests/test_day7_regression.py tests/test_v1_ltm.py tests/test_v2_rag_retriever.py tests/test_v2_rag_chat.py tests/test_v2_rag_memory.py tests/test_v2_ltm_api.py tests/test_day5_policy.py` 全过（11 条）。
- UI 发送消息后，诊断区可直接看到命中记忆数量与 citations，便于演示“为何这样回答”。

---

## 2026-03-19 情绪识别改为 LLM 为主 + 关键词兜底

### 完成内容
- **情绪识别**：由纯关键词改为 **LLM 优先、规则兜底**
  - **风险分层**：仍用关键词（`_risk_from_keywords`），保证高危不漏、可解释
  - **情绪 label/intensity/evidence**：优先调用 LLM 单轮（固定 prompt 要求只输出 JSON）；解析失败或请求异常时回退到规则（`_emotion_from_rules`）
- **接口**：`analyze_emotion` 改为 async，入参增加 `llm: LLMClient`，便于复用同一 client
- **/chat**：先 `llm = _build_llm_client()`，再 `await analyze_emotion(req.message, llm=llm)`，主对话复用该 `llm`
- **测试**：Day2 因每轮 /chat 多一次情绪 LLM 调用，将「LLM 调用次数」断言改为每轮 2 次（情绪 + 对话），并调整取第二轮对话的索引

### 涉及文件
- 修改：`app/emotion/analyzer.py`（重写为 LLM 主 + 规则兜底）、`app/api/routes.py`（提前构建 llm、await analyze_emotion）、`tests/test_day2_sessions.py`（调用次数与索引）

### 验收
- pytest 7 条全过
- 情绪泛化更好（换说法可由 LLM 识别）；风险仍由关键词保证；LLM 失败时规则兜底不影响主对话

---

## 2026-03-19 Day3 手动验收通过

### 验收方式
在 Swagger（`http://127.0.0.1:8076/docs`）上对真实服务做黑盒验证。

### 结果
- **POST /chat**（message: "今天西安天气怎么样", user_id: hzr, session_id: 001）→ 200，返回 reply、trace_id、user_id、session_id。
- **GET /trace/{trace_id}**（上述 trace_id）→ 200，返回完整 trace：steps（load_session_and_trim_stm、llm_call、persist_memory）、decision（占位）、metrics（latency_ms 5545、token_in/out 估算、model deepseek-chat）。
- **GET /traces?user_id=hzr&session_id=001** → 200，items 中含该条 trace。

### 备注
- 曾因 PyCharm 运行配置「工作目录」选错导致一键运行加载旧代码、Swagger 无 trace 接口；已修正工作目录并记录于 `排障记录/2026-03-19_PyCharm工作目录选错导致运行旧版本代码.md`。

---

## 2026-03-19 Day4：情绪识别（Emotion v0）+ 安全模式占位

### 完成内容
- **emotion_analyzer**（`app/emotion/analyzer.py`）
  - 输出：`label`（开心/平静/低落/焦虑/愤怒/疲惫）、`intensity`（0-3）、`evidence`、`risk_tier`（一般/关注/高风险）
  - v0 规则：高风险关键词（自伤/他伤）→ 高风险；关注词（抑郁/崩溃等）→ 关注；情绪关键词 → label + intensity
- **安全模式 v0**
  - 当 `risk_tier == "高风险"` 时使用固定安全策略 system prompt（更克制、导向专业支持）
  - trace 中写入 `decision.safety_mode`、`decision.safety_trigger_reason`
- **TraceDecision** 新增 `safety_trigger_reason`（Day4 触发原因）
- **/chat**：先调用 `analyze_emotion(req.message)`，再选 system、写 trace 的 emotion/risk_tier/safety 字段
- **测试**：`tests/test_day4_emotion.py`（trace 含 emotion/risk_tier；高风险输入 safety_mode=True）

### 涉及文件
- 新增：`app/emotion/__init__.py`、`app/emotion/analyzer.py`、`tests/test_day4_emotion.py`
- 修改：`app/trace/models.py`（TraceDecision.safety_trigger_reason）、`app/api/routes.py`（接入情绪与安全策略）

### 验收
- pytest 7 条全过（含 Day4）
- 典型输入（焦虑/自伤相关）在 trace 中可见情绪与风险分层；高风险时 safety_mode 与触发原因写入 trace

---

## 2026-03-18 Day3：Trace 体系（可观测性）

### 完成内容
- **Trace 数据结构**（`app/trace/models.py`）
  - `TraceRecord`：trace_id、request、steps、decision、metrics
  - `TraceStep`：name、start_ms、end_ms、input_summary、output_summary、error
  - `TraceRequest`：user_id、session_id、message、timestamp_ms
  - `TraceDecision`：emotion、mode、mode_reason、safety_mode（占位，供 Day4/Day5 接）
  - `TraceMetrics`：latency_ms、token_in、token_out、model、degraded
- **Trace 存储**（`app/trace/store.py`）
  - 默认：文件落盘。单条 `data/trace/<trace_id>.json`，索引 `data/trace/index.jsonl`
  - 配置：`settings.trace_dir`（默认 `data/trace`）
  - 测试用：`InMemoryTraceStore`，不写磁盘
- **API**
  - `GET /trace/{trace_id}`：按 trace_id 查单条
  - `GET /traces?user_id=&session_id=&limit=`：按会话列最近 trace
- **/chat 打点**
  - 步骤：`load_session_and_trim_stm`、`llm_call`、`persist_memory`
  - 失败（502/空响应）也落 trace；token 先用字符数估算占位
- **配置**：`app/core/settings.py` 新增 `trace_dir`
- **测试**：`tests/test_day3_trace.py`（chat 产生 trace、可查单条、可列会话 trace）

### 涉及文件
- 新增：`app/trace/__init__.py`、`app/trace/models.py`、`app/trace/store.py`、`tests/test_day3_trace.py`
- 修改：`app/core/settings.py`、`app/api/schemas.py`、`app/api/routes.py`

### 验收
- pytest 全过（含 Day2 + Day3）
- 调用 `/chat` 后可用返回的 `trace_id` 调 `GET /trace/{trace_id}` 拿到完整链路

---

## 2026-03-18 Day2：会话与短期记忆（STM）

### 完成内容
- 请求支持 `user_id`、`session_id`（空则归一为 default / 新 UUID）
- 会话存储：`app/memory/store.py`（`InMemorySessionStore`），按 (user_id, session_id) 存消息列表
- STM 裁剪：`app/memory/stm.py`（`trim_messages_by_char_budget`），按字符预算保留最近消息
- `POST /sessions/reset`：清空指定会话的短期记忆
- `/chat` 流程：取历史 → 裁剪 → 拼 system+history+本轮 user → 调 LLM → 写回 user+assistant

### 涉及文件
- 新增：`app/memory/store.py`、`app/memory/stm.py`、`app/api/schemas.py` 中 SessionReset 相关、`tests/test_day2_sessions.py`
- 修改：`app/api/routes.py`、`app/core/settings.py`（stm_max_chars）

### 验收
- 同一 session_id 多轮上下文生效；不同 session 隔离；reset 后历史清空

---

## 2026-03-18 Day1：项目骨架与最小可运行链路

### 完成内容
- 目录结构：`app/`、`app/api/`、`app/core/`、`app/llm/`、`app/memory/`、`app/trace/` 等
- 配置：`.env` + `pydantic-settings`（`app/core/settings.py`），含 LLM base_url、api_key、model
- FastAPI：`GET /health`、`POST /chat`（仅需 message，返回模型回复）
- `LLMClient`（`app/llm/client.py`）：OpenAI 兼容 ChatCompletions，`chat()` + `extract_text()`

### 涉及文件
- 新增：`app/main.py`、`app/core/settings.py`、`app/llm/client.py`、`app/api/routes.py`、`app/api/schemas.py`、`requirements.txt`、`.env.example` 等

### 验收
- 服务可启动；`POST /chat` 能返回模型回复

---

*后续每次开发在此文件末尾追加一条「日期 + DayN/主题 + 完成内容 + 涉及文件 + 验收」即可。*
