# 开发记录

本文件夹用于记录**每次开发**的内容：完成的功能、涉及文件、验收结果等，便于回溯和写周报/简历。

- 每次开发可新增一条记录（推荐单文件一条，按日期命名，如 `YYYY-MM-DD_DayN_简述.md`）
- 或在本目录下的 `开发记录.md` 中追加一条「开发条目」

当前约定：
- **排障类**问题与解决 → 放在项目根目录的 `排障记录/`
- **功能开发**进度与变更 → 放在本目录 `开发记录/`
