# 常用命令与说明

项目根目录下执行。端口、路径等可按需修改。

---

## 1. 激活虚拟环境

**PowerShell（推荐）：**

```powershell
.\.venv\Scripts\Activate.ps1
```

**CMD：**

```cmd
.\.venv\Scripts\activate.bat
```

- **说明**：使用项目自带的 `.venv`，保证依赖与解释器一致。激活后终端前会显示 `(.venv)`。
- **注意**：若同时装了 Conda，先 `conda deactivate` 再激活 venv，避免用错 Python。

---

## 2. 启动服务（FastAPI + uvicorn）

```powershell
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8076
```

- **说明**：以开发模式启动，`--reload` 下改代码会自动重启。Swagger 文档：`http://127.0.0.1:8076/docs`。
- **端口**：8076 可改为 8075、8077、8000 等（避免与其它程序占用冲突）。
- **前提**：已激活 venv；项目根目录下有 `.env` 且配置好 `LLM_API_KEY` 等（否则 `/chat` 会 500）。V1 可选配置（共情策略路径、情绪落盘、策略开关）见 `.env.example` 注释，修改后需重启后端。

---

## 3. 运行测试（pytest）

**全量测试（推荐日常用）：**

```powershell
python -m pytest -q
```

- **说明**：运行 `tests/` 下所有用例，`-q` 为简洁输出。覆盖 Day1/Day2/Day3 等。

**只跑某文件：**

```powershell
python -m pytest -q tests\test_day3_trace.py
python -m pytest -q tests\test_day2_sessions.py
```

**带打印/详细输出：**

```powershell
python -m pytest -v
python -m pytest -v -s
```

- `-v`：每条用例一行；`-s`：不捕获 stdout，方便看 `print`。

**说明**：测试会 mock LLM，不发起真实 API 请求；Trace 测试使用内存 store，不写磁盘。

---

## 4. Day2 端到端验证脚本（可选）

服务已启动时，可用脚本快速验证会话记忆与 reset：

```powershell
python scripts\verify_day2.py http://127.0.0.1:8076
```

- **说明**：脚本会请求 `/chat`、`/sessions/reset` 等，检查多轮记忆与会话隔离。末尾需传入当前服务 base URL（端口与启动时一致）。

---

## 5. 一键顺序（复制即用）

在项目根目录、PowerShell 下：

```powershell
.\.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8076
```

另开一个终端跑测试：

```powershell
cd "d:\Users\hzr08\Desktop\研二下\校招学习\项目\个性化情感陪伴智能体"
.\.venv\Scripts\Activate.ps1
python -m pytest -q
```

---

## 6. 启动 Gradio UI（Day6 演示）

**先启动后端**（同上 uvicorn），再另开终端：

```powershell
.\.venv\Scripts\Activate.ps1
python -m ui.gradio_app
```

- 浏览器打开：`http://127.0.0.1:7860`
- 左侧：对话区；右侧：诊断面板（emotion、mode、trace_id、耗时、token、Trace JSON）
- 可修改 user_id / session_id 后点「重置会话」清空该会话记忆
