
# 外卖系统项目 - 完整面试准备文档（深度版）

## 第一部分：项目整体与架构

### 问题1：项目的技术架构是什么？为什么这样设计？

**回答思路**：
```
【技术架构】
- 后端：Spring Boot 2.7.3 + MyBatis + MySQL + Redis
- 项目结构：Maven多模块
  - sky-common：公共模块（工具类、常量、异常、结果封装）
  - sky-pojo：实体类模块（Entity、DTO、VO）
  - sky-server：业务模块（Controller、Service、Mapper）

【设计原因】
1. 多模块设计：
   - 职责清晰，便于维护
   - 可以独立打包和部署
   - 公共代码复用

2. 技术选型：
   - Spring Boot：快速开发，约定优于配置
   - MyBatis：SQL可控，适合复杂查询
   - Redis：高性能缓存
   - MySQL：关系型数据库，适合业务数据
```

### 问题2：项目的分层架构是怎样的？

**回答思路**：
```
【分层架构】
1. Controller层：接收请求，参数校验，调用Service
2. Service层：业务逻辑处理
3. Mapper层：数据库操作
4. Entity/DTO/VO：数据传输对象

【设计原则】
- Controller：只负责接收和响应，不处理业务逻辑
- Service：业务逻辑集中处理
- Mapper：只负责数据访问
- DTO：接收前端数据
- VO：返回给前端的数据
- Entity：数据库实体
```

### 问题3：为什么使用DTO、VO、Entity三层对象？

**回答思路**：
1. 职责分离：
   - Entity：对应数据库表结构
   - DTO：接收前端数据，可能包含多个Entity的字段
   - VO：返回给前端，可能包含计算字段
2. 安全性：
   - 避免暴露数据库字段
   - 可以隐藏敏感信息
3. 灵活性：
   - 前后端数据结构可以不同
   - 便于版本升级

---

## 第二部分：业务场景与流程（深度）

### 问题4：用户下单的完整流程是什么？涉及哪些表？

**回答思路（结合代码）**：

```
【完整流程】
1. 用户选择菜品，加入购物车
   - 涉及表：shopping_cart（购物车表）

2. 用户点击下单
   - 校验地址簿（address_book表）
   - 校验购物车（shopping_cart表）

3. 创建订单（@Transactional事务）
   - 插入订单表（orders表）：1条记录
   - 插入订单明细表（order_detail表）：n条记录
   - 清空购物车（shopping_cart表）

4. 返回订单信息

【代码实现】
OrderServiceImpl.submitOrder()方法：
- 第69行：查询地址簿
- 第83行：查询购物车
- 第102行：插入订单
- 第113行：批量插入订单明细
- 第116行：清空购物车
```

### 问题5：订单和订单明细的关系是什么？如何设计？

**回答思路**：
1. 关系：一对多
   - 一个订单对应多个订单明细
   - 订单明细通过order_id关联订单
2. 设计原因：
   - 订单表存储订单基本信息
   - 订单明细表存储商品详情
   - 避免订单表字段过多
3. 数据一致性：
   - 使用事务保证同时插入
   - 使用外键约束（可选）

### 问题6：用户取消订单时，如何判断是否需要退款？

**回答思路（结合代码）**：

```325:360:sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java
    public void userCancelById(Long id) throws Exception {
        // 根据id查询订单
        Orders ordersDB = orderMapper.getById(id);

        // 校验订单是否存在
        if (ordersDB == null) {
            throw new OrderBusinessException(MessageConstant.ORDER_NOT_FOUND);
        }

        //订单状态 1待付款 2待接单 3已接单 4派送中 5已完成 6已取消
        if (ordersDB.getStatus() > 2) {
            throw new OrderBusinessException(MessageConstant.ORDER_STATUS_ERROR);
        }

        Orders orders = new Orders();
        orders.setId(ordersDB.getId());

        // 订单处于待接单状态下取消，需要进行退款
        if (ordersDB.getStatus().equals(Orders.TO_BE_CONFIRMED)) {
            //调用微信支付退款接口
            weChatPayUtil.refund(
                    ordersDB.getNumber(), //商户订单号
                    ordersDB.getNumber(), //商户退款单号
                    new BigDecimal(0.01),//退款金额，单位 元
                    new BigDecimal(0.01));//原订单金额

            //支付状态修改为 退款
            orders.setPayStatus(Orders.REFUND);
        }

        // 更新订单状态、取消原因、取消时间
        orders.setStatus(Orders.CANCELLED);
        orders.setCancelReason("用户取消");
        orders.setCancelTime(LocalDateTime.now());
        orderMapper.update(orders);
    }
```

**回答思路**：
1. 判断逻辑：
   - 待支付状态：直接取消，无需退款
   - 待接单状态：已支付，需要退款
   - 已接单及以后：不允许取消
2. 退款流程：
   - 调用微信支付退款接口
   - 更新订单支付状态为"退款"
   - 更新订单状态为"已取消"

### 问题7：购物车的实现逻辑是什么？

**回答思路（结合代码）**：

```36:72:sky-server/src/main/java/com/sky/service/impl/ShoppingCartServiceImpl.java
    public void addShoppingCart(ShoppingCartDTO shoppingCartDTO) {
        //判断当前加入到购物车中的商品是否已经存在了
        ShoppingCart shoppingCart = new ShoppingCart();
        BeanUtils.copyProperties(shoppingCartDTO,shoppingCart);
        Long userId = BaseContext.getCurrentId();
        shoppingCart.setUserId(userId);

        List<ShoppingCart> list = shoppingCartMapper.list(shoppingCart);

        //如果已经存在了，只需要将数量加一
        if(list != null && list.size() > 0){
            ShoppingCart cart = list.get(0);
            cart.setNumber(cart.getNumber() + 1);//update shopping_cart set number = ? where id = ?
            shoppingCartMapper.updateNumberById(cart);
        }else {
            //如果不存在，需要插入一条购物车数据
            //判断本次添加到购物车的是菜品还是套餐
            Long dishId = shoppingCartDTO.getDishId();
            if(dishId != null){
                //本次添加到购物车的是菜品
                Dish dish = dishMapper.getById(dishId);
                shoppingCart.setName(dish.getName());
                shoppingCart.setImage(dish.getImage());
                shoppingCart.setAmount(dish.getPrice());
            }else{
                //本次添加到购物车的是套餐
                Long setmealId = shoppingCartDTO.getSetmealId();
                Setmeal setmeal = setmealMapper.getById(setmealId);
                shoppingCart.setName(setmeal.getName());
                shoppingCart.setImage(setmeal.getImage());
                shoppingCart.setAmount(setmeal.getPrice());
            }
            shoppingCart.setNumber(1);
            shoppingCart.setCreateTime(LocalDateTime.now());
            shoppingCartMapper.insert(shoppingCart);
        }
    }
```

**回答思路**：
1. 添加逻辑：
   - 查询购物车中是否已存在相同商品
   - 如果存在：数量+1
   - 如果不存在：插入新记录，数量为1
2. 设计考虑：
   - 支持菜品和套餐两种类型
   - 通过dishId和setmealId区分
   - 使用userId关联用户

### 问题8：菜品和套餐的关系是什么？如何设计？

**回答思路**：
1. 关系：多对多
   - 一个套餐包含多个菜品
   - 一个菜品可以属于多个套餐
2. 表设计：
   - dish表：菜品表
   - setmeal表：套餐表
   - setmeal_dish表：套餐菜品关联表（中间表）
3. 业务逻辑：
   - 创建套餐时，同时插入关联关系
   - 删除菜品时，检查是否被套餐关联

---

## 第三部分：Spring框架原理（八股文）

### 问题9：Spring Boot的自动配置原理是什么？

**回答思路**：
1. 核心注解：
   - `@SpringBootApplication` = `@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`
2. 自动配置原理：
   - `@EnableAutoConfiguration` 导入 `AutoConfigurationImportSelector`
   - 读取 `META-INF/spring.factories` 文件
   - 根据条件注解（@ConditionalOnClass等）决定是否加载配置类
3. 项目中的应用：
   - Redis自动配置：检测到Redis依赖，自动配置RedisTemplate
   - MyBatis自动配置：检测到MyBatis依赖，自动配置SqlSessionFactory

### 问题10：Spring的Bean生命周期是什么？

**回答思路**：
1. 生命周期阶段：
   - 实例化（Instantiation）
   - 属性赋值（Populate properties）
   - 初始化（Initialization）
   - 使用（In use）
   - 销毁（Destruction）
2. 关键接口：
   - `InitializingBean`：初始化后执行
   - `DisposableBean`：销毁前执行
   - `@PostConstruct`：初始化后执行
   - `@PreDestroy`：销毁前执行
3. 项目中的应用：
   - `@Component` 标注的类会被Spring管理
   - `@Autowired` 在属性赋值阶段注入依赖

### 问题11：Spring的依赖注入方式有哪些？你用的是哪种？

**回答思路**：
1. 注入方式：
   - 构造器注入
   - Setter注入
   - 字段注入（@Autowired）
2. 项目中使用：
   - 主要使用字段注入（@Autowired）
   - 优点：代码简洁
   - 缺点：不利于测试，无法使用final字段
3. 最佳实践：
   - 推荐使用构造器注入（可以保证依赖不可变）

### 问题12：Spring的事务管理原理是什么？

**回答思路**：
1. 实现方式：
   - 基于AOP动态代理
   - `@Transactional` 标注的方法会被代理
2. 事务传播机制：
   - REQUIRED（默认）：如果存在事务则加入，不存在则创建
   - REQUIRES_NEW：总是创建新事务
   - NESTED：嵌套事务
3. 项目中的应用：
   - `@EnableTransactionManagement` 启用事务管理
   - `@Transactional` 标注需要事务的方法
   - 如：订单下单、菜品保存等

### 问题13：Spring AOP的实现原理是什么？

**回答思路**：
1. 实现方式：
   - JDK动态代理：目标类实现接口
   - CGLIB代理：目标类没有实现接口
2. 代理创建时机：
   - Bean创建时，检测是否需要AOP增强
   - 如果需要，创建代理对象替代原始对象
3. 项目中的应用：
   - `@Cacheable`：缓存切面
   - `@Transactional`：事务切面
   - `AutoFillAspect`：自定义切面

---

## 第四部分：MyBatis原理（八股文）

### 问题14：MyBatis的工作原理是什么？

**回答思路**：
1. 核心组件：
   - SqlSessionFactory：会话工厂
   - SqlSession：会话
   - Mapper接口：数据访问接口
   - Mapper XML：SQL映射文件
2. 执行流程：
   - 解析Mapper XML文件
   - 创建Mapper接口的代理对象
   - 调用方法时，根据方法名找到对应的SQL
   - 执行SQL，返回结果
3. 项目中的应用：
   - Mapper接口：`EmployeeMapper`、`OrderMapper`等
   - XML文件：`mapper/*.xml`

### 问题15：MyBatis的#{}和${}的区别是什么？

**回答思路**：
1. `#{}`：
   - 预编译，防止SQL注入
   - 会自动添加引号
   - 推荐使用
2. `${}`：
   - 字符串拼接，有SQL注入风险
   - 不会添加引号
   - 适用于动态表名、列名
3. 项目中的使用：
   - 主要使用 `#{}`
   - `${}` 用于动态SQL（如动态表名）

### 问题16：MyBatis如何实现批量插入？

**回答思路**：
1. 方式一：foreach标签
   ```xml
   <insert id="insertBatch">
       INSERT INTO order_detail (order_id, dish_id, number)
       VALUES
       <foreach collection="list" item="item" separator=",">
           (#{item.orderId}, #{item.dishId}, #{item.number})
       </foreach>
   </insert>
   ```
2. 方式二：MyBatis-Plus的批量插入
3. 项目中的使用：
   - `orderDetailMapper.insertBatch()`：批量插入订单明细
   - `dishFlavorMapper.insertBatch()`：批量插入口味

---

## 第五部分：数据库设计与SQL

### 问题17：订单表的设计思路是什么？

**回答思路**：
1. 核心字段：
   - id：主键
   - number：订单号（唯一索引）
   - status：订单状态
   - pay_status：支付状态
   - user_id：用户ID（外键）
   - amount：订单金额
   - order_time：下单时间
2. 设计考虑：
   - 订单号和用户ID建立索引
   - 状态字段使用常量类管理
   - 时间字段用于定时任务查询

### 问题18：如何设计订单明细表？

**回答思路**：
1. 表设计：
   - id：主键
   - order_id：订单ID（外键）
   - dish_id：菜品ID
   - setmeal_id：套餐ID
   - name：菜品/套餐名称（冗余字段，避免关联查询）
   - number：数量
   - amount：单价
2. 设计考虑：
   - 冗余字段：存储名称，避免关联查询
   - 支持菜品和套餐两种类型
   - order_id建立索引

### 问题19：如何优化慢查询？

**回答思路**：
1. 索引优化：
   - 为常用查询字段建立索引
   - 如：order_time、status、user_id
2. SQL优化：
   - 避免SELECT *
   - 使用LIMIT分页
   - 避免子查询
3. 项目中的优化：
   - 使用PageHelper分页
   - 缓存热点数据
   - 避免N+1查询

---

## 第六部分：业务逻辑深度问题

### 问题20：用户取消订单的业务规则是什么？

**回答思路（结合代码）**：
1. 状态限制：
   - 只有待支付和待接单状态可以取消
   - 已接单及以后状态不允许取消
2. 退款规则：
   - 待支付：直接取消，无需退款
   - 待接单（已支付）：需要调用退款接口
3. 代码实现：
   - 第335行：检查订单状态
   - 第343行：判断是否需要退款
   - 第345行：调用退款接口

### 问题21：商户拒单的业务逻辑是什么？

**回答思路（结合代码）**：

```485:512:sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java
    public void rejection(OrdersRejectionDTO ordersRejectionDTO) throws Exception {
        // 根据id查询订单
        Orders ordersDB = orderMapper.getById(ordersRejectionDTO.getId());

        // 订单只有存在且状态为2（待接单）才可以拒单
        if (ordersDB == null || !ordersDB.getStatus().equals(Orders.TO_BE_CONFIRMED)) {
            throw new OrderBusinessException(MessageConstant.ORDER_STATUS_ERROR);
        }

        //支付状态
        Integer payStatus = ordersDB.getPayStatus();
        if (payStatus == Orders.PAID) {
            //用户已支付，需要退款
            String refund = weChatPayUtil.refund(
                    ordersDB.getNumber(),
                    ordersDB.getNumber(),
                    new BigDecimal(0.01),
                    new BigDecimal(0.01));
            log.info("申请退款：{}", refund);
        }

        // 拒单需要退款，根据订单id更新订单状态、拒单原因、取消时间
        Orders orders = new Orders();
        orders.setId(ordersDB.getId());
        orders.setStatus(Orders.CANCELLED);
```

**回答思路**：
1. 业务规则：
   - 只有待接单状态可以拒单
   - 如果已支付，需要退款
   - 更新订单状态为已取消
   - 记录拒单原因
2. 代码逻辑：
   - 第490行：校验订单状态
   - 第496行：判断支付状态
   - 第498行：调用退款接口

### 问题22：菜品批量删除的业务逻辑是什么？

**回答思路（结合代码）**：

```87:111:sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java
    @Transactional
    public void deleteBatch(List<Long> ids) {
        //判断当前菜品是否能够删除---是否存在起售中的菜品？？
        for (Long id : ids) {
            Dish dish = dishMapper.getById(id);
            if (dish.getStatus() == StatusConstant.ENABLE) {
                //当前菜品处于起售中，不能删除
                throw new DeletionNotAllowedException(MessageConstant.DISH_ON_SALE);
            }
        }

        //判断当前菜品是否能够删除---是否被套餐关联了？？
        List<Long> setmealIds = setmealDishMapper.getSetmealIdsByDishIds(ids);
        if (setmealIds != null && setmealIds.size() > 0) {
            //当前菜品被套餐关联了，不能删除
            throw new DeletionNotAllowedException(MessageConstant.DISH_BE_RELATED_BY_SETMEAL);
        }

        //删除菜品表中的菜品数据
        for (Long id : ids) {
            dishMapper.deleteById(id);
            //删除菜品关联的口味数据
            dishFlavorMapper.deleteByDishId(id);
        }
    }
```

**回答思路**：
1. 业务规则：
   - 起售中的菜品不能删除
   - 被套餐关联的菜品不能删除
   - 删除菜品时，同时删除关联的口味数据
2. 代码逻辑：
   - 第90-96行：检查是否起售
   - 第99-103行：检查是否被套餐关联
   - 第106-110行：删除菜品和口味

### 问题23：订单金额是如何计算的？

**回答思路**：
1. 计算时机：
   - 下单时，从购物车计算总金额
   - 购物车中每个商品的金额 = 单价 × 数量
2. 存储位置：
   - 订单表存储总金额
   - 订单明细表存储每个商品的金额
3. 精度问题：
   - 使用BigDecimal存储金额
   - 避免浮点数精度问题

---

## 第七部分：异常处理与代码质量

### 问题24：项目的异常体系是如何设计的？

**回答思路（结合代码）**：

```6:15:sky-common/src/main/java/com/sky/exception/BaseException.java
public class BaseException extends RuntimeException {

    public BaseException() {
    }

    public BaseException(String msg) {
        super(msg);
    }

}
```

**回答思路**：
1. 异常体系：
   - `BaseException`：基础业务异常
   - 各种具体异常：`OrderBusinessException`、`AccountNotFoundException`等
2. 全局异常处理：
   - `@RestControllerAdvice` 统一处理异常
   - 返回统一的Result格式
3. 异常分类：
   - 业务异常：BaseException
   - SQL异常：SQLIntegrityConstraintViolationException
   - 系统异常：其他Exception

### 问题25：统一返回结果Result的设计思路是什么？

**回答思路（结合代码）**：

```11:38:sky-common/src/main/java/com/sky/result/Result.java
@Data
public class Result<T> implements Serializable {

    private Integer code; //编码：1成功，0和其它数字为失败
    private String msg; //错误信息
    private T data; //数据

    public static <T> Result<T> success() {
        Result<T> result = new Result<T>();
        result.code = 1;
        return result;
    }

    public static <T> Result<T> success(T object) {
        Result<T> result = new Result<T>();
        result.data = object;
        result.code = 1;
        return result;
    }

    public static <T> Result<T> error(String msg) {
        Result result = new Result();
        result.msg = msg;
        result.code = 0;
        return result;
    }

}
```

**回答思路**：
1. 设计目的：
   - 统一响应格式
   - 便于前端处理
   - 包含状态码、消息、数据
2. 使用方式：
   - `Result.success()`：成功无数据
   - `Result.success(data)`：成功有数据
   - `Result.error(msg)`：失败
3. 泛型设计：
   - 支持任意类型的数据
   - 类型安全

### 问题26：为什么自定义JacksonObjectMapper？

**回答思路（结合代码）**：

```25:51:sky-common/src/main/java/com/sky/json/JacksonObjectMapper.java
public class JacksonObjectMapper extends ObjectMapper {

    public static final String DEFAULT_DATE_FORMAT = "yyyy-MM-dd";
    //public static final String DEFAULT_DATE_TIME_FORMAT = "yyyy-MM-dd HH:mm:ss";
    public static final String DEFAULT_DATE_TIME_FORMAT = "yyyy-MM-dd HH:mm";
    public static final String DEFAULT_TIME_FORMAT = "HH:mm:ss";

    public JacksonObjectMapper() {
        super();
        //收到未知属性时不报异常
        this.configure(FAIL_ON_UNKNOWN_PROPERTIES, false);

        //反序列化时，属性不存在的兼容处理
        this.getDeserializationConfig().withoutFeatures(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES);

        SimpleModule simpleModule = new SimpleModule()
                .addDeserializer(LocalDateTime.class, new LocalDateTimeDeserializer(DateTimeFormatter.ofPattern(DEFAULT_DATE_TIME_FORMAT)))
                .addDeserializer(LocalDate.class, new LocalDateDeserializer(DateTimeFormatter.ofPattern(DEFAULT_DATE_FORMAT)))
                .addDeserializer(LocalTime.class, new LocalTimeDeserializer(DateTimeFormatter.ofPattern(DEFAULT_TIME_FORMAT)))
                .addSerializer(LocalDateTime.class, new LocalDateTimeSerializer(DateTimeFormatter.ofPattern(DEFAULT_DATE_TIME_FORMAT)))
                .addSerializer(LocalDate.class, new LocalDateSerializer(DateTimeFormatter.ofPattern(DEFAULT_DATE_FORMAT)))
                .addSerializer(LocalTime.class, new LocalTimeSerializer(DateTimeFormatter.ofPattern(DEFAULT_TIME_FORMAT)));

        //注册功能模块 例如，可以添加自定义序列化器和反序列化器
        this.registerModule(simpleModule);
    }
}
```

**回答思路**：
1. 目的：
   - 统一日期时间格式
   - 处理LocalDateTime序列化问题
   - 兼容未知属性
2. 配置内容：
   - 日期格式：yyyy-MM-dd
   - 日期时间格式：yyyy-MM-dd HH:mm
   - 时间格式：HH:mm:ss
3. 使用位置：
   - `WebMvcConfiguration.extendMessageConverters()` 中注册

---

## 第八部分：高并发与性能优化

### 问题27：如果同时有1万个用户下单，系统如何应对？

**回答思路**：
1. 数据库层面：
   - 连接池优化（Druid连接池）
   - 数据库读写分离
   - 分库分表（按用户ID或时间）
2. 应用层面：
   - 异步处理订单（消息队列）
   - 限流（令牌桶、漏桶算法）
   - 缓存热点数据
3. 架构层面：
   - 负载均衡（Nginx）
   - 服务降级
   - 熔断机制

### 问题28：订单号是如何生成的？会重复吗？

**回答思路（结合代码）**：

```96:96:sky-server/src/main/java/com/sky/service/impl/OrderServiceImpl.java
        orders.setNumber(String.valueOf(System.currentTimeMillis()));
```

**回答思路**：
1. 当前实现：
   - 使用时间戳生成订单号
   - 问题：高并发下可能重复
2. 优化方案：
   - 时间戳 + 随机数
   - 时间戳 + 机器ID + 序列号（雪花算法）
   - UUID（但较长）
3. 推荐方案：
   - 雪花算法：保证唯一性，有序性

### 问题29：如何防止用户重复下单？

**回答思路**：
1. 问题场景：
   - 用户快速点击下单按钮
   - 网络延迟导致重复请求
2. 解决方案：
   - 前端：按钮防抖
   - 后端：分布式锁（Redis）
   ```java
   String lockKey = "order:lock:" + userId;
   Boolean lock = redisTemplate.opsForValue()
       .setIfAbsent(lockKey, "1", 5, TimeUnit.SECONDS);
   if (lock) {
       try {
           // 下单逻辑
       } finally {
           redisTemplate.delete(lockKey);
       }
   }
   ```

### 问题30：如何优化订单查询性能？

**回答思路**：
1. 数据库优化：
   - 为user_id、status、order_time建立索引
   - 使用分页查询
2. 缓存优化：
   - 缓存用户订单列表
   - 缓存订单详情
3. 查询优化：
   - 避免N+1查询
   - 使用批量查询

---

## 第九部分：分布式与微服务场景

### 问题31：如果项目要拆分成微服务，如何设计？

**回答思路**：
1. 服务划分：
   - 用户服务：用户管理、认证
   - 商品服务：菜品、套餐管理
   - 订单服务：订单管理、支付
   - 通知服务：WebSocket推送
2. 技术选型：
   - 服务注册：Nacos/Eureka
   - 服务调用：Feign/OpenFeign
   - 网关：Spring Cloud Gateway
   - 配置中心：Nacos Config
3. 数据一致性：
   - 分布式事务：Seata
   - 最终一致性：消息队列

### 问题32：分布式环境下，如何保证订单号唯一？

**回答思路**：
1. 问题：
   - 多台服务器同时生成订单号
   - 时间戳可能重复
2. 解决方案：
   - 雪花算法：机器ID + 时间戳 + 序列号
   - Redis自增：使用Redis的INCR命令
   - 数据库序列：使用数据库自增ID

### 问题33：分布式环境下，如何实现分布式锁？

**回答思路**：
1. Redis分布式锁：
   ```java
   // 加锁
   String lockKey = "lock:order:" + orderId;
   Boolean lock = redisTemplate.opsForValue()
       .setIfAbsent(lockKey, "1", 30, TimeUnit.SECONDS);
   
   // 解锁
   redisTemplate.delete(lockKey);
   ```
2. 问题：
   - 锁过期时间设置
   - 锁误删问题
3. 优化：
   - 使用UUID作为锁的值
   - 解锁时检查锁的值

---

## 第十部分：设计模式与代码设计

### 问题34：项目中使用了哪些设计模式？

**回答思路**：
1. 单例模式：
   - Spring Bean默认单例
   - `BaseContext` 使用static字段
2. 工厂模式：
   - Spring BeanFactory创建Bean
3. 代理模式：
   - Spring AOP动态代理
   - 事务管理、缓存
4. 模板方法模式：
   - JdbcTemplate、RedisTemplate
5. 策略模式：
   - 不同的异常处理策略

### 问题35：为什么使用Builder模式？

**回答思路（结合代码）**：
```java
// 使用Builder模式
Employee employee = Employee.builder()
    .id(1L)
    .name("张三")
    .status(1)
    .build();
```
1. 优势：
   - 代码可读性好
   - 支持链式调用
   - 参数可选
2. 实现：
   - 使用Lombok的@Builder注解
   - 自动生成Builder类

### 问题36：DTO、VO、Entity的设计原则是什么？

**回答思路**：
1. Entity：
   - 对应数据库表
   - 包含所有字段
   - 不包含业务逻辑
2. DTO：
   - 接收前端数据
   - 可能包含多个Entity的字段
   - 用于数据传输
3. VO：
   - 返回给前端
   - 可能包含计算字段
   - 隐藏敏感信息

---

## 第十一部分：Spring框架深度原理

### 问题37：Spring的循环依赖是如何解决的？

**回答思路**：
1. 问题场景：
   - A依赖B，B依赖A
   - 创建A时需要B，创建B时需要A
2. 解决方案：
   - 三级缓存：
     - 一级缓存：单例对象
     - 二级缓存：早期对象（未完成属性注入）
     - 三级缓存：对象工厂
3. 项目中的配置：
   ```yaml
   spring:
     main:
       allow-circular-references: true
   ```

### 问题38：Spring Bean的作用域有哪些？

**回答思路**：
1. 作用域类型：
   - singleton（默认）：单例
   - prototype：多例
   - request：请求作用域
   - session：会话作用域
2. 项目中的使用：
   - 大部分使用singleton
   - Controller、Service都是单例

### 问题39：@Autowired的注入原理是什么？

**回答思路**：
1. 注入时机：
   - Bean创建后，属性赋值阶段
2. 注入方式：
   - 字段注入：通过反射设置字段值
   - 构造器注入：通过构造器参数
   - Setter注入：通过setter方法
3. 注入过程：
   - 根据类型查找Bean
   - 如果多个，根据名称匹配
   - 通过反射注入

---

## 第十二部分：Redis深度原理

### 问题40：Redis的数据结构有哪些？项目中用了哪些？

**回答思路**：
1. 数据结构：
   - String：字符串
   - Hash：哈希
   - List：列表
   - Set：集合
   - ZSet：有序集合
2. 项目中的使用：
   - String：缓存商品数据
   - 其他结构：未使用（可以扩展）

### 问题41：Redis的持久化机制是什么？

**回答思路**：
1. RDB：
   - 定时快照
   - 恢复快，但可能丢失数据
2. AOF：
   - 记录每个写操作
   - 数据安全，但文件大
3. 项目中的配置：
   - 需要查看Redis配置
   - 建议：RDB + AOF混合

### 问题42：Redis的内存淘汰策略有哪些？

**回答思路**：
1. 策略类型：
   - noeviction：不淘汰
   - allkeys-lru：最近最少使用
   - allkeys-random：随机淘汰
   - volatile-lru：过期键LRU
2. 项目中的配置：
   - 需要根据业务选择
   - 推荐：allkeys-lru

---

## 第十三部分：业务场景深度问题

### 问题43：支付回调如何保证幂等性？

**回答思路**：
1. 问题：
   - 微信可能重复回调
   - 需要保证只处理一次
2. 解决方案：
   - 使用Redis记录已处理的订单号
   ```java
   String key = "pay:processed:" + outTradeNo;
   Boolean processed = redisTemplate.hasKey(key);
   if (processed) {
       return; // 已处理，直接返回
   }
   // 处理订单
   redisTemplate.set(key, "1", 1, TimeUnit.DAYS);
   ```

### 问题44：如何保证订单和库存的一致性？

**回答思路**：
1. 问题场景：
   - 下单时扣减库存
   - 需要保证原子性
2. 解决方案：
   - 数据库乐观锁：使用版本号
   - 数据库悲观锁：SELECT FOR UPDATE
   - 分布式锁：Redis实现
3. 项目中的实现：
   - 当前项目没有库存管理
   - 可以扩展实现

### 问题45：订单状态机如何设计？

**回答思路**：
1. 状态定义：
   ```
   待支付 → 待接单 → 已接单 → 派送中 → 已完成
            ↓
          已取消
   ```
2. 状态转换规则：
   - 待支付：可以取消、可以支付
   - 待接单：可以取消（退款）、可以接单、可以拒单
   - 已接单：可以派送
   - 派送中：可以完成
3. 实现方式：
   - 使用状态模式
   - 或者使用枚举 + 状态转换表

---

## 第十四部分：代码质量与最佳实践

### 问题46：项目中如何保证代码质量？

**回答思路**：
1. 代码规范：
   - 使用Lombok减少样板代码
   - 统一异常处理
   - 统一返回格式
2. 代码复用：
   - AOP减少重复代码
   - 工具类封装
   - 公共方法提取
3. 可维护性：
   - 分层清晰
   - 职责单一
   - 注释完善

### 问题47：BeanUtils.copyProperties的原理是什么？

**回答思路**：
1. 原理：
   - 使用反射获取属性
   - 复制同名同类型的属性
2. 使用场景：
   - Entity转DTO
   - DTO转Entity
   - Entity转VO
3. 注意事项：
   - 只复制同名属性
   - 类型必须匹配
   - 性能比手动赋值稍差

### 问题48：为什么使用PageHelper而不是手动分页？

**回答思路**：
1. PageHelper优势：
   - 代码简洁
   - 自动添加LIMIT
   - 支持多种数据库
2. 原理：
   - 拦截SQL
   - 自动添加分页参数
3. 使用方式：
   ```java
   PageHelper.startPage(pageNum, pageSize);
   List<Employee> list = employeeMapper.pageQuery(dto);
   ```

---

## 第十五部分：扩展场景问题

### 问题49：如果要做秒杀功能，如何设计？

**回答思路**：
1. 技术方案：
   - 限流：令牌桶算法
   - 库存：Redis原子操作（DECR）
   - 订单：消息队列异步处理
2. 流程设计：
   - 前端：倒计时、防重复点击
   - 后端：限流 → 扣库存 → 生成订单 → 异步处理
3. 优化方案：
   - 预热：提前加载商品到缓存
   - 削峰：消息队列缓冲
   - 降级：服务降级策略

### 问题50：如何实现订单的分布式事务？

**回答思路**：
1. 问题场景：
   - 订单服务、库存服务、积分服务
   - 需要保证要么全部成功，要么全部失败
2. 解决方案：
   - 两阶段提交（2PC）
   - TCC模式（Try-Confirm-Cancel）
   - 最终一致性（消息队列）
3. 项目中的实现：
   - 当前使用本地事务
   - 可以扩展使用Seata

### 问题51：如何实现订单的异步处理？

**回答思路**：
1. 当前实现：
   - 同步处理，用户等待
2. 优化方案：
   - 消息队列（RabbitMQ/Kafka）
   - 流程：下单 → 发送消息 → 立即返回 → 异步处理订单
3. 优势：
   - 提升响应速度
   - 削峰填谷
   - 解耦服务

---

## 第十六部分：实际生产问题

### 问题52：如果生产环境突然出现大量401错误，如何排查？

**回答思路**：
1. 排查步骤：
   - 检查JWT secretKey是否被修改
   - 检查服务器时间是否同步
   - 检查token是否过期
   - 查看拦截器日志
2. 应急方案：
   - 临时延长token过期时间
   - 提供token刷新接口
   - 回滚到上一个版本

### 问题53：如果Redis突然宕机，系统还能正常工作吗？

**回答思路**：
1. 当前问题：
   - 缓存失效，所有请求访问数据库
   - 可能导致数据库压力过大
2. 解决方案：
   - 本地缓存作为二级缓存
   - 降级策略：Redis不可用时直接查数据库
   - Redis集群：提高可用性

### 问题54：如何监控系统的性能指标？

**回答思路**：
1. 监控指标：
   - QPS：每秒请求数
   - 响应时间：接口耗时
   - 错误率：异常请求比例
   - 缓存命中率
2. 监控工具：
   - Prometheus + Grafana
   - Spring Boot Actuator
   - 自定义埋点

---

## 第十七部分：算法与数据结构

### 问题55：订单号生成算法如何设计？

**回答思路**：
1. 需求：
   - 唯一性
   - 有序性（便于排序）
   - 可读性
2. 方案：
   - 雪花算法：64位ID
   - 时间戳 + 随机数
   - UUID（但无序）

### 问题56：如何实现LRU缓存？

**回答思路**：
1. 数据结构：
   - HashMap + 双向链表
2. 操作：
   - get：从HashMap获取，移到链表头
   - put：如果存在更新，移到链表头；如果不存在，插入链表头，删除链表尾
3. Java实现：
   - LinkedHashMap（accessOrder=true）

---

## 第十八部分：综合场景题

### 问题57：设计一个高并发的订单系统，需要考虑哪些方面？

**回答思路**：
1. 架构设计：
   - 负载均衡
   - 服务拆分
   - 数据库分库分表
2. 性能优化：
   - 缓存热点数据
   - 异步处理订单
   - 数据库读写分离
3. 高可用：
   - 服务降级
   - 熔断机制
   - 限流保护
4. 数据一致性：
   - 分布式事务
   - 最终一致性
   - 补偿机制

### 问题58：如果让你重新设计这个项目，你会怎么做？

**回答思路**：
1. 架构升级：
   - 微服务架构
   - API网关统一鉴权
   - 服务注册与发现
2. 技术升级：
   - 使用XXL-Job替代SpringTask
   - 使用RabbitMQ处理异步任务
   - 使用Elasticsearch做搜索
3. 数据设计：
   - 分库分表
   - 读写分离
   - 数据归档

---

## 面试回答技巧总结

### 1. 回答结构（STAR法则）
- Situation：项目背景
- Task：具体任务
- Action：采取的行动（技术实现）
- Result：达到的结果（性能提升、问题解决）

### 2. 技术点回答模板
```
1. 问题背景：为什么需要这个技术
2. 技术选型：为什么选择这个方案
3. 实现细节：具体如何实现（结合代码）
4. 遇到的问题：实现过程中遇到的问题
5. 优化方案：如何改进和优化
```

### 3. 常见陷阱
- 不要只说"我用了XX"，要说"为什么用"和"怎么用"
- 不要只说优点，要承认问题和改进方向
- 不要编造数据，可以说相对数据
- 不要只说技术，要结合业务场景

### 4. 加分项
- 能说出当前实现的问题
- 能提出改进方案
- 能考虑分布式、高并发场景
- 能结合业务场景说明技术选型
- 能说出技术原理和底层实现

---

## 快速复习清单

### 必须掌握（核心）
- [ ] 项目整体介绍（2-3分钟）
- [ ] 四个技术点的实现原理
- [ ] 每个技术点的代码位置
- [ ] 订单业务流程
- [ ] 支付回调流程

### 建议掌握（扩展）
- [ ] Spring框架原理（IOC、AOP、事务）
- [ ] MyBatis原理
- [ ] Redis原理
- [ ] 数据库设计思路
- [ ] 异常处理机制

### 扩展了解（加分）
- [ ] 分布式场景下的问题
- [ ] 高并发场景的处理
- [ ] 微服务改造思路
- [ ] 性能优化方案
- [ ] 设计模式的应用

---

这份文档涵盖了项目面试的各个方面。建议：
1. 先熟悉项目代码，能快速定位
2. 准备每个技术点的2-3分钟介绍
3. 准备常见问题的标准答案
4. 准备扩展性问题的思路
5. 多练习，能够流畅表达

祝你面试顺利。