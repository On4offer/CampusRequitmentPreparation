# 📘 《Java 集合框架》学习笔记

系统梳理 Java 集合框架（JCF）的体系结构、List/Set/Map、迭代器、工具类与源码要点，便于系统学习与校招面试速查。

> **版本说明**：以 **JDK 8+** 为基准；涉及 `List.of()`/`Set.of()` 为 JDK 9+，ConcurrentHashMap 以 JDK 8 实现（数组+链表+红黑树，CAS+synchronized）为准。

------

# 🧩 第一章：集合框架概述

> **目标：**
>  掌握 Java 集合框架（JCF, Java Collections Framework）整体设计思想、层级结构、核心接口与分类，建立从“容器”角度理解数据结构的体系思维。

> **本章在整体中解决什么问题**：建立对 JCF 的整体认知——集合与数组的区别、Collection 与 Map 两大分支、接口+实现+算法的设计思想。学完本章后**第二章**讲 List/Set/Queue 等 Collection 体系；**第三章**讲 Map 体系；**第七章**会回到源码（ArrayList、HashMap 等），本章是选型与面试画图的基础。

------

## 🧠 1. 什么是集合（Collection）

### 📘 概念

在 Java 中，**集合（Collection）** 是用于存储、操作和传输对象的 **容器对象（Container）**。
 它用于替代数组，提供了更灵活的**动态扩容、元素查找、排序、去重、并发访问控制**等功能。

简单来说：

> **集合 = 可变长度的对象容器**

------

### 📦 与数组的区别

| 特性     | 数组（Array）    | 集合（Collection）                               |
| -------- | ---------------- | ------------------------------------------------ |
| 长度     | 固定             | 可变                                             |
| 元素类型 | 基本类型 / 对象  | 仅对象（引用类型）                               |
| 操作支持 | 仅索引访问       | 增删改查、遍历、排序、过滤等                     |
| 线程安全 | 否               | 部分集合支持（如 `Vector`、`ConcurrentHashMap`） |
| 泛型支持 | 无（需要包装类） | 有泛型，类型安全                                 |

------

### ✅ 示例

```java
// 数组
String[] arr = {"A", "B", "C"};

// 集合
List<String> list = new ArrayList<>();
list.add("A");
list.add("B");
list.add("C");
```

集合相比数组，更易于**动态扩展和操作对象集合**。

------

## 🎯 2. 集合框架的设计目标与优势

### 📌 设计目标

1. **统一接口**：为各种集合类型提供标准化接口（List、Set、Map等）。
2. **可扩展性**：允许自定义实现（如自定义HashMap、LRU缓存）。
3. **高性能**：内部采用高效数据结构（如哈希表、红黑树、链表）。
4. **算法与数据分离**：算法通过 `Collections` 工具类独立实现。
5. **类型安全与泛型支持**：从 JDK1.5 开始，集合全面引入泛型。

------

### 🌟 集合框架的优势

| 优势           | 说明                                             |
| -------------- | ------------------------------------------------ |
| 🧱 统一性       | 所有集合遵循统一的接口规范（Collection / Map）   |
| 🔄 可扩展性     | 自定义实现接口即可创建新集合                     |
| ⚡ 高效性       | HashMap、ArrayList 等经过优化的算法实现          |
| 🔒 安全性       | 支持同步包装（`Collections.synchronizedList()`） |
| 🧩 泛型支持     | 保证类型安全，避免强制类型转换                   |
| 🔍 丰富算法支持 | 排序、搜索、打乱、同步化、不可变集合等           |

------

## 🏗️ 3. Java 集合框架的体系结构图

### 📊 逻辑层级图（简化版）

```text
                ┌────────────┐
                │ Collection │
                └─────┬──────┘
                      │
       ┌──────────────┼──────────────┐
       │              │              │
    ┌───────┐     ┌───────┐     ┌─────────┐
    │  List │     │  Set  │     │  Queue  │
    └───────┘     └───────┘     └─────────┘
       │              │              │
 ArrayList       HashSet        LinkedList
 LinkedList      TreeSet        PriorityQueue
 Vector          LinkedHashSet  ArrayDeque

                   ┌────────────┐
                   │    Map     │
                   └─────┬──────┘
                         │
          ┌──────────────┼──────────────┐
          │              │              │
       HashMap       LinkedHashMap   TreeMap
                         │
                   ConcurrentHashMap
```

> 📌 说明：
>
> - `Collection` 是所有单值容器（List / Set / Queue）的顶层接口。
> - `Map` 独立存在，存储的是 **键值对 (key-value)**。

------

## ⚖️ 4. Collection 与 Map 的区别

| 对比项       | Collection                     | Map                                 |
| ------------ | ------------------------------ | ----------------------------------- |
| 存储结构     | 单值元素集合                   | 键值对集合                          |
| 元素访问     | 通过迭代器或下标               | 通过 key 访问                       |
| 接口层级     | List、Set、Queue               | HashMap、TreeMap、LinkedHashMap     |
| 是否允许重复 | List 允许，Set 不允许          | key 不可重复，value 可重复          |
| 典型实现类   | ArrayList、HashSet、LinkedList | HashMap、TreeMap、ConcurrentHashMap |

------

### 💡 小例子对比

```java
// Collection 示例：List
List<String> names = new ArrayList<>();
names.add("Tom");
names.add("Jerry");
System.out.println(names.get(0)); // Tom

// Map 示例：Key-Value 存储
Map<String, Integer> score = new HashMap<>();
score.put("Tom", 90);
score.put("Jerry", 85);
System.out.println(score.get("Tom")); // 90
```

------

## 🧱 5. 集合框架的常见接口层级结构

### 🌳 Collection 接口体系

| 接口      | 特点           | 常见实现类                      |
| --------- | -------------- | ------------------------------- |
| **List**  | 有序，可重复   | ArrayList, LinkedList, Vector   |
| **Set**   | 无序，不可重复 | HashSet, LinkedHashSet, TreeSet |
| **Queue** | 先进先出       | LinkedList, PriorityQueue       |
| **Deque** | 双端队列       | ArrayDeque, LinkedList          |

------

### 🧩 Map 接口体系

| 接口              | 特点                         | 常见实现类                      |
| ----------------- | ---------------------------- | ------------------------------- |
| **Map**           | key-value 结构，key 不可重复 | HashMap, TreeMap, LinkedHashMap |
| **ConcurrentMap** | 支持并发访问                 | ConcurrentHashMap               |
| **SortedMap**     | 可排序 Map                   | TreeMap                         |

------

## 🧠 6. JCF（Java Collections Framework）的设计思想

### ✳️ 设计核心：**接口 + 实现 + 算法**

| 模块                          | 说明                   | 示例                                      |
| ----------------------------- | ---------------------- | ----------------------------------------- |
| **接口（Interfaces）**        | 定义行为规范           | Collection、List、Set、Map                |
| **实现类（Implementations）** | 提供具体的数据结构     | ArrayList、HashMap、TreeSet               |
| **算法（Algorithms）**        | 通过工具类实现操作逻辑 | Collections.sort()、Collections.shuffle() |

------

### 💡 示例：统一算法操作不同实现类

```java
List<Integer> nums = new ArrayList<>(List.of(3, 1, 2));
Collections.sort(nums);   // ArrayList 排序

Set<Integer> set = new TreeSet<>(nums);
Collections.reverse(nums); // 同样适用于 List
```

> 同一个算法可作用于不同集合实现类 → **统一接口设计的强大威力！**

------

# ✅ 本章小结

| 知识点     | 面试关键词                     | 实际应用         |
| ---------- | ------------------------------ | ---------------- |
| 集合定义   | 动态对象容器、替代数组         | 存储、遍历、查找 |
| 设计目标   | 统一接口、高性能、类型安全     | JCF 选型         |
| 框架结构   | Collection、Map 两大分支       | List/Set/Queue/Map |
| 设计思想   | 接口 + 实现 + 算法             | Collections 工具类 |

------

**学习要点**：能画出 Collection 与 Map 的体系简图；能说出集合与数组的区别及 JCF 的设计思想。

------

## 🎯 面试常见追问

| 面试官提问           | 回答思路 |
| -------------------- | -------- |
| 集合和数组的区别？   | 数组长度固定、可存基本类型；集合长度可变、仅对象、支持丰富操作与泛型，部分实现线程安全。 |
| 为什么 HashMap 用数组+链表+红黑树？ | 数组按 hash 定位；冲突用链表；链表过长（如≥8）转红黑树，将查找从 O(n) 降为 O(log n)。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| 把 Collection 和 Map 混为一谈 | 一个是单值容器，一个是键值对；顶层接口不同 | Collection 是 List/Set/Queue 的父接口；Map 独立，存 key-value。 |
| 面试画图只画实现类不画接口 | 容易漏掉 List/Set/Map 与实现类的关系 | 先画 Collection/Map，再画 List/Set/Queue 及 HashMap 等实现，体现「接口+实现」设计。 |
| 记不清 Set 是否有序 | Set 接口不保证顺序；实现类不同：HashSet 无序，LinkedHashSet 插入序，TreeSet 有序 | 问「Set 有序吗」要答：看实现——HashSet 无序，TreeSet 有序，LinkedHashSet 插入序。 |

------

# 🧩 第二章：Collection 接口体系

> **目标：**
>  通过学习本章内容，掌握 Java 集合框架中的四个主要接口 **List、Set、Queue、Deque**，理解其核心实现类、性能特性以及适用场景，形成良好的集合使用习惯。

> **本章在整体中解决什么问题**：第一章给出了 JCF 全貌；本章落实 **Collection 分支**——List（ArrayList/LinkedList）、Set（HashSet/TreeSet）、Queue/Deque 的用法与选型。学完本章后**第三章**讲 Map 体系；**第四章**讲迭代器与遍历（含 fail-fast）；**第七章**会分析 ArrayList 等源码。

------

## 🧠 2.1 **Collection 总览**

### 📘 基本功能

Java 中的 **Collection** 接口是所有集合类的父接口，定义了常见的集合操作方法，包括：

- **增**：`add(E e)` / `addAll(Collection<? extends E> c)`
- **删**：`remove(Object o)` / `clear()`
- **改**：`replaceAll(UnaryOperator<E> operator)`
- **查**：`contains(Object o)` / `containsAll(Collection<?> c)` / `size()`
- **迭代**：`iterator()` / `forEach(Consumer<? super E> action)`
- **批量操作**：`retainAll(Collection<?> c)` / `removeAll(Collection<?> c)`

------

### 🧩 **Collection 常见子接口**

1. **List**：有序，可重复，支持按索引访问元素
2. **Set**：无序，不可重复
3. **Queue**：先进先出（FIFO）数据结构
4. **Deque**：双端队列，支持两端插入和删除元素

------

## 🧳 2.2 **List 接口与实现类**

### 📌 **1. ArrayList**

#### 🧠 底层结构

- `ArrayList` 是基于动态数组实现的集合。
- 它的底层是一个对象数组，通过 `Object[] elementData` 存储元素。

#### 📈 扩容机制与时间复杂度

- 默认初始容量为 10，当元素个数超过容量时，`ArrayList` 会扩容，扩容为原数组大小的 1.5 倍。
- 扩容的时间复杂度是 **O(n)**，因为需要创建新的数组并将旧数组元素复制过来。

#### 🔒 线程安全问题

- `ArrayList` 不是线程安全的。
- 如果需要在多线程环境下使用线程安全的列表，可以使用 `CopyOnWriteArrayList`，它在每次修改时都会复制一份新的列表。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
System.out.println(list.get(0)); // 输出: Java
```

------

### 📌 **2. LinkedList**

#### 🧠 底层结构

- `LinkedList` 是基于双向链表实现的集合，每个元素包含前后指针。
- 提供 O(1) 时间复杂度的元素插入和删除操作。

#### ⚙️ 实现队列与栈的功能

- `LinkedList` 实现了 **Queue** 和 **Deque** 接口，可以作为队列或双端队列使用。

#### 🏗️ 示例代码

```java
Queue<String> queue = new LinkedList<>();
queue.add("A");
queue.add("B");
System.out.println(queue.poll()); // 输出: A

Deque<String> deque = new LinkedList<>();
deque.addFirst("C");
deque.addLast("D");
System.out.println(deque.removeFirst()); // 输出: C
```

------

### 📌 **3. Vector（旧）**

#### 🧠 底层结构

- `Vector` 类似于 `ArrayList`，底层也是数组。
- 不同之处在于，`Vector` 是 **同步的**，适用于线程安全的场景。

#### ⚡ 性能缺陷

- 由于同步开销，`Vector` 的性能较 `ArrayList` 差，且不再推荐使用。

#### 🏗️ 示例代码

```java
Vector<String> vector = new Vector<>();
vector.add("Old");
vector.add("Vector");
System.out.println(vector.get(0)); // 输出: Old
```

> 🚨 **注意**：建议使用 `ArrayList` 代替 `Vector`，如果需要线程安全，可以考虑使用 `CopyOnWriteArrayList`。

------

## 🧳 2.3 **Set 接口与实现类**

### 📌 **1. HashSet**

#### 🧠 底层结构

- `HashSet` 基于 `HashMap` 实现，存储元素时通过计算元素的 **hashCode** 决定元素位置。
- `HashSet` 不保证元素的顺序。

#### ⚙️ HashCode 与 equals 的关系

- 插入元素时，`HashSet` 会根据 **hashCode** 来定位桶，如果发生哈希冲突，则通过 **equals** 方法进一步比较元素是否相等。

#### 🏗️ 示例代码

```java
Set<String> set = new HashSet<>();
set.add("Java");
set.add("Python");
set.add("Java"); // 重复元素不会加入
System.out.println(set); // 输出: [Java, Python]
```

------

### 📌 **2. LinkedHashSet**

#### 🧠 底层结构

- `LinkedHashSet` 基于 `HashMap` 实现，除了存储元素外，还维护一个双向链表，用于保持插入顺序。
- 保证插入元素的顺序。

#### ⚙️ 应用场景：LRU 缓存

- 因为元素是按插入顺序存储的，`LinkedHashSet` 可以用于实现 LRU 缓存。

#### 🏗️ 示例代码

```java
Set<String> linkedSet = new LinkedHashSet<>();
linkedSet.add("A");
linkedSet.add("B");
linkedSet.add("C");
System.out.println(linkedSet); // 输出: [A, B, C]
```

------

### 📌 **3. TreeSet**

#### 🧠 底层结构

- `TreeSet` 基于 **红黑树** 实现，元素是 **有序** 的。
- 支持 **自然顺序** 或 **Comparator** 排序。

#### ⚙️ 可排序与自定义 Comparator

- 可以通过 `Comparator` 自定义排序规则。

#### 🏗️ 示例代码

```java
Set<Integer> treeSet = new TreeSet<>();
treeSet.add(5);
treeSet.add(1);
treeSet.add(3);
System.out.println(treeSet); // 输出: [1, 3, 5]
```

------

## 🧳 2.4 **Queue / Deque 接口与实现类**

### 📌 **1. Queue 与优先级队列 PriorityQueue**

#### 🧠 底层结构

- `Queue` 是先进先出（FIFO）数据结构，用于存储等待处理的元素。
- `PriorityQueue` 是一个 **基于堆** 的优先队列，按照元素的自然顺序或自定义比较器顺序排序。

#### 🏗️ 示例代码

```java
Queue<Integer> priorityQueue = new PriorityQueue<>();
priorityQueue.add(3);
priorityQueue.add(1);
priorityQueue.add(2);
System.out.println(priorityQueue.poll()); // 输出: 1（优先级最低的）
```

------

### 📌 **2. Deque 与 ArrayDeque**

#### 🧠 底层结构

- `Deque` 是双端队列，支持从两端插入和删除元素。
- `ArrayDeque` 是基于数组实现的 **可扩展** 双端队列。

#### 🏗️ 示例代码

```java
Deque<String> deque = new ArrayDeque<>();
deque.addFirst("A");
deque.addLast("B");
System.out.println(deque.removeFirst()); // 输出: A
System.out.println(deque.removeLast()); // 输出: B
```

------

### 📌 **3. BlockingQueue（并发包中的阻塞队列）**

#### 🧠 主要实现类

1. **LinkedBlockingQueue**：一个基于链表的阻塞队列，支持容量限制。
2. **ArrayBlockingQueue**：一个基于数组的阻塞队列，支持容量限制。
3. **DelayQueue**：支持延迟任务的队列，元素在到期前不可消费。
4. **PriorityBlockingQueue**：优先级队列，按照元素的优先级排序。

#### 🏗️ 示例代码

```java
BlockingQueue<Integer> queue = new LinkedBlockingQueue<>();
queue.put(1);  // 阻塞方法，直到队列有空间
System.out.println(queue.take());  // 阻塞方法，直到队列有元素
```

------

# ✅ 本章小结

| 知识点   | 面试关键词                           | 实际应用         |
| -------- | ------------------------------------ | ---------------- |
| List     | ArrayList 数组/扩容 1.5 倍、LinkedList 双向链表 | 有序可重复、按索引 |
| Set      | HashSet/HashMap、TreeSet/红黑树、LinkedHashSet 有序 | 去重、排序、保序 |
| Queue/Deque | PriorityQueue 堆、ArrayDeque 数组双端、BlockingQueue | 任务队列、栈、生产者消费者 |

------

**学习要点**：能说出 ArrayList 与 LinkedList 的区别及适用场景；能说出 HashSet、TreeSet、LinkedHashSet 的区别；了解 BlockingQueue 的典型实现。

------

## 🎯 面试常见追问

| 面试官提问               | 回答思路 |
| ------------------------ | -------- |
| ArrayList 和 LinkedList 区别？ | ArrayList 基于数组，随机访问 O(1)，尾部增删 O(1) 均摊；LinkedList 基于双向链表，头尾增删 O(1)，随机访问 O(n)。 |
| HashSet 如何保证不重复？  | 基于 HashMap，用元素的 hashCode 与 equals；先比 hash 再 equals，都相同则视为重复不插入。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| 需要随机访问仍用 LinkedList | LinkedList 按索引访问要遍历，O(n) | 随机访问多选 ArrayList；头尾增删多、少按索引访问选 LinkedList。 |
| 多线程下用 ArrayList/HashSet 等非线程安全集合 | 并发写会导致数据错乱或 CME | 用 ConcurrentHashMap、CopyOnWriteArrayList，或用 Collections.synchronizedXxx 包装并注意迭代时加锁。 |
| 在循环里频繁用 get(i) 遍历 LinkedList | 每次 get 都是 O(n) | 用 Iterator 或 for-each，一次遍历 O(n)；不要 for (i=0; i<list.size(); i++) list.get(i)。 |
| 需要有序 Set 却用 HashSet | HashSet 无序 | 要插入序用 LinkedHashSet，要排序用 TreeSet（或带比较器）。 |

------

# 🧩 第三章：Map 接口体系

> **目标：**
>  通过学习本章内容，掌握 Java 中 **Map 接口及其实现类**，理解其底层结构、扩容机制、性能特点、线程安全及应用场景。

> **本章在整体中解决什么问题**：在第二章 Collection 之后，本章讲 **Map 分支**——HashMap、TreeMap、LinkedHashMap、ConcurrentHashMap 的用法与区别。学完本章后**第四章**讲迭代与 fail-fast；**第七章**会深入 HashMap/ConcurrentHashMap 的 put、扩容、树化等源码，本章是「会用」的前提。

------

## 🧠 1. **Map 接口概览**

### 📘 **键值对存储**

- `Map` 是一种 **键值对** 数据结构，其中每个元素由 **键（key）** 和 **值（value）** 组成，且 **key** 是唯一的。
- 常见应用：缓存、字典、计数器等。

### 📈 **常见操作与遍历方式**

1. **增**：`put(K key, V value)`
2. **查**：`get(Object key)`
3. **删**：`remove(Object key)`
4. **修改**：`replace(K key, V oldValue, V newValue)`
5. **包含检查**：`containsKey(Object key)` / `containsValue(Object value)`
6. **遍历方式**：
   - `entrySet()`：返回 `Set<Map.Entry<K,V>>`，用于遍历键值对。
   - `keySet()`：返回 `Set<K>`，用于遍历键。
   - `values()`：返回 `Collection<V>`，用于遍历值。

#### 🏗️ 示例代码

```java
Map<String, Integer> map = new HashMap<>();
map.put("Apple", 1);
map.put("Banana", 2);
map.put("Cherry", 3);

System.out.println(map.get("Apple")); // 输出: 1
map.remove("Banana");
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}
```

------

## 🧳 2. **HashMap**

### 📘 **底层数据结构：数组 + 链表 + 红黑树**

- `HashMap` 底层由 **数组** 和 **链表** 组成。当发生哈希冲突时，多个元素会被存储在同一个数组位置的 **链表** 中。
- 如果链表长度超过一定阈值（默认为 8），则会 **树化**，转为红黑树，以提升查找效率。

### ⚡ **扩容机制与负载因子**

- 默认负载因子：0.75，当当前容量达到 `threshold = capacity * loadFactor` 时，`HashMap` 会扩容，通常扩容为原来的两倍。
- 扩容操作是 **O(n)**，因为需要将原数组中的元素重新计算哈希并复制到新数组。

### 🔄 **hash 冲突与链表树化**

- 当多个元素的哈希值相同（即哈希冲突），`HashMap` 会将它们存储在同一个桶（bucket）中，使用 **链表** 或 **红黑树** 处理冲突。
- 树化机制：当冲突的链表长度超过阈值（默认为 8），链表会转换为红黑树，时间复杂度降低为 **O(log n)**。

#### 🏗️ 示例代码

```java
Map<String, Integer> hashMap = new HashMap<>();
hashMap.put("Java", 10);
hashMap.put("Python", 20);
hashMap.put("C", 30);
System.out.println(hashMap.get("Java"));  // 输出: 10
```

------

## 🧳 3. **LinkedHashMap**

### 📘 **访问顺序与 LRU 缓存实现**

- `LinkedHashMap` 继承自 `HashMap`，并额外维护了一个 **双向链表**，以保证插入元素的顺序或访问顺序（按 `accessOrder`）。
- 默认按 **插入顺序** 排列元素，但可以通过设置 `accessOrder = true` 来按访问顺序排列元素。

### ⚡ **应用场景：LRU 缓存**

- `LinkedHashMap` 可以通过重写 `removeEldestEntry()` 方法实现 **LRU（Least Recently Used）** 缓存，即在缓存超过一定容量时，自动移除最少使用的元素。

#### 🏗️ 示例代码

```java
Map<String, Integer> linkedHashMap = new LinkedHashMap<>(16, 0.75f, true);
linkedHashMap.put("Java", 10);
linkedHashMap.put("Python", 20);
linkedHashMap.put("C", 30);
linkedHashMap.put("JavaScript", 40);

// 访问某个元素
linkedHashMap.get("Java");
linkedHashMap.put("Ruby", 50);

// 删除最旧的元素
linkedHashMap.entrySet().removeIf(entry -> entry.getValue() == 10); // 通过条件删除
System.out.println(linkedHashMap);
```

------

## 🧳 4. **TreeMap**

### 📘 **基于红黑树实现的有序 Map**

- `TreeMap` 是 **有序的** `Map` 实现，基于 **红黑树** 数据结构。
- 默认情况下，`TreeMap` 按照 **键的自然顺序** 排序，或者可以通过传入自定义的 `Comparator` 实现排序。

### ⚙️ **自定义排序规则**

- 使用 `Comparator` 允许对 `TreeMap` 进行自定义排序，例如按 **降序** 排序。

#### 🏗️ 示例代码

```java
Map<Integer, String> treeMap = new TreeMap<>();
treeMap.put(1, "Java");
treeMap.put(3, "Python");
treeMap.put(2, "C");

System.out.println(treeMap); // 输出: {1=Java, 2=C, 3=Python}

// 自定义排序：按键降序
Map<Integer, String> descendingMap = new TreeMap<>((a, b) -> b - a);
descendingMap.put(1, "Java");
descendingMap.put(3, "Python");
descendingMap.put(2, "C");

System.out.println(descendingMap); // 输出: {3=Python, 2=C, 1=Java}
```

------

## 🧳 5. **Hashtable 与 ConcurrentHashMap**

### 📘 **同步机制比较**

1. **Hashtable**：
   - `Hashtable` 是 **线程安全** 的 `Map`，但其所有方法都被 `synchronized` 修饰，导致性能较差。
   - 不推荐使用，通常使用 `ConcurrentHashMap` 作为替代。
2. **ConcurrentHashMap**：
   - `ConcurrentHashMap` 提供了更高效的线程安全机制，通过 **分段锁**（Segment）来减少锁的竞争。
   - 在读操作时，**读操作无需加锁**，只有在写操作时才进行锁定。

### ⚡ **分段锁与 CAS 优化**

- `ConcurrentHashMap` 使用 **分段锁** 技术，将数据分成多个段，每个段独立加锁，保证高并发情况下的性能。
- 通过 **CAS（Compare and Swap）** 实现非阻塞更新操作，减少了传统锁机制的性能开销。

#### 🏗️ 示例代码

```java
Map<String, Integer> concurrentMap = new ConcurrentHashMap<>();
concurrentMap.put("Java", 10);
concurrentMap.put("Python", 20);

System.out.println(concurrentMap.get("Java"));  // 输出: 10
```

> 🚨 **注意**：对于多线程高并发环境，推荐使用 `ConcurrentHashMap`，而不推荐使用 `Hashtable`。

------

# ✅ 本章小结

| 知识点   | 面试关键词                                   | 实际应用     |
| -------- | -------------------------------------------- | ------------ |
| HashMap  | 数组+链表+红黑树、负载因子 0.75、树化阈值 8  | 缓存、字典   |
| LinkedHashMap | 双向链表、插入/访问顺序、removeEldestEntry | LRU 缓存     |
| TreeMap  | 红黑树、有序、Comparator                     | 范围查询、排序 |
| ConcurrentHashMap | JDK8 锁桶+CAS、非 Hashtable 全表锁       | 高并发缓存   |

------

**学习要点**：能说出 HashMap 的 put/扩容/树化流程；能说出与 Hashtable、ConcurrentHashMap 的区别；能用 LinkedHashMap 实现简单 LRU。

------

## 🎯 面试常见追问

| 面试官提问                 | 回答思路 |
| -------------------------- | -------- |
| HashMap 负载因子为什么 0.75？ | 权衡空间与冲突：过小扩容频繁，过大冲突多；0.75 是经验值，兼顾查找与空间。 |
| ConcurrentHashMap 如何保证线程安全？ | JDK8 用 CAS 插空桶、synchronized 锁桶头，锁粒度小；读多无锁。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| 用可变对象做 HashMap key 且放入后修改了该对象 | 修改后 hashCode 变，get 可能找不到或行为不确定 | 做 key 的对象要不可变（如 String、Integer），或放入后绝不修改影响 hashCode/equals 的字段。 |
| 多线程写用 HashMap | 并发 put 可能死循环（JDK7 头插）、数据丢失 | 多线程写用 ConcurrentHashMap；只读多写少可考虑 CopyOnWrite 思路。 |
| 需要顺序却用 HashMap | HashMap 不保证顺序 | 插入序用 LinkedHashMap；键排序用 TreeMap。 |
| 忘记 HashMap 允许 null key/value 而 ConcurrentHashMap 不允许 | 混用时 NPE | ConcurrentHashMap 不允许 null key/value；HashMap 允许一个 null key、多个 null value。 |

------

# 🧩 第四章：迭代器与遍历机制

> **目标：**
>  理解 Java 中集合的遍历机制，掌握 `Iterator`、`ListIterator`、`foreach` 等遍历方式的使用，了解并解决 `ConcurrentModificationException` 错误，学习如何在多线程环境下安全地遍历集合。

> **本章在整体中解决什么问题**：在第二、三章「会用」List/Set/Map 之后，本章讲**如何安全遍历**——Iterator、foreach、fail-fast 原理及遍历时修改导致的 CME。学完本章后**第五章**讲 Collections/Arrays 工具类；**第七章**源码中会涉及 modCount 与 fail-fast 的实现。

------

## 🧠 1. **Iterator 接口与 fail-fast 原理**

### 📘 **Iterator 接口概览**

- `Iterator` 是 Java 集合框架中用于 **遍历** 集合的标准接口。
- 它提供了三种基本方法：
  - `boolean hasNext()`：判断是否还有下一个元素。
  - `E next()`：返回下一个元素。
  - `void remove()`：删除当前元素（可选操作）。

### 🔐 **fail-fast 原理**

- **fail-fast** 是一种机制，当集合在遍历过程中被结构性修改（例如元素被添加或删除）时，`Iterator` 会在下一次操作时抛出 **`ConcurrentModificationException`** 异常。***与其让程序在不确定状态下继续运行，不如立即失败***
- 该机制的目的是防止遍历过程中发生不一致的修改，确保遍历和修改操作的安全性。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
list.add("C");

Iterator<String> iterator = list.iterator();
while (iterator.hasNext()) {
    String element = iterator.next();
    if ("Python".equals(element)) {
        list.remove(element);  // 修改集合，触发 ConcurrentModificationException
    }
}
```

在上面的代码中，`list.remove(element)` 会导致 `ConcurrentModificationException` 异常，因为我们在使用 `Iterator` 遍历时修改了集合。

------

## 🧳 2. **ListIterator 特性与双向遍历**

### 📘 **ListIterator 概览**

- `ListIterator` 是 `Iterator` 的子接口，专门用于 **`List`** 类型的集合。
- 它支持双向遍历，可以向前和向后遍历元素。
- `ListIterator` 提供了以下方法：
  - `boolean hasNext()`：判断是否还有下一个元素。
  - `E next()`：返回下一个元素。
  - `boolean hasPrevious()`：判断是否还有前一个元素。
  - `E previous()`：返回前一个元素。
  - `void add(E e)`：在当前元素前插入元素。
  - `void set(E e)`：替换当前元素。

### 🔄 **双向遍历的优势**

- `ListIterator` 支持 **双向遍历**，不仅可以遍历列表的元素，还可以在遍历过程中修改元素或进行插入操作。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
list.add("C");

ListIterator<String> iterator = list.listIterator();
while (iterator.hasNext()) {
    String element = iterator.next();
    System.out.println(element); // 向前遍历
}

while (iterator.hasPrevious()) {
    String element = iterator.previous();
    System.out.println(element); // 向后遍历
}
```

------

## 🧳 3. **foreach 与 Iterable**

### 📘 **foreach 与 Iterable 接口**

- `foreach` 是 Java 5 引入的语法糖，简化了集合的遍历操作。
- 通过 `Iterable` 接口，所有实现了 `Iterable` 接口的集合类（如 `List`、`Set`）可以使用 `foreach` 进行遍历。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
list.add("C");

// 使用 foreach 遍历集合
list.forEach(element -> System.out.println(element));
```

- `foreach` 的好处是简洁、易于理解，并且可以与 Lambda 表达式结合使用，实现灵活的遍历操作。

------

## 🧳 4. **ConcurrentModificationException 触发机制**

### 📘 **触发机制概览**

- `ConcurrentModificationException` 异常会在以下情况下被触发：
  1. 在使用 `Iterator` 遍历集合时，集合被其他线程修改（例如，删除或添加元素）。
  2. 在单线程操作时，遍历集合时集合结构被修改（例如，使用 `for-each` 遍历时修改集合）。

### ⚡ **为什么会抛出这个异常**

- Java 的集合类在遍历过程中会保存一个 **modCount** 变量，表示集合结构的修改次数。每当结构发生变化时，`modCount` 会自增。
- 当 `Iterator` 在遍历过程中发现 `modCount` 的值和它创建时保存的值不一致时，就会抛出 `ConcurrentModificationException`。
- 任何遍历过程中对原集合的结构性修改都会触发异常；与是否多线程无关，单线程自己修改也会触发
- 只有通过迭代器自身的修改方法才是安全的（`iterator.remove(); list.removeIf(item -> item.equals("B"))`）

------

## 🧳 5. **安全遍历的几种方式**

### 📘 **1. CopyOnWriteArrayList**

- `CopyOnWriteArrayList` 是一种线程安全的列表实现，适用于 **读多写少** 的场景。
- 它通过 **写时复制** 的策略来实现线程安全：每次写操作都会复制一份新的数组，这样不会影响正在进行的遍历操作。

#### 🏗️ 示例代码

```java
List<String> list = new CopyOnWriteArrayList<>();
list.add("Java");
list.add("Python");

Iterator<String> iterator = list.iterator();
while (iterator.hasNext()) {
    String element = iterator.next();
    list.add("C"); // 在遍历时安全地修改集合
}
```

- `CopyOnWriteArrayList` 适用于需要频繁遍历且修改较少的场景，例如缓存、监听器集合等。

### 📘 **2. ConcurrentHashMap**

- `ConcurrentHashMap` 是线程安全的 `Map`，它支持并发读操作，不会在读操作时加锁。对于写操作（如 `put` 或 `remove`），它通过 **分段锁**（Segment Locking）来提高并发性能。
- `ConcurrentHashMap` 通过 **CAS**（Compare and Swap）优化操作，避免了锁的竞争。

#### 🏗️ 示例代码

```java
Map<String, Integer> map = new ConcurrentHashMap<>();
map.put("Java", 10);
map.put("Python", 20);

map.forEach((key, value) -> System.out.println(key + ": " + value)); // 安全遍历
```

- `ConcurrentHashMap` 适用于多线程环境中，需要在遍历的同时执行写操作的场景。

### 📘 **3. 使用 `synchronized` 块同步集合**

- 如果不使用 `CopyOnWriteArrayList` 或 `ConcurrentHashMap`，可以使用 **同步块** 来保证线程安全。在遍历集合时，使用 `synchronized` 来控制对集合的访问。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");

synchronized (list) {
    for (String element : list) {
        System.out.println(element);
    }
}
```

- `synchronized` 可以保证在遍历过程中不会发生集合结构的修改，避免触发 `ConcurrentModificationException`。

------

# ✅ 本章小结

| 知识点     | 面试关键词                           | 实际应用         |
| ---------- | ------------------------------------ | ---------------- |
| Iterator   | hasNext/next/remove、fail-fast        | 遍历时安全删除   |
| fail-fast  | modCount、遍历中结构修改抛 CME        | 避免用 for-each 时直接增删集合 |
| 安全遍历   | 迭代器 remove、CopyOnWrite、Concurrent、synchronized | 多线程或遍历中修改 |

------

**学习要点**：能解释 ConcurrentModificationException 的触发原因；能说出遍历时安全修改的几种方式（iterator.remove、CopyOnWriteArrayList、ConcurrentHashMap、synchronized）。

------

## 🎯 面试常见追问

| 面试官提问               | 回答思路 |
| ------------------------ | -------- |
| fail-fast 是什么？       | 迭代器在遍历时检查 modCount，若集合被结构性修改则抛 ConcurrentModificationException，及早失败。 |
| 遍历时如何安全删除元素？ | 用 Iterator.remove()；或 removeIf；或多线程下用 CopyOnWriteArrayList/ConcurrentHashMap，或 synchronized 包住遍历与修改。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| for-each 里直接 remove 元素 | 会触发 fail-fast，抛 ConcurrentModificationException | 用 Iterator.remove()，或 removeIf(predicate)，或复制一份再删。 |
| 误以为 fail-safe 就绝对安全 | fail-safe 是迭代副本，读到的可能是旧数据 | CopyOnWrite 等迭代的是快照，修改对当前迭代不可见；要语义正确需理解「快照」含义。 |
| 多线程下边迭代边修改未做同步 | 非线程安全集合会 CME 或未定义行为 | 遍历时用 synchronized 包住整个迭代与修改，或改用并发集合。 |

------

# 🧩 第五章：工具类与算法支持

> **目标：**
>  理解 Java 集合框架中的常用工具类 `Collections` 和 `Arrays` 的使用，掌握常见集合操作（如排序、反转、同步化）的方法，学习如何创建不可变集合及其应用。

> **本章在整体中解决什么问题**：前几章讲的是单种集合的用法；本章讲**跨集合的算法与工具**——Collections.sort、同步包装、不可变集合，以及 Arrays.asList 的注意点。学完本章后**第六章**讲泛型与类型安全；**第八章**实战中会用到工具类与不可变集合。

------

## 🧠 1. **`Collections` 工具类常用方法**

### 📘 **概述**

`Collections` 是一个 **静态工具类**，提供了许多常见的集合操作方法，能够简化集合的常规操作，避免手动实现这些常见功能。

### 🔧 **常用方法**

1. **排序（sort）**

   - 用于对 `List` 进行排序，可以使用自然顺序或自定义比较器。

   #### 示例代码

   ```java
   List<Integer> list = Arrays.asList(3, 1, 2, 5, 4);
   Collections.sort(list);  // 使用自然顺序排序
   System.out.println(list);  // 输出: [1, 2, 3, 4, 5]
   
   Collections.sort(list, Comparator.reverseOrder());  // 按降序排序
   System.out.println(list);  // 输出: [5, 4, 3, 2, 1]
   ```

2. **反转（reverse）**

   - 反转 `List` 中元素的顺序。

   #### 示例代码

   ```java
   List<String> list = Arrays.asList("A", "B", "C", "D");
   Collections.reverse(list);  // 反转顺序
   System.out.println(list);  // 输出: [D, C, B, A]
   ```

3. **填充（fill）**

   - 用指定元素填充整个 `List`。

   #### 示例代码

   ```java
   List<String> list = new ArrayList<>(Arrays.asList("A", "B", "C"));
   Collections.fill(list, "X");  // 将所有元素替换为 "X"
   System.out.println(list);  // 输出: [X, X, X]
   ```

4. **同步化（synchronizedList）**

   - 返回一个线程安全的 `List`，适用于多线程环境。

   #### 示例代码

   ```java
   List<String> list = new ArrayList<>();
   List<String> synchronizedList = Collections.synchronizedList(list);
   ```

5. **最大/最小值（max、min）**

   - 查找 `Collection` 中的最大值或最小值，可以使用自然顺序或自定义比较器。

   #### 示例代码

   ```java
   List<Integer> list = Arrays.asList(3, 1, 4, 1, 5, 9, 2, 6, 5);
   Integer max = Collections.max(list);  // 获取最大值
   Integer min = Collections.min(list);  // 获取最小值
   System.out.println("Max: " + max);  // 输出: Max: 9
   System.out.println("Min: " + min);  // 输出: Min: 1
   ```

------

## 🧳 2. **`Arrays` 工具类**

### 📘 **概述**

`Arrays` 是一个 **静态工具类**，提供了操作数组的常用方法，如数组排序、二分查找、转换为集合等。

### 🔧 **常用方法**

1. **数组与集合转换（asList）**

   - 将数组转换为 `List`。注意，返回的 `List` 只是原始数组的一个视图，无法改变其大小。

   #### 示例代码

   ```java
   String[] array = {"Java", "Python", "C"};
   List<String> list = Arrays.asList(array);
   System.out.println(list);  // 输出: [Java, Python, C]
   
   // 注意：不能修改 List 的大小
   // list.add("JavaScript");  // 会抛出 UnsupportedOperationException
   ```

2. **排序（sort）**

   - 对数组进行排序。

   #### 示例代码

   ```java
   int[] arr = {5, 2, 8, 1, 4};
   Arrays.sort(arr);  // 排序
   System.out.println(Arrays.toString(arr));  // 输出: [1, 2, 4, 5, 8]
   ```

3. **二分查找（binarySearch）**

   - 对已排序数组进行二分查找，返回目标元素的索引。

   #### 示例代码

   ```java
   int[] arr = {1, 2, 4, 5, 8};
   int index = Arrays.binarySearch(arr, 4);  // 查找元素 4
   System.out.println(index);  // 输出: 2
   ```

4. **填充（fill）**

   - 使用指定元素填充数组。

   #### 示例代码

   ```java
   int[] arr = new int[5];
   Arrays.fill(arr, 9);  // 将数组填充为 9
   System.out.println(Arrays.toString(arr));  // 输出: [9, 9, 9, 9, 9]
   ```

5. **比较（equals）**

   - 比较两个数组是否相等。

   #### 示例代码

   ```java
   int[] arr1 = {1, 2, 3};
   int[] arr2 = {1, 2, 3};
   boolean isEqual = Arrays.equals(arr1, arr2);  // 返回 true
   System.out.println(isEqual);  // 输出: true
   ```

------

## 🧳 3. **不可变集合与只读视图**

### 📘 **不可变集合概念**

不可变集合是指集合内容在创建后不能被修改。它们是 **线程安全** 的，因此非常适合在多线程环境下使用。

### 🔧 **`Collections.unmodifiableList()`**

- 返回一个不可修改的集合视图。如果对其进行修改，抛出 `UnsupportedOperationException` 异常。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");

List<String> unmodifiableList = Collections.unmodifiableList(list);
unmodifiableList.add("C");  // 会抛出 UnsupportedOperationException
```

### 🔧 **`List.of()` 与 `Set.of()`（JDK9+）**

- 从 JDK9 开始，`List.of()` 和 `Set.of()` 方法可以直接创建不可变集合。
- 这些集合是 **不可修改** 的，不能添加、删除或修改元素。

#### 🏗️ 示例代码

```java
List<String> immutableList = List.of("Java", "Python", "C");
Set<String> immutableSet = Set.of("Java", "Python", "C");

immutableList.add("JavaScript");  // 会抛出 UnsupportedOperationException
immutableSet.remove("Java");     // 会抛出 UnsupportedOperationException
```

### ⚡ **不可变集合的优点**

1. **线程安全**：由于集合内容无法改变，不存在多线程环境下的数据竞争。
2. **防止不小心修改**：不允许对集合进行任何修改，保证数据一致性。
3. **性能优化**：不可变集合通常会进行优化（如缓存）。

------

# ✅ 本章小结

| 知识点     | 面试关键词                                   | 实际应用         |
| ---------- | -------------------------------------------- | ---------------- |
| Collections | sort、reverse、fill、synchronizedList、max/min | 排序、同步包装   |
| Arrays     | asList、sort、binarySearch、equals           | 数组与 List 互转、查找 |
| 不可变集合 | unmodifiableList、List.of/Set.of（JDK9+）   | 配置、常量集合   |

------

**学习要点**：能说出 Arrays.asList() 返回列表不可增删（是数组视图）；能说出不可变集合的创建方式及适用场景。

------

## 🎯 面试常见追问

| 面试官提问           | 回答思路 |
| -------------------- | -------- |
| Arrays.asList() 的坑？ | 返回的是固定大小的列表视图，add/remove 会抛 UnsupportedOperationException；需可变时用 new ArrayList<>(Arrays.asList(...))。 |
| 不可变集合有啥用？   | 线程安全、防止误改、适合做缓存或配置；可用 Collections.unmodifiableXxx 或 List.of/Set.of。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| Arrays.asList 返回列表直接 add/remove | 返回的是固定大小视图， backed by 原数组 | 需要可变列表时用 `new ArrayList<>(Arrays.asList(...))`；JDK 9+ 可用 `List.of()` 得到不可变列表。 |
| 用 Collections.sort 排 Set | Set 无序，sort 只接受 List | 先转 List 再 sort：`new ArrayList<>(set)` 然后 `Collections.sort(list)`；或直接用 TreeSet。 |
| 不可变集合包装后仍持有原集合引用并修改 | 原集合被改，包装的「不可变」视图也会变 | 若希望真正不可变，用 List.of/Set.of 或先复制再包装；不要暴露原引用。 |

------

# 🧩 第六章：泛型与集合类型安全

> **目标：**
>  了解 Java 泛型的使用及其在集合中的作用，掌握类型擦除、通配符、边界限定、原始类型集合的风险等关键概念，确保集合的类型安全。

> **本章在整体中解决什么问题**：前面各章用到的 `List<String>`、`Map<K,V>` 都依赖泛型；本章系统讲**泛型在集合中的用法**——类型擦除、通配符、PECS，避免运行时 ClassCastException。与《泛型反射与注解》笔记中的泛型章互补；**第七章**源码里会看到擦除后的实际类型。

------

## 🧠 **1. 泛型在集合中的作用**

### 📘 **泛型概述**

- 泛型（Generics）是 Java 5 引入的特性，它允许在定义类、接口或方法时使用类型参数，从而使得代码更加通用和类型安全。
- 泛型的主要作用是 **提供类型安全**，在编译时就能检查出类型错误，避免了运行时类型转换错误。

### 🔧 **泛型在集合中的应用**

- 泛型使得集合可以存储特定类型的对象，确保集合中的元素类型一致，避免了强制类型转换。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
list.add("Python");
// list.add(10);  // 编译时错误，不能添加非 String 类型的元素
System.out.println(list);  // 输出: [Java, Python]
```

- 通过泛型，`List<String>` 确保了集合只能包含 `String` 类型的元素，这样就避免了类型转换异常。

------

## 🧳 **2. 类型擦除机制**

### 📘 **类型擦除概念**

- Java 泛型是通过 **类型擦除（Type Erasure）** 实现的，编译器会在编译时将泛型类型转换为原始类型。
- 在运行时，泛型不再存在，只留下原始类型的集合，所有泛型信息在编译后都会被擦除。

### ⚡ **类型擦除的具体实现**

- 例如，`List<String>` 和 `List<Integer>` 在编译后都会变成 `List`，运行时无法知道元素的实际类型。

#### 🏗️ 示例代码

```java
List<String> list1 = new ArrayList<>();
List<Integer> list2 = new ArrayList<>();
// 运行时，list1 和 list2 都是 List 类型，编译器无法区分它们
System.out.println(list1.getClass() == list2.getClass());  // 输出: true
```

- 类型擦除机制使得 **泛型代码兼容旧版本 Java**，但也导致了运行时无法获取泛型的类型信息。

------

## 🧳 **3. 通配符与边界限定**

### 📘 **通配符（Wildcard）概述**

- Java 泛型提供了通配符 `?`，表示 **未知类型**。它常用于方法中，表示可以接受不同类型的集合。

#### 🧑‍💻 **上界通配符 `? extends T`**

- `? extends T` 表示接受任何 `T` 或其子类的类型。

#### 🧑‍💻 **下界通配符 `? super T`**

- `? super T` 表示接受任何 `T` 或其父类的类型。

### ⚡ **边界限定的应用**

- 上界通配符常用于 **只读取** 集合元素的场景。
- 下界通配符常用于 **写入** 集合元素的场景。

#### 🏗️ 示例代码

```java
// 上界通配符：只读操作
public static void printList(List<? extends Number> list) {
    for (Number n : list) {
        System.out.println(n);
    }
}

// 下界通配符：写入操作
public static void addIntegers(List<? super Integer> list) {
    list.add(1);  // 只允许添加 Integer 或其子类元素
    // list.add(1.5);  // 编译时错误，不能添加 Double 类型
}
```

- 使用通配符时，尽量避免在方法中修改集合的元素，尤其是当集合的类型是 `? extends T` 时。

------

## 🧳 **4. 原始类型集合的风险**

### 📘 **原始类型集合概述**

- 在使用泛型时，如果没有指定具体的类型参数，就会导致原始类型的集合。
- 原始类型集合会导致 **类型安全** 问题，因为它会绕过泛型检查，使得元素类型不受限制。

#### 🧑‍💻 **原始类型集合的示例**

```java
List list = new ArrayList();
list.add("Java");
list.add(10);  // 添加了不同类型的元素，编译时不会报错

String str = (String) list.get(0);  // 强制类型转换，可能导致 ClassCastException
```

### ⚡ **原始类型集合的风险**

- 使用原始类型集合时，编译器无法检测到类型不匹配，可能导致运行时的类型转换异常（如 `ClassCastException`）。

### 🔒 **避免原始类型集合的建议**

- 总是使用泛型来定义集合，避免使用原始类型集合。

#### 🏗️ 示例代码

```java
List<String> list = new ArrayList<>();
list.add("Java");
// list.add(10);  // 编译时错误，类型不匹配
```

------

## 🧳 **5. 泛型数组与集合协变问题**

### 📘 **泛型数组问题**

- 在 Java 中，**泛型数组** 是不允许创建的，因为泛型的 **协变性**（子类可以替代父类）会导致类型不匹配问题。

#### 🧑‍💻 **泛型数组的问题示例**

```java
List<String>[] listArray = new List<String>[10];  // 编译错误
```

- 这是因为泛型类型的信息在运行时会被擦除，无法确定元素类型，导致无法创建泛型数组。

### ⚡ **解决方案**

- 可以使用 **`ArrayList`** 或 **`List`** 来代替泛型数组：

```java
List<List<String>> listOfLists = new ArrayList<>();
```

- 另外，还可以使用 **`Object[]`** 来创建数组，并通过类型转换来实现类型安全，但这会丧失泛型带来的类型检查优势。

------

# ✅ 本章小结

| 知识点     | 面试关键词                           | 实际应用     |
| ---------- | ------------------------------------ | ------------ |
| 泛型与集合 | 类型安全、避免强转                   | List&lt;T&gt;、Map&lt;K,V&gt; |
| 类型擦除   | 编译期擦除、运行时无泛型             | 与《泛型反射与注解》笔记呼应 |
| 通配符     | ? extends T 只读、? super T 只写、PECS | 方法签名、API 设计 |
| 原始类型   | 丧失类型安全、ClassCastException     | 避免裸 List/Map |

------

**学习要点**：能说出泛型在集合中的作用与类型擦除；能区分 ? extends T 与 ? super T 的读写性及 PECS。

------

## 🎯 面试常见追问

| 面试官提问             | 回答思路 |
| ---------------------- | -------- |
| ? extends T 和 ? super T 区别？ | extends 可读为 T，不可写；super 可写 T，读只能得到 Object；PECS：生产者 extends，消费者 super。 |
| 为什么不能 new 泛型数组？ | 擦除后无法保证类型安全，易产生运行时错误；用 List&lt;List&lt;T&gt;&gt; 或 Array.newInstance 替代。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| 用原始类型（raw type）如 List 不写泛型 | 失去类型检查，取元素要强转，易 ClassCastException | 始终写泛型 List&lt;String&gt; 等；遗留 API 若返回原始类型，尽快转成带泛型的变量。 |
| 泛型方法里「读」用 super、「写」用 extends | 容易记反 | PECS：Producer Extends, Consumer Super——往外取用 extends，往里放用 super。 |
| 想 new 泛型数组 T[] | 擦除后无法创建 T[]，只能得到 Object[] | 用 List&lt;T&gt; 或 (T[]) Array.newInstance(Class, n)，并注意类型安全。 |

------

# 🧩 第七章：集合框架源码分析（核心类）

> **目标：**
>  通过源码分析，深入理解 Java 集合框架中核心类（如 `ArrayList`、`HashMap`、`ConcurrentHashMap` 和 `LinkedHashMap`）的实现原理，掌握其底层数据结构、算法优化和性能特性。

> **本章在整体中解决什么问题**：第二、三章讲了「怎么用」，本章讲**底层怎么实现**——ArrayList 扩容、HashMap put/树化/扩容、ConcurrentHashMap JDK8 锁桶、LinkedHashMap LRU。校招常要求画图或口述流程；学完本章后**第八章**汇总实战与面试题，可结合本章答「底层结构」「为什么线程安全」等追问。

------

## 🧠 **1. ArrayList 源码结构分析**

### 📘 **底层数据结构：elementData**

- `ArrayList` 是基于 **动态数组** 实现的集合，底层存储数据的数组称为 `elementData`。
- 初始化时，`ArrayList` 会创建一个长度为 10 的空数组，当元素超过当前容量时，`ArrayList` 会扩容。

#### 🧑‍💻 **构造函数与扩容机制**

```java
// 默认构造函数，初始化容量为 10
public ArrayList() {
    this(DEFAULT_CAPACITY);
}

// 指定容量构造函数
public ArrayList(int initialCapacity) {
    if (initialCapacity > 0) {
        this.elementData = new Object[initialCapacity];
    } else if (initialCapacity == 0) {
        this.elementData = EMPTY_ELEMENTDATA;
    } else {
        throw new IllegalArgumentException("Illegal Capacity: " + initialCapacity);
    }
}
```

- 默认容量为 10，`ArrayList` 会根据元素的个数动态扩容。扩容是通过 `ensureCapacity` 方法进行的。

#### 🧑‍💻 **扩容逻辑**

- 当 `ArrayList` 的容量达到 `elementData.length` 时，`ArrayList` 会进行扩容，通常扩容为原容量的 1.5 倍。

```java
private void grow(int minCapacity) {
    // 增长因子为 1.5 倍
    int oldCapacity = elementData.length;
    int newCapacity = oldCapacity + (oldCapacity >> 1);
    if (newCapacity - minCapacity < 0)
        newCapacity = minCapacity;
    elementData = Arrays.copyOf(elementData, newCapacity);
}
```

#### ⚡ **扩容的时间复杂度**

- 扩容操作是 **O(n)**，因为需要创建新的数组并将旧数组的元素复制到新数组中。

------

## 🧳 **2. HashMap 源码分析**

### 📘 **底层数据结构：数组 + 链表 + 红黑树**

- `HashMap` 底层是一个数组，数组的每个位置是一个 **链表** 或 **红黑树**，用于处理哈希冲突。
- `HashMap` 使用 **哈希表** 结构存储键值对，每个键值对通过计算哈希值定位到桶数组的位置。

#### 🧑‍💻 **put() 方法**

- `put()` 方法用于插入键值对，首先计算键的哈希值，然后查找对应的桶位置。如果该位置没有元素，则直接插入；如果已经有元素，则需要通过 **链表** 或 **红黑树** 处理冲突。

```java
public V put(K key, V value) {
    if (key == null)
        return putForNullKey(value);
    int hash = hash(key);
    int i = indexFor(hash, table.length);
    // 插入新元素或更新已有元素
    for (Entry<K,V> e = table[i]; e != null; e = e.next) {
        if (e.hash == hash && (e.key == key || key.equals(e.key))) {
            V oldValue = e.value;
            e.value = value;
            return oldValue;
        }
    }
    // 插入新的元素
    modCount++;
    addEntry(hash, key, value, i);
    return null;
}
```

#### ⚡ **resize() 和 treeify() 流程**

- 当 **链表** 长度超过一定阈值（默认为 8）时，`HashMap` 会将链表转换为 **红黑树**，以提高查询性能。

```java
private void treeifyBin(Node<K,V>[] tab, int hash) {
    // 将链表转换为红黑树
    // 树化操作
}
```

- `resize()` 方法用于扩容，当 `HashMap` 容量达到负载因子的阈值时，会进行扩容操作。

```java
void resize() {
    // 扩容操作
    int newCapacity = oldCapacity << 1;
    threshold = (int)(newCapacity * loadFactor);
    transfer(newTable);
}
```

#### 📍 HashMap put 流程简图（面试可画）

```
put(key, value)
    │
    ├─ 1. 若 key==null → 放 table[0]，否则 hash(key) 得到 hash 值
    ├─ 2. 下标 i = (n-1) & hash，定位到桶 table[i]
    ├─ 3. 若桶为空 → 直接 newNode 放进去
    ├─ 4. 若桶不为空 → 比较 key（先 hash 再 equals）
    │      ├─ 相等 → 覆盖 value，返回 oldValue
    │      └─ 不相等 → 若为树节点则走红黑树 putTreeVal；否则走链表
    │             ├─ 链表遍历到尾插入；若链表长度≥8 且 table.length≥64 → treeifyBin 转红黑树
    │             └─ 插入后若 size > threshold → resize() 扩容为 2 倍
    └─ 5. 返回 null（新插入）或 oldValue（覆盖）
```

> **面试怎么说**：HashMap put 先算 hash 再 (n-1)&hash 定位桶；桶空直接放，桶不空比 key，相等覆盖，不等则链表尾插或红黑树插入；链表长度≥8 且数组≥64 会树化；超过负载因子（0.75）* 容量会扩容 2 倍并 rehash。

------

## 🧳 **3. ConcurrentHashMap 源码分析**

### 📘 **高并发优化：分段锁与 CAS**

- `ConcurrentHashMap` 采用了 **分段锁（Segment）** 技术，将数据分为多个段，每个段维护独立的锁，从而提高并发性能。
- `ConcurrentHashMap` 的写操作是通过 **CAS（Compare and Swap）** 实现的，保证操作的 **原子性**，避免锁的竞争。

#### 🧑‍💻 **Segment → Node → CAS → synchronized**

```java
final Segment<K,V> segment = segments[hash & (segments.length - 1)];
synchronized (segment) {
    // CAS 操作
    segment.put(key, value);
}
```

- `ConcurrentHashMap` 使用 **分段锁** 来对不同段的数据加锁，因此可以并发修改不同段的元素，从而提升性能。

#### ⚡ **并发优化的核心**

- 通过分段锁和 CAS，`ConcurrentHashMap` 能够在高并发场景下提供良好的性能，避免了整个 `Map` 锁定的瓶颈。

#### 📍 ConcurrentHashMap JDK8 简图与面试话术

**JDK8 已取消 Segment**，结构为 **数组 + 链表 + 红黑树**，与 HashMap 类似，区别在并发控制：

```
put(key, value)
    │
    ├─ 1. 计算 hash，定位到桶（Node 数组下标）
    ├─ 2. 若桶为空 → CAS 插入新节点（无锁）
    ├─ 3. 若桶不为空 → synchronized 锁住桶头节点
    │      ├─ 再检查一次当前桶头是否被别的线程改过
    │      ├─ 链表：遍历找 key，有则覆盖，无则尾插；≥8 且 数组≥64 则树化
    │      └─ 红黑树：按树节点 putTreeVal
    └─ 4. 若 binCount 达到阈值 → 可能触发扩容（多线程协助 transfer）
```

> **面试怎么说**：JDK8 的 ConcurrentHashMap 没有 Segment 了，结构是数组+链表+红黑树。空桶用 CAS 插入，有冲突时只锁**当前桶的头节点**（synchronized），所以锁粒度小、并发度高。读操作一般无锁（volatile 读），扩容时多线程可以协助迁移桶。

------

## 🧳 **4. LinkedHashMap LRU 实现机制**

### 📘 **底层数据结构：双向链表**

- `LinkedHashMap` 在 `HashMap` 的基础上，增加了 **双向链表**，用于保持元素的插入顺序或访问顺序。

#### 🧑‍💻 **removeEldestEntry() 方法**

- `LinkedHashMap` 通过重写 `removeEldestEntry()` 方法实现 LRU 缓存的功能。该方法在每次插入新元素后被调用，决定是否移除最老的元素。

```java
protected boolean removeEldestEntry(Map.Entry<K,V> eldest) {
    return size() > MAX_ENTRIES;
}
```

- `removeEldestEntry()` 方法可以根据 `Map` 的大小或其他条件，自动删除最旧的元素，常用于实现 **LRU（Least Recently Used）** 缓存。

#### 🧑‍💻 **LRU 缓存示例**

```java
Map<String, Integer> lruCache = new LinkedHashMap<>(16, 0.75f, true);
lruCache.put("A", 1);
lruCache.put("B", 2);
lruCache.put("C", 3);
lruCache.get("A");  // 访问 "A"
lruCache.put("D", 4); // 自动移除 "B"（最少使用）
```

- 设置 `accessOrder` 为 `true`，`LinkedHashMap` 会按 **访问顺序** 排序元素，常用于实现 LRU 缓存。

------

# ✅ 本章小结

| 知识点     | 面试关键词                                   | 实际应用     |
| ---------- | -------------------------------------------- | ------------ |
| ArrayList  | elementData、默认 10、扩容 1.5 倍、grow()    | 随机访问、动态扩容 |
| HashMap    | 数组+链表+红黑树、put/resize/treeify、负载因子 0.75 | 缓存、字典   |
| ConcurrentHashMap | JDK8 锁桶头+CAS、无 Segment                  | 高并发 Map   |
| LinkedHashMap | 双向链表、accessOrder、removeEldestEntry     | LRU 缓存     |

------

**学习要点**：能简述 ArrayList 扩容、HashMap put/树化/扩容流程；能说出 JDK8 ConcurrentHashMap 与 Hashtable 的区别；能写出 LinkedHashMap 实现 LRU 的思路。

------

## 🎯 面试常见追问

| 面试官提问               | 回答思路 |
| ------------------------ | -------- |
| HashMap 为什么用红黑树？ | 链表过长时查找 O(n)，红黑树 O(log n)；树化阈值 8，退化阈值 6，避免频繁转换。 |
| JDK8 ConcurrentHashMap 还有 Segment 吗？ | JDK8 已去掉 Segment，改为数组+链表+红黑树，锁单个桶（Node），CAS+ synchronized。 |

------

### ⚠️ 常见坑与注意点

| 现象 / 易错点 | 原因 | 正确做法 / 怎么记 |
|---------------|------|-------------------|
| 面试说 HashMap 还是 Segment 分段锁 | JDK8 已取消 Segment | 答 JDK8：数组+链表+红黑树，CAS 插空桶，synchronized 锁桶头；JDK7 才是 Segment。 |
| 混淆树化条件 | 不是链表≥8 就树化 | 链表长度≥8 **且** 数组长度≥64 才树化；否则只扩容，减少树化开销。 |
| 遍历时修改 modCount 导致 CME | 源码里增删改会改 modCount，迭代器检查 | 不要在用 iterator/for-each 时直接 add/remove；用 Iterator.remove() 或 removeIf。 |
| ArrayList 无参构造后以为容量是 0 | 第一次 add 才给 10，之前 elementData 是空数组 | 默认容量 10 是第一次 add 时扩容出来的；已知大小时用带参构造减少扩容次数。 |

------

# 🧩 第八章：项目实战与面试要点

> **目标：** 串联集合在项目中的典型用法与校招高频面试题，便于实战与冲刺背诵。

> **本章在整体中解决什么问题**：在前面各章「概念+用法+源码」基础上，本章汇总**实战场景**（LRU、本地缓存、生产者消费者）与**面试高频题**。若面试官追问底层细节，可回到**第七章**；追问 List/Set/Map 选型见**第二、三章**；追问 fail-fast、泛型见**第四、六章**。

------

## 🧠 1. 实战应用场景

### 📘 LRU 缓存（LinkedHashMap）

- 使用 `LinkedHashMap` 构造器 `accessOrder=true`，并重写 `removeEldestEntry(Map.Entry)`，当 `size() > capacity` 时返回 true，即可在 put 时自动移除最久未访问的 entry。
- 或使用 Guava `Cache`、Caffeine 等现成缓存库。

### 📘 本地缓存（ConcurrentHashMap）

- 高并发下用 `ConcurrentHashMap` 做本地缓存（key 业务键，value 为数据或带过期时间的包装）；结合定时任务或惰性清理过期项。

### 📘 生产者-消费者（BlockingQueue）

- 使用 `ArrayBlockingQueue` / `LinkedBlockingQueue`，生产者 `put()`，消费者 `take()`，实现解耦与背压。

------

## 🧳 2. 高频面试题汇总（要点）

| 题目                     | 要点简述 |
| ------------------------ | -------- |
| ArrayList 与 LinkedList 区别 | 见第二章小结；ArrayList 数组随机访问快，LinkedList 头尾增删快。 |
| HashMap 底层与线程安全   | 数组+链表+红黑树；非线程安全，多线程用 ConcurrentHashMap。 |
| HashMap 与 Hashtable     | Hashtable 全表 synchronized，已过时；HashMap 非线程安全，ConcurrentHashMap 锁桶。 |
| ConcurrentHashMap 为何线程安全 | JDK8：CAS 插空桶、synchronized 锁桶头，读无锁。 |
| Set 如何去重              | 基于 hashCode 与 equals；HashSet 用 HashMap 的 key 存元素。 |
| fail-fast 与 fail-safe   | fail-fast：迭代时发现 modCount 变化抛 CME；fail-safe：如 CopyOnWriteArrayList 迭代副本，不抛 CME。 |
| 红黑树在 HashMap 中的作用 | 链表长度>8 时树化，查找由 O(n) 降为 O(log n)。 |
| 如何实现 LRU 缓存        | LinkedHashMap(accessOrder=true) + removeEldestEntry；或手写双向链表+HashMap。 |
| 负载因子为什么 0.75       | 权衡冲突率与空间；过小扩容频繁，过大冲突多，0.75 经验值。 |

------

# ✅ 本章小结

| 知识点     | 面试关键词           | 实际应用     |
| ---------- | -------------------- | ------------ |
| LRU        | LinkedHashMap、removeEldestEntry | 本地缓存     |
| 本地缓存   | ConcurrentHashMap、过期策略       | 高并发缓存   |
| 生产者消费者 | BlockingQueue、put/take           | 异步解耦     |

------

**学习要点**：能口述 ArrayList/LinkedList、HashMap/ConcurrentHashMap、fail-fast 与 LRU 实现等题的答案；能结合项目举例（如用 Map 做缓存、用 Queue 做任务队列）。

------

## 🎯 面试常见追问

| 面试官提问         | 回答思路 |
| ------------------ | -------- |
| 项目中用过哪些集合？ | 可答：ArrayList 存列表、HashMap 做缓存、ConcurrentHashMap 做并发缓存、BlockingQueue 做消息队列等。 |
| 如何设计一个 LRU？ | LinkedHashMap( capacity, 0.75f, true ) + removeEldestEntry 当 size>capacity 返回 true；或双向链表+HashMap 手写。 |

------

### 若追问可延伸至的章节

| 追问方向 | 延伸章节 |
|----------|----------|
| HashMap/ConcurrentHashMap 底层、put 流程、为什么线程安全 | **第七章** 源码分析 |
| ArrayList 与 LinkedList 区别、Set 如何去重、List/Set/Map 选型 | **第二、三章** Collection 与 Map 体系 |
| fail-fast、遍历时安全删除、CME 原因 | **第四章** 迭代器与遍历机制 |
| 泛型、PECS、类型擦除 | **第六章** 泛型与集合类型安全 |
| Collections/Arrays 工具类、不可变集合、Arrays.asList 坑 | **第五章** 工具类与算法支持 |

------

# 📎 附录：面试题速查

1. **ArrayList 和 LinkedList 区别？** 底层数组 vs 双向链表；随机访问 O(1) vs O(n)；尾部增删 ArrayList 均摊 O(1)，LinkedList 头尾 O(1)。
2. **HashMap 底层结构？** 数组+链表+红黑树；hash 定位桶，冲突链表，长度≥8 树化；负载因子 0.75，扩容 2 倍。
3. **HashMap 和 Hashtable 区别？** Hashtable 全表 synchronized、不允许 null；HashMap 非线程安全、允许 null key/value；高并发用 ConcurrentHashMap。
4. **ConcurrentHashMap 怎么保证线程安全？** JDK8：CAS+锁桶头，读无锁；JDK7：Segment 分段锁。
5. **Set 如何去重？** 基于元素的 hashCode 与 equals；HashSet 底层是 HashMap 的 key。
6. **fail-fast 和 fail-safe？** fail-fast：迭代过程检测 modCount，结构变化抛 CME；fail-safe：迭代副本（如 CopyOnWriteArrayList），不抛 CME。
7. **为什么 HashMap 负载因子 0.75？** 平衡冲突与空间；0.75 是经验值，扩容阈值 = capacity * 0.75。
8. **如何用集合实现 LRU？** LinkedHashMap(accessOrder=true) + removeEldestEntry；或双向链表+HashMap。
9. **Arrays.asList() 注意什么？** 返回固定大小视图，不能 add/remove；要可变用 new ArrayList<>(Arrays.asList(...))。

