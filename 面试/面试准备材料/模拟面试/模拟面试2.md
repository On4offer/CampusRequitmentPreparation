# 面试题评判与标准答案

## 第一阶段：实习经历深挖（智识神工）

### 题目1：Excel批量导入优化

**原题：** 如果导入的 Excel 有 1 万行数据，且每一行都需要去数据库查一下是否存在（精确匹配），直接这么写会有什么问题？你是怎么优化的？

**你的回答：** 这样会导致查询很慢，会有非常多数据库连接，用户等待时间长。我的解决方案是先数据库模糊匹配查询可能匹配的商品，然后再内存精确比对。

**评判：**
- 优点：指出了数据库连接和性能问题
- 不足：
  1. 未说明具体问题（N+1查询、连接池耗尽、事务时间过长）
  2. 优化方案不够具体（未提及批量查询、缓存、线程池等）
  3. 未说明内存比对的具体实现

**标准答案：**

**问题分析：**
1. N+1查询：1万次数据库查询，性能差
2. 数据库连接池耗尽：并发连接过多
3. 事务时间过长：可能导致锁等待
4. 响应时间过长：用户体验差

**优化方案：**
```java
// 方案1：批量查询 + 内存匹配（推荐）
public void importExcel(MultipartFile file) {
    List<Product> excelProducts = parseExcel(file);
    
    // 1. 批量提取商品名称（去重）
    Set<String> productNames = excelProducts.stream()
        .map(Product::getName)
        .collect(Collectors.toSet());
    
    // 2. 一次性批量查询数据库（IN查询）
    List<Product> dbProducts = productMapper.selectBatchByNames(productNames);
    
    // 3. 构建内存Map，key为"名称+规格"
    Map<String, Product> dbProductMap = dbProducts.stream()
        .collect(Collectors.toMap(
            p -> p.getName() + "|" + p.getSpecification(),
            p -> p
        ));
    
    // 4. 内存中精确匹配
    for (Product excelProduct : excelProducts) {
        String key = excelProduct.getName() + "|" + excelProduct.getSpecification();
        Product matchedProduct = dbProductMap.get(key);
        if (matchedProduct != null) {
            // 匹配成功，处理业务逻辑
        }
    }
}

// 方案2：使用线程池并发处理（适合数据量特别大的场景）
@Autowired
private ThreadPoolExecutor excelImportExecutor;

public void importExcel(MultipartFile file) {
    List<Product> excelProducts = parseExcel(file);
    
    // 分批处理，每批1000条
    int batchSize = 1000;
    CountDownLatch latch = new CountDownLatch(
        (excelProducts.size() + batchSize - 1) / batchSize
    );
    
    for (int i = 0; i < excelProducts.size(); i += batchSize) {
        List<Product> batch = excelProducts.subList(
            i, Math.min(i + batchSize, excelProducts.size())
        );
        
        excelImportExecutor.submit(() -> {
            try {
                processBatch(batch); // 批量查询+匹配
            } finally {
                latch.countDown();
            }
        });
    }
    
    latch.await(30, TimeUnit.SECONDS);
}
```

**性能对比：**
- 优化前：1万次查询，耗时约100秒
- 优化后：1次批量查询 + 内存匹配，耗时约1秒

---

### 题目2：POI解析Excel的两种模式

**原题：** POI 解析 Excel 有两种模式（UserModel 和 SAX），如果文件很大（比如 50MB），你用的哪种？为什么？如果不用 SAX，内存溢出了怎么排查？

**你的回答：** 不懂。

**评判：** 需要补充POI解析模式的知识。

**标准答案：**

**两种模式对比：**

| 模式                     | 特点                              | 内存占用              | 适用场景        |
| ------------------------ | --------------------------------- | --------------------- | --------------- |
| **UserModel（DOM模式）** | 将整个Excel加载到内存，构建对象树 | 高（文件大小的3-5倍） | 小文件（<10MB） |
| **SAX模式（事件驱动）**  | 逐行读取，不加载整个文件          | 低（固定内存）        | 大文件（>10MB） |

**代码实现：**

```java
// UserModel模式（不推荐大文件）
Workbook workbook = WorkbookFactory.create(inputStream);
Sheet sheet = workbook.getSheetAt(0);
for (Row row : sheet) {
    // 处理每一行
}

// SAX模式（推荐大文件）
public void parseExcelBySAX(InputStream inputStream) {
    OPCPackage pkg = OPCPackage.open(inputStream);
    XSSFReader reader = new XSSFReader(pkg);
    SharedStringsTable sst = reader.getSharedStringsTable();
    XMLReader parser = XMLReaderFactory.createXMLReader();
    
    parser.setContentHandler(new SheetHandler(sst)); // 自定义处理器
    InputStream sheetInputStream = reader.getSheet("rId1");
    InputSource sheetSource = new InputSource(sheetInputStream);
    parser.parse(sheetSource);
}

// 自定义SheetHandler
class SheetHandler extends DefaultHandler {
    @Override
    public void startElement(String uri, String localName, 
                            String qName, Attributes attributes) {
        if ("row".equals(qName)) {
            // 处理行开始
        } else if ("c".equals(qName)) {
            // 处理单元格
        }
    }
}
```

**内存溢出排查：**
1. 查看堆内存：`jmap -heap <pid>`
2. 导出堆转储：`jmap -dump:format=b,file=heap.hprof <pid>`
3. 使用MAT分析：查找占用内存大的对象
4. 检查代码：确认是否使用UserModel解析大文件
5. JVM参数：`-Xmx2g -XX:+HeapDumpOnOutOfMemoryError`

---

### 题目3：登录拦截器实现

**原题：** 你是怎么实现拦截的？是用了 Spring MVC 的 Interceptor 还是 Filter？两者的区别是什么？为什么选这个？

**你的回答：** 拦截器，粒度更细，可以精确到方法层面，更灵活。filter一般用作全局日志相关的，不适合这里。

**评判：**
- 优点：正确选择了Interceptor
- 不足：未说明Filter和Interceptor的区别，理由不够充分

**标准答案：**

**Filter vs Interceptor：**

| 特性         | Filter（过滤器）                    | Interceptor（拦截器）                        |
| ------------ | ----------------------------------- | -------------------------------------------- |
| **所属框架** | Servlet规范                         | Spring MVC框架                               |
| **执行时机** | 在DispatcherServlet之前             | 在DispatcherServlet之后，Controller之前/之后 |
| **作用范围** | 所有请求（包括静态资源）            | 只拦截Controller方法                         |
| **获取信息** | 只能获取HttpServletRequest/Response | 可以获取HandlerMethod、ModelAndView等        |
| **异常处理** | 无法直接处理Controller异常          | 可以处理Controller异常                       |
| **依赖注入** | 需要特殊配置                        | 可以直接使用@Autowired                       |

**代码实现：**

```java
// Interceptor实现
@Component
public class LoginInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request, 
                           HttpServletResponse response, 
                           Object handler) throws Exception {
        // 1. 判断是否需要拦截（静态资源不拦截）
        if (!(handler instanceof HandlerMethod)) {
            return true;
        }
        
        // 2. 检查登录状态
        String token = request.getHeader("authorization");
        if (StrUtil.isBlank(token)) {
            response.setStatus(401);
            return false;
        }
        
        // 3. 验证token
        UserDTO user = userService.getUserByToken(token);
        if (user == null) {
            response.setStatus(401);
            return false;
        }
        
        // 4. 保存用户信息到ThreadLocal
        UserHolder.saveUser(user);
        return true;
    }
    
    @Override
    public void afterCompletion(HttpServletRequest request, 
                               HttpServletResponse response, 
                               Object handler, Exception ex) {
        // 清理ThreadLocal，防止内存泄漏
        UserHolder.removeUser();
    }
}

// 配置拦截器
@Configuration
public class MvcConfig implements WebMvcConfigurer {
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new LoginInterceptor())
                .excludePathPatterns("/user/login", "/user/code")
                .order(1);
    }
}
```

**为什么选择Interceptor：**
1. 粒度更细：可精确到Controller方法
2. 可获取HandlerMethod：可判断方法上的注解
3. 可处理异常：afterCompletion可获取异常信息
4. 依赖注入方便：可直接使用Spring Bean

---

### 题目4：密码修改接口安全

**原题：** 如果用户修改密码的接口（/user/updatePwd）没有做特殊保护，用户能不能绕过登录拦截器直接发请求修改密码？你在代码里是怎么防止这个漏洞的？

**你的回答：** 可以的，修改密码的接口是不能被拦截的。

**评判：**
- 问题：回答有误。修改密码接口应该被拦截，需要登录才能修改
- 正确做法：修改密码接口需要登录，但需要额外验证（旧密码、验证码等）

**标准答案：**

**安全漏洞分析：**
1. 如果修改密码接口不被拦截：未登录用户可直接修改他人密码
2. 如果只依赖拦截器：已登录用户可能被CSRF攻击

**防护措施：**

```java
// 1. 修改密码接口必须登录（被拦截器拦截）
@PostMapping("/user/updatePwd")
public Result updatePassword(@RequestBody UpdatePasswordDTO dto) {
    Long userId = UserHolder.getUser().getId();
    
    // 2. 验证旧密码（防止token泄露后被恶意修改）
    User user = userService.getById(userId);
    if (!passwordEncoder.matches(dto.getOldPassword(), user.getPassword())) {
        return Result.fail("旧密码错误");
    }
    
    // 3. 验证新密码强度
    if (!isPasswordStrong(dto.getNewPassword())) {
        return Result.fail("密码强度不足");
    }
    
    // 4. 防止密码与旧密码相同
    if (passwordEncoder.matches(dto.getNewPassword(), user.getPassword())) {
        return Result.fail("新密码不能与旧密码相同");
    }
    
    // 5. 更新密码
    userService.updatePassword(userId, dto.getNewPassword());
    
    // 6. 清除其他设备的登录状态（可选）
    tokenService.invalidateOtherTokens(userId);
    
    return Result.ok();
}

// 拦截器配置：修改密码接口必须登录
registry.addInterceptor(new LoginInterceptor())
        .addPathPatterns("/**")
        .excludePathPatterns("/user/login", "/user/code", "/user/register");
        // 注意：/user/updatePwd 不在排除列表中，必须登录
```

**多层防护：**
1. 登录拦截器：确保用户已登录
2. 旧密码验证：防止token泄露
3. 密码强度校验：防止弱密码
4. 频率限制：防止暴力破解
5. 操作日志：记录密码修改操作

---

### 题目5：异步日志存储

**原题：** 你是用了@Async注解，还是自己开了线程池，或者是用了 MQ？数据一致性：如果主业务流程成功了，但是异步记录日志失败了（比如日志服务挂了），数据会丢吗？对于审计日志这种要求可靠性的场景，你怎么保证 "至少消费一次"？

**你的回答：** 使用到的线程池，内置线程池completaablefuture.runAsync，是一个commonpool。会丢失，使用消息队列，生产者开启持久化，消费者开启确认机制，然后加上重试。同时考虑降级写本地文件。

**评判：**
- 优点：提到了MQ、持久化、确认机制、重试、降级
- 不足：未说明CompletableFuture.runAsync的问题（使用ForkJoinPool，不适合IO密集型）

**标准答案：**

**异步实现方案对比：**

| 方案               | 优点             | 缺点                     | 适用场景       |
| ------------------ | ---------------- | ------------------------ | -------------- |
| **@Async**         | 简单，Spring支持 | 默认线程池不适合IO密集型 | 轻量级异步任务 |
| **自定义线程池**   | 可控，可配置     | 需要手动管理             | IO密集型任务   |
| **MQ（消息队列）** | 可靠性高，解耦   | 复杂度高，需要MQ中间件   | 高可靠性场景   |

**代码实现：**

```java
// 方案1：自定义线程池（适合一般场景）
@Configuration
public class ThreadPoolConfig {
    @Bean("logExecutor")
    public ThreadPoolExecutor logExecutor() {
        return new ThreadPoolExecutor(
            4, 8, 60L, TimeUnit.SECONDS,
            new LinkedBlockingQueue<>(1000),
            new ThreadFactoryBuilder().setNameFormat("log-%d").build(),
            new CallerRunsPolicy()
        );
    }
}

@Service
public class AuditLogService {
    @Autowired
    @Qualifier("logExecutor")
    private ThreadPoolExecutor logExecutor;
    
    public void recordLog(OperationLog log) {
        // 提交到线程池，失败不影响主流程
        logExecutor.submit(() -> {
            try {
                auditLogMapper.insert(log);
            } catch (Exception e) {
                log.error("记录日志失败", e);
                // 降级：写入本地文件
                writeToLocalFile(log);
            }
        });
    }
}

// 方案2：MQ方案（适合高可靠性场景）
@Service
public class AuditLogService {
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    public void recordLog(OperationLog log) {
        // 1. 发送到MQ，开启持久化
        rabbitTemplate.convertAndSend(
            "audit.log.exchange",
            "audit.log.routingKey",
            log,
            message -> {
                // 设置消息持久化
                message.getMessageProperties().setDeliveryMode(
                    MessageDeliveryMode.PERSISTENT
                );
                return message;
            }
        );
    }
}

// 消费者：保证至少消费一次
@RabbitListener(queues = "audit.log.queue")
public void consumeLog(OperationLog log, Channel channel, 
                      @Header(AmqpHeaders.DELIVERY_TAG) long tag) {
    try {
        // 1. 处理日志
        auditLogMapper.insert(log);
        
        // 2. 手动ACK（确认消费成功）
        channel.basicAck(tag, false);
        
    } catch (Exception e) {
        // 3. 处理失败，拒绝消息并重新入队
        channel.basicNack(tag, false, true); // requeue=true，重新入队
        
        // 4. 记录重试次数，超过阈值写入死信队列
        if (getRetryCount(log) > 3) {
            sendToDeadLetterQueue(log);
        }
    }
}
```

**保证至少消费一次的措施：**
1. 消息持久化：`deliveryMode = PERSISTENT`
2. 队列持久化：`durable = true`
3. 手动ACK：处理成功后才确认
4. 失败重试：NACK后重新入队
5. 死信队列：超过重试次数后进入死信队列
6. 降级方案：MQ不可用时写入本地文件，MQ恢复后补偿

---

### 题目6：敏感信息脱敏

**原题：** 你是怎么实现敏感信息脱敏的？是在实体类 toString 的时候处理，还是在入 DB 前处理？有没有考虑过 AOP（面向切面）来统一处理日志记录？

**你的回答：** url和表单是跳过密码，token等，JSON体是替换为*占位，同时限制长度，处理时间是在实体类就处理，取到数据就处理。考虑过AOP方案，也是无侵入，且更灵活。使用拦截器是因为项目已有拦截器可以直接继续用。

**评判：**
- 优点：提到了多种脱敏方式，考虑了AOP
- 不足：未说明具体实现细节，未说明AOP vs 拦截器的选择理由

**标准答案：**

**脱敏实现方案：**

```java
// 方案1：实体类toString脱敏（不推荐，toString主要用于调试）
@Override
public String toString() {
    return "User{" +
        "id=" + id +
        ", username='" + username + '\'' +
        ", password='******'" +  // 脱敏
        ", phone='" + maskPhone(phone) + '\'' +
        '}';
}

// 方案2：拦截器统一脱敏（推荐）
@Component
public class SensitiveDataInterceptor implements HandlerInterceptor {
    
    @Override
    public void afterCompletion(HttpServletRequest request, 
                              HttpServletResponse response,
                              Object handler, Exception ex) {
        // 1. 获取请求参数
        Map<String, Object> params = getRequestParams(request);
        
        // 2. 脱敏处理
        desensitizeParams(params);
        
        // 3. 记录日志
        log.info("请求参数：{}", JSON.toJSONString(params));
    }
    
    private void desensitizeParams(Map<String, Object> params) {
        // 跳过敏感字段
        params.remove("password");
        params.remove("token");
        params.remove("authorization");
        
        // 手机号脱敏：138****5678
        if (params.containsKey("phone")) {
            String phone = (String) params.get("phone");
            params.put("phone", maskPhone(phone));
        }
        
        // 身份证脱敏：110101********1234
        if (params.containsKey("idCard")) {
            String idCard = (String) params.get("idCard");
            params.put("idCard", maskIdCard(idCard));
        }
    }
}

// 方案3：AOP统一处理（最灵活）
@Aspect
@Component
public class SensitiveDataAspect {
    
    @Around("@annotation(com.hmdp.annotation.OperationLog)")
    public Object around(ProceedingJoinPoint joinPoint) throws Throwable {
        // 1. 获取方法参数
        Object[] args = joinPoint.getArgs();
        
        // 2. 脱敏处理
        for (Object arg : args) {
            if (arg instanceof HttpServletRequest) {
                desensitizeRequest((HttpServletRequest) arg);
            } else {
                desensitizeObject(arg);
            }
        }
        
        // 3. 执行方法
        Object result = joinPoint.proceed();
        
        // 4. 结果脱敏
        return desensitizeResult(result);
    }
    
    private void desensitizeObject(Object obj) {
        // 使用反射遍历字段，对敏感字段脱敏
        Field[] fields = obj.getClass().getDeclaredFields();
        for (Field field : fields) {
            if (field.isAnnotationPresent(Sensitive.class)) {
                field.setAccessible(true);
                try {
                    Object value = field.get(obj);
                    field.set(obj, desensitize(value, field.getAnnotation(Sensitive.class)));
                } catch (IllegalAccessException e) {
                    // 忽略
                }
            }
        }
    }
}

// 自定义注解
@Target(ElementType.FIELD)
@Retention(RetentionPolicy.RUNTIME)
public @interface Sensitive {
    SensitiveType type() default SensitiveType.PASSWORD;
}

// 使用示例
public class User {
    @Sensitive(type = SensitiveType.PASSWORD)
    private String password;
    
    @Sensitive(type = SensitiveType.PHONE)
    private String phone;
}
```

**AOP vs 拦截器选择：**
- AOP：更灵活，可精确到方法级别，可处理返回值
- 拦截器：简单，适合统一处理HTTP请求

**推荐方案：** 拦截器处理请求参数，AOP处理业务方法参数和返回值

---

## 第二阶段：个人项目深挖（小众点评）

### 题目7：缓存击穿解决方案

**原题：** 标准的互斥锁（Mutex Lock）和逻辑过期（Logical Expire）解决击穿有什么区别？为什么在你的场景下选择了逻辑过期？它的优缺点是什么？

**你的回答：** 互斥锁是缓存过期之后就去重建缓存，只有一个线程能拿到锁去异步查询重建，而逻辑过期是缓存数据部显式指定过期时间，而是数据内存储一个逻辑过期时间，查询过期之后一个线程获得锁去异步重建缓存，并返回旧值。采用逻辑过期是因为对实时一致性要求不高，所以返回旧值不阻塞提升用户体验。

**评判：**
- 优点：基本理解了两种方案的区别
- 不足：未说明互斥锁会阻塞用户请求，未说明逻辑过期的缺点

**标准答案：**

**两种方案对比：**

| 特性             | 互斥锁（Mutex Lock）             | 逻辑过期（Logical Expire）             |
| ---------------- | -------------------------------- | -------------------------------------- |
| **缓存过期时机** | Redis TTL过期，缓存被删除        | 缓存永不过期，逻辑过期时间存储在数据中 |
| **用户请求处理** | 缓存过期后，所有请求阻塞等待重建 | 返回旧数据，后台异步重建               |
| **数据一致性**   | 强一致性（返回最新数据）         | 最终一致性（可能返回过期数据）         |
| **用户体验**     | 可能阻塞，响应慢                 | 不阻塞，响应快                         |
| **实现复杂度**   | 简单                             | 较复杂                                 |

**代码实现：**

```java
// 方案1：互斥锁
public Shop queryWithMutex(Long id) {
    String key = CACHE_SHOP_KEY + id;
    // 1. 查询缓存
    String shopJson = stringRedisTemplate.opsForValue().get(key);
    if (StrUtil.isNotBlank(shopJson)) {
        return JSONUtil.toBean(shopJson, Shop.class);
    }
    
    // 2. 缓存未命中，尝试获取锁
    String lockKey = LOCK_SHOP_KEY + id;
    boolean isLock = tryLock(lockKey);
    
    if (!isLock) {
        // 3. 获取锁失败，休眠后重试
        Thread.sleep(50);
        return queryWithMutex(id); // 递归重试
    }
    
    try {
        // 4. 获取锁成功，双重检查
        shopJson = stringRedisTemplate.opsForValue().get(key);
        if (StrUtil.isNotBlank(shopJson)) {
            return JSONUtil.toBean(shopJson, Shop.class);
        }
        
        // 5. 查询数据库
        Shop shop = shopMapper.selectById(id);
        if (shop == null) {
            // 缓存空值，防止缓存穿透
            stringRedisTemplate.opsForValue().set(key, "", 2, TimeUnit.MINUTES);
            return null;
        }
        
        // 6. 写入缓存
        stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop), 30, TimeUnit.MINUTES);
        return shop;
    } finally {
        unlock(lockKey);
    }
}

// 方案2：逻辑过期
public Shop queryWithLogicalExpire(Long id) {
    String key = CACHE_SHOP_KEY + id;
    // 1. 查询缓存
    String json = stringRedisTemplate.opsForValue().get(key);
    if (StrUtil.isBlank(json)) {
        // 缓存未命中，返回null（需要预热）
        return null;
    }
    
    // 2. 反序列化
    RedisData redisData = JSONUtil.toBean(json, RedisData.class);
    Shop shop = JSONUtil.toBean((JSONObject) redisData.getData(), Shop.class);
    LocalDateTime expireTime = redisData.getExpireTime();
    
    // 3. 判断是否过期
    if (expireTime.isAfter(LocalDateTime.now())) {
        // 未过期，直接返回
        return shop;
    }
    
    // 4. 已过期，尝试获取锁重建
    String lockKey = LOCK_SHOP_KEY + id;
    boolean isLock = tryLock(lockKey);
    
    if (isLock) {
        // 5. 获取锁成功，开启独立线程重建
        CACHE_REBUILD_EXECUTOR.submit(() -> {
            try {
                // 查询数据库
                Shop newShop = shopMapper.selectById(id);
                // 重建缓存
                setWithLogicalExpire(key, newShop, 20L, TimeUnit.SECONDS);
            } finally {
                unlock(lockKey);
            }
        });
    }
    
    // 6. 返回旧数据（无论是否获取到锁）
    return shop;
}
```

**为什么选择逻辑过期：**
1. 用户体验好：不阻塞，响应快
2. 高并发场景：大量请求不会同时阻塞
3. 数据容忍度：店铺信息更新不频繁，可接受短暂过期

**逻辑过期的缺点：**
1. 数据可能过期：返回旧数据，需要业务容忍
2. 需要预热：首次查询缓存未命中
3. 内存占用：缓存永不过期，占用内存

---

### 题目8：Redisson分布式锁

**原题：** Redisson 的分布式锁默认是可重入的，它的底层数据结构是什么？（Hash）。为什么要用 Hash 而不是简单的 String？

**你的回答：** hash可以存成对的信息，方便修改部分。

**评判：**
- 优点：知道使用Hash
- 不足：未说明Hash存储的具体内容（线程ID、重入次数）

**标准答案：**

**Redisson分布式锁底层结构：**

```java
// Redis中的数据结构
Key: "lock:order:123"
Type: Hash
Fields:
  - "8743c9c0-0795-4907-87fd-6c719a6c4587:1": 1  // 线程ID:重入次数 -> 重入次数值
```

**为什么使用Hash：**
1. 存储重入信息：`线程ID:重入次数 -> 重入次数值`
2. 支持可重入：同一线程可多次获取锁，记录重入次数
3. 原子操作：`HINCRBY` 可原子性增加重入次数
4. 释放锁安全：释放时检查线程ID，只有持有锁的线程才能释放

**代码实现：**

```java
// Redisson内部实现（简化版）
public boolean tryLock(long waitTime, long leaseTime, TimeUnit unit) {
    String threadId = getThreadId(); // UUID:线程ID
    return get(tryLockAsync(waitTime, leaseTime, unit));
}

// Lua脚本：获取锁
String luaScript = 
    "if (redis.call('exists', KEYS[1]) == 0) then " +
    "  redis.call('hset', KEYS[1], ARGV[2], 1); " +  // 设置Hash字段
    "  redis.call('pexpire', KEYS[1], ARGV[1]); " +
    "  return nil; " +
    "end; " +
    "if (redis.call('hexists', KEYS[1], ARGV[2]) == 1) then " +
    "  redis.call('hincrby', KEYS[1], ARGV[2], 1); " +  // 重入次数+1
    "  redis.call('pexpire', KEYS[1], ARGV[1]); " +
    "  return nil; " +
    "end; " +
    "return redis.call('pttl', KEYS[1]);";

// Lua脚本：释放锁
String unlockScript = 
    "if (redis.call('hexists', KEYS[1], ARGV[3]) == 0) then " +
    "  return nil; " +
    "end; " +
    "local counter = redis.call('hincrby', KEYS[1], ARGV[3], -1); " +  // 重入次数-1
    "if (counter > 0) then " +
    "  redis.call('pexpire', KEYS[1], ARGV[2]); " +  // 还有重入，续期
    "  return 0; " +
    "else " +
    "  redis.call('del', KEYS[1]); " +  // 重入次数为0，删除锁
    "  redis.call('publish', KEYS[2], ARGV[1]); " +  // 发布解锁消息
    "  return 1; " +
    "end; " +
    "return nil;";
```

**Hash vs String对比：**

| 特性           | String       | Hash                          |
| -------------- | ------------ | ----------------------------- |
| **存储内容**   | 线程ID       | 线程ID:重入次数 -> 重入次数值 |
| **可重入支持** | 不支持       | 支持                          |
| **原子操作**   | SET/GET      | HINCRBY                       |
| **释放锁**     | 需要额外检查 | 内置重入次数管理              |

---

### 题目9：Redlock问题

**原题：** Redis 如果是主从架构，主节点挂了，锁还没同步到从节点，这时候从节点变主节点，另一个线程来加锁也成功了，这就导致了锁失效（Redlock 问题）。你在项目里考虑过这个问题吗？怎么解决的？

**你的回答：** 没有设计，可以考虑红锁。

**评判：**
- 优点：知道Redlock
- 不足：未说明Redlock的具体实现和适用场景

**标准答案：**

**问题分析：**
1. 主从异步复制：主节点写入锁后，从节点可能未同步
2. 主节点故障：从节点晋升为主节点，锁丢失
3. 锁失效：另一个线程可获取同一把锁

**解决方案：**

```java
// 方案1：Redlock算法（适合高可靠性场景）
@Configuration
public class RedissonConfig {
    @Bean
    public RedissonClient redissonClient() {
        Config config = new Config();
        
        // Redlock：连接多个Redis节点
        config.useClusterServers()
            .addNodeAddress("redis://127.0.0.1:7000")
            .addNodeAddress("redis://127.0.0.1:7001")
            .addNodeAddress("redis://127.0.0.1:7002")
            .addNodeAddress("redis://127.0.0.1:7003")
            .addNodeAddress("redis://127.0.0.1:7004");
        
        return Redisson.create(config);
    }
}

// Redlock原理：
// 1. 向N个Redis节点发送加锁请求
// 2. 当且仅当大多数节点（N/2+1）加锁成功，才认为加锁成功
// 3. 锁的过期时间 = 锁的有效时间 - 获取锁耗时
// 4. 释放锁时，向所有节点发送释放请求

// 方案2：单Redis + 业务层保证（适合一般场景）
// 1. 锁的过期时间设置合理（大于业务执行时间）
// 2. 看门狗机制自动续期
// 3. 业务幂等性保证

// 方案3：Zookeeper分布式锁（强一致性）
// Zookeeper的写操作需要大多数节点确认，天然支持Redlock
```

**Redlock算法步骤：**
1. 获取当前时间T1
2. 依次向N个Redis节点请求加锁
3. 计算加锁耗时 = 当前时间T2 - T1
4. 锁的有效时间 = 锁的过期时间 - 加锁耗时
5. 如果大多数节点加锁成功 && 锁的有效时间 > 0，则加锁成功
6. 否则，向所有节点发送释放锁请求

**适用场景：**
- Redlock：金融、支付等强一致性场景
- 单Redis + 看门狗：一般业务场景（如秒杀）

---

### 题目10：看门狗机制

**原题：** 如果你的业务代码在执行业务时，服务器突然断电了，锁没释放怎么办？Redisson 的看门狗（Watch Dog）机制是怎么工作的？

**你的回答：** 断电了锁信息一起持久化，回复了应该就可以把。看门狗是一个后台线程每隔一段时间检查线程释放还持有锁，如果持有就延迟锁过期时间。

**评判：**
- 优点：基本理解了看门狗机制
- 不足：断电后锁信息不会持久化（Redis是内存数据库），未说明看门狗的续期逻辑

**标准答案：**

**问题分析：**
1. 服务器断电：JVM进程终止，锁无法释放
2. Redis锁过期：锁设置了TTL，过期后自动释放
3. 但业务可能还在执行：如果业务执行时间 > 锁的TTL，锁提前释放

**看门狗机制：**

```java
// Redisson看门狗实现原理
public class RedissonLock {
    private long lockWatchdogTimeout = 30 * 1000; // 默认30秒
    
    // 获取锁（不指定leaseTime时，启用看门狗）
    public boolean tryLock(long waitTime, TimeUnit unit) {
        return tryLock(waitTime, -1, unit); // leaseTime=-1表示启用看门狗
    }
    
    // 看门狗续期逻辑
    private void scheduleExpirationRenewal(String threadId) {
        // 1. 创建定时任务，每 lockWatchdogTimeout / 3 秒执行一次
        Timeout task = commandExecutor.getConnectionManager()
            .newTimeout(new TimerTask() {
                @Override
                public void run(Timeout timeout) {
                    // 2. 检查当前线程是否还持有锁
                    if (internalLockLeaseTime == -1) { // -1表示启用看门狗
                        // 3. 续期：重新设置锁的过期时间
                        RFuture<Boolean> future = renewExpirationAsync(threadId);
                        future.onComplete((res, e) -> {
                            if (e != null) {
                                // 续期失败，停止看门狗
                                return;
                            }
                            if (res) {
                                // 续期成功，继续下一次续期
                                scheduleExpirationRenewal(threadId);
                            }
                        });
                    }
                }
            }, lockWatchdogTimeout / 3, TimeUnit.MILLISECONDS); // 默认10秒续期一次
    }
    
    // 续期Lua脚本
    String renewScript = 
        "if (redis.call('hexists', KEYS[1], ARGV[2]) == 1) then " +
        "  redis.call('pexpire', KEYS[1], ARGV[1]); " +  // 重新设置过期时间
        "  return 1; " +
        "end; " +
        "return 0;";
}
```

**看门狗工作流程：**
1. 获取锁时不指定leaseTime（或leaseTime=-1），启用看门狗
2. 看门狗每10秒（lockWatchdogTimeout/3）检查一次
3. 如果线程还持有锁，执行续期（重新设置30秒过期时间）
4. 如果线程释放锁或进程终止，看门狗停止续期
5. 锁在30秒后自动过期释放

**断电场景处理：**
1. 服务器断电：JVM进程终止，看门狗线程停止
2. 锁自动过期：Redis中的锁TTL到期后自动删除
3. 业务可能未完成：如果业务执行时间 > 30秒，锁提前释放，可能导致并发问题

**最佳实践：**
1. 合理设置锁的过期时间：大于业务执行时间
2. 启用看门狗：自动续期，防止业务未完成锁就过期
3. 业务幂等性：即使锁提前释放，业务也要保证幂等

---

### 题目11：Redis Stream消息队列

**原题：** 为什么不用 RabbitMQ 或者 RocketMQ，而选择 Redis Stream？在什么场景下 Redis Stream 会有性能瓶颈？

**你的回答：** Stream更轻量化，这个项目也是一个小型项目，Stream足够了。在分布式场景下，高并发场景下，严格的消息顺序，持久化，可靠性场景下Stream无法满足。

**评判：**
- 优点：正确说明了选型理由和局限性
- 不足：未说明具体的性能瓶颈点

**标准答案：**

**选型对比：**

| 特性           | Redis Stream          | RabbitMQ         | RocketMQ       |
| -------------- | --------------------- | ---------------- | -------------- |
| **部署复杂度** | 低（Redis已部署）     | 中               | 高             |
| **消息持久化** | 支持（AOF/RDB）       | 支持（磁盘）     | 支持（磁盘）   |
| **消息顺序**   | 支持（单消费者组）    | 支持（单队列）   | 支持（单队列） |
| **可靠性**     | 中（依赖Redis持久化） | 高（ACK机制）    | 高（ACK机制）  |
| **性能**       | 高（内存操作）        | 中               | 高             |
| **适用场景**   | 中小型项目，轻量级    | 企业级，高可靠性 | 大规模，高并发 |

**为什么选择Redis Stream：**
1. 项目规模小：秒杀订单量不大，Stream足够
2. 技术栈统一：项目已使用Redis，无需引入新中间件
3. 部署简单：无需单独部署MQ服务
4. 学习成本低：团队熟悉Redis

**Redis Stream性能瓶颈：**
1. 内存限制：消息存储在内存，数据量大时内存压力大
2. 持久化性能：AOF同步写入影响性能
3. 单机性能：单Redis实例有性能上限
4. 消息堆积：大量未消费消息占用内存
5. 集群支持：Redis Cluster对Stream支持有限

**代码实现：**

```java
// Redis Stream生产者
public void sendOrderMessage(VoucherOrder order) {
    Map<String, Object> message = new HashMap<>();
    message.put("userId", order.getUserId());
    message.put("voucherId", order.getVoucherId());
    message.put("id", order.getId());
    
    // XADD stream.orders * field1 value1 field2 value2
    stringRedisTemplate.opsForStream().add("stream.orders", message);
}

// Redis Stream消费者
@PostConstruct
public void init() {
    SECKILL_ORDER_EXECUTOR.submit(new VoucherOrderHandler());
}

private class VoucherOrderHandler implements Runnable {
    @Override
    public void run() {
        while (true) {
            try {
                // XREADGROUP GROUP g1 c1 COUNT 1 BLOCK 2000 STREAMS stream.orders >
                List<MapRecord<String, Object, Object>> list = 
                    stringRedisTemplate.opsForStream().read(
                        Consumer.from("g1", "c1"),
                        StreamReadOptions.empty()
                            .count(1)
                            .block(Duration.ofSeconds(2)),
                        StreamOffset.create("stream.orders", ReadOffset.lastConsumed())
                    );
                
                if (list == null || list.isEmpty()) {
                    continue;
                }
                
                // 处理消息
                MapRecord<String, Object, Object> record = list.get(0);
                VoucherOrder order = parseOrder(record);
                createVoucherOrder(order);
                
                // 确认消息
                stringRedisTemplate.opsForStream().acknowledge(
                    "stream.orders", "g1", record.getId()
                );
                
            } catch (Exception e) {
                log.error("处理订单异常", e);
                handlePendingList(); // 处理pending-list
            }
        }
    }
}
```

**什么时候需要升级到RabbitMQ/RocketMQ：**
1. 消息量 > 100万/天
2. 需要严格的消息顺序
3. 需要消息事务
4. 需要消息路由、优先级等高级特性
5. 需要跨机房部署

---

### 题目12：Redis Stream消息重试

**原题：** 消费者拿到消息后，处理业务失败了，Redis Stream 是怎么保证消息重试的？你是手动 ACK 还是自动 ACK？

**你的回答：** 手动ACK，保证重试的话是检查pendinglist有消息就继续消费直到ACK。

**评判：**
- 优点：知道手动ACK和pending-list
- 不足：未说明pending-list的具体处理逻辑

**标准答案：**

**消息重试机制：**

```java
// 手动ACK + Pending-List处理
private class VoucherOrderHandler implements Runnable {
    @Override
    public void run() {
        while (true) {
            try {
                // 1. 先处理pending-list（失败重试的消息）
                handlePendingList();
                
                // 2. 处理新消息
                List<MapRecord<String, Object, Object>> list = 
                    stringRedisTemplate.opsForStream().read(
                        Consumer.from("g1", "c1"),
                        StreamReadOptions.empty()
                            .count(1)
                            .block(Duration.ofSeconds(2)),
                        StreamOffset.create("stream.orders", ReadOffset.lastConsumed())
                    );
                
                if (list == null || list.isEmpty()) {
                    continue;
                }
                
                // 3. 处理消息
                MapRecord<String, Object, Object> record = list.get(0);
                processMessage(record);
                
            } catch (Exception e) {
                log.error("处理订单异常", e);
            }
        }
    }
    
    // 处理pending-list（失败重试）
    private void handlePendingList() {
        while (true) {
            try {
                // XREADGROUP GROUP g1 c1 COUNT 1 STREAMS stream.orders 0
                // 0表示从pending-list读取
                List<MapRecord<String, Object, Object>> list = 
                    stringRedisTemplate.opsForStream().read(
                        Consumer.from("g1", "c1"),
                        StreamReadOptions.empty().count(1),
                        StreamOffset.create("stream.orders", ReadOffset.from("0"))
                    );
                
                if (list == null || list.isEmpty()) {
                    break; // pending-list为空，退出
                }
                
                // 处理pending消息
                MapRecord<String, Object, Object> record = list.get(0);
                processMessage(record);
                
            } catch (Exception e) {
                log.error("处理pending消息异常", e);
                break;
            }
        }
    }
    
    // 处理消息（带重试机制）
    private void processMessage(MapRecord<String, Object, Object> record) {
        int retryCount = 0;
        int maxRetries = 3;
        
        while (retryCount < maxRetries) {
            try {
                // 1. 解析消息
                VoucherOrder order = parseOrder(record);
                
                // 2. 处理业务
                createVoucherOrder(order);
                
                // 3. 手动ACK（确认消费成功）
                stringRedisTemplate.opsForStream().acknowledge(
                    "stream.orders", "g1", record.getId()
                );
                
                return; // 处理成功，退出
                
            } catch (Exception e) {
                retryCount++;
                log.error("处理消息失败，重试次数：{}/{}", retryCount, maxRetries, e);
                
                if (retryCount >= maxRetries) {
                    // 超过重试次数，发送到死信队列或记录日志
                    sendToDeadLetterQueue(record);
                    // 仍然ACK，避免无限重试
                    stringRedisTemplate.opsForStream().acknowledge(
                        "stream.orders", "g1", record.getId()
                    );
                } else {
                    // 等待后重试
                    try {
                        Thread.sleep(1000 * retryCount); // 指数退避
                    } catch (InterruptedException ie) {
                        Thread.currentThread().interrupt();
                    }
                }
            }
        }
    }
}
```

**Redis Stream消息流转：**
1. 新消息：`XADD stream.orders * field value` → 进入stream
2. 消费者读取：`XREADGROUP GROUP g1 c1 STREAMS stream.orders >` → 消息进入pending-list
3. 处理成功：`XACK stream.orders g1 messageId` → 从pending-list移除
4. 处理失败：不ACK → 消息留在pending-list
5. 重试：`XREADGROUP GROUP g1 c1 STREAMS stream.orders 0` → 从pending-list读取

**手动ACK vs 自动ACK：**
- 手动ACK：处理成功后才ACK，失败消息留在pending-list，可重试
- 自动ACK：读取即ACK，失败消息无法重试（不推荐）

---

### 题目13：秒杀超卖问题

**原题：** 你提到了 "乐观锁策略"，在 SQL 层面是怎么写的？如果并发量特别大，乐观锁导致大量请求失败，系统的吞吐量会急剧下降，这时候怎么优化？

**你的回答：** 就是where条件哦安短释放满足条件stock>0。改为悲观锁，或者把数据放入缓存。

**评判：**
- 优点：知道乐观锁的SQL写法
- 不足：未说明乐观锁失败的原因，优化方案不够具体

**标准答案：**

**乐观锁SQL实现：**

```java
// 方案1：版本号机制
UPDATE seckill_voucher 
SET stock = stock - 1, version = version + 1 
WHERE voucher_id = ? AND version = ? AND stock > 0;

// 方案2：CAS机制（推荐）
UPDATE seckill_voucher 
SET stock = stock - 1 
WHERE voucher_id = ? AND stock > 0;  // stock>0保证不会超卖

// MyBatis-Plus实现
boolean success = seckillVoucherService.update()
    .setSql("stock = stock - 1")
    .eq("voucher_id", voucherId)
    .gt("stock", 0)  // 乐观锁：stock > 0
    .update();
```

**乐观锁失败原因：**
1. 高并发下，多个线程同时读取stock=1
2. 多个线程同时执行UPDATE，但只有一个成功（stock变为0）
3. 其他线程UPDATE失败（affected rows = 0），业务失败

**优化方案：**

```java
// 方案1：Redis预扣库存 + 异步下单（推荐）
@Override
public Result seckillVoucher(Long voucherId) {
    Long userId = UserHolder.getUser().getId();
    
    // 1. Lua脚本原子性扣减Redis库存
    Long result = stringRedisTemplate.execute(
        SECKILL_SCRIPT,
        Collections.emptyList(),
        voucherId.toString(), userId.toString(), orderId.toString()
    );
    
    if (result.intValue() != 0) {
        return Result.fail(result == 1 ? "库存不足" : "不能重复下单");
    }
    
    // 2. 有购买资格，发送到消息队列异步下单
    VoucherOrder order = new VoucherOrder();
    order.setId(orderId);
    order.setUserId(userId);
    order.setVoucherId(voucherId);
    
    stringRedisTemplate.opsForStream().add("stream.orders", 
        BeanUtil.beanToMap(order));
    
    return Result.ok(orderId);
}

// Lua脚本：原子性扣库存
String seckillScript = 
    "local stockKey = 'seckill:stock:' .. ARGV[1] " +
    "local orderKey = 'seckill:order:' .. ARGV[1] " +
    "local userId = ARGV[2] " +
    "local orderId = ARGV[3] " +
    " " +
    "if (tonumber(redis.call('get', stockKey)) <= 0) then " +
    "  return 1 " +
    "end " +
    "if (redis.call('sismember', orderKey, userId) == 1) then " +
    "  return 2 " +
    "end " +
    "redis.call('incrby', stockKey, -1) " +
    "redis.call('sadd', orderKey, userId) " +
    "redis.call('xadd', 'stream.orders', '*', 'userId', userId, 'voucherId', ARGV[1], 'id', orderId) " +
    "return 0";

// 方案2：分段锁（适合库存特别大的场景）
// 将库存分成多个段，每个段一个锁，减少锁竞争
public Result seckillVoucher(Long voucherId) {
    // 1. 根据userId取模，选择分段
    int segment = (int) (userId % 10);
    String lockKey = "lock:seckill:" + voucherId + ":" + segment;
    
    RLock lock = redissonClient.getLock(lockKey);
    // ... 处理逻辑
}

// 方案3：令牌桶限流（控制并发）
@RateLimiter(qps = 100)  // 限制QPS为100
public Result seckillVoucher(Long voucherId) {
    // ... 处理逻辑
}
```

**性能对比：**
- 乐观锁：高并发下大量失败，吞吐量低
- Redis预扣库存：内存操作，性能高，吞吐量大
- 异步下单：削峰填谷，数据库压力小

---

### 题目14：一人一单限制

**原题：** 你提到了 "用户维度的分布式锁"，具体的 Key 是怎么设计的？如果有一个用户疯狂点击，锁会不会一直被他自己重入持有，导致其他用户也买不到？

**你的回答：** 前缀+userid。不会，锁的是用户维度，不同用户是并行的。

**评判：**
- 优点：正确理解了用户维度锁
- 不足：未说明重入锁的问题，未说明如何防止同一用户重复下单

**标准答案：**

**锁的Key设计：**

```java
// 锁的Key：lock:order:{userId}
String lockKey = "lock:order:" + userId;
RLock lock = redissonClient.getLock(lockKey);

// 为什么用userId而不是voucherId？
// 1. 一人一单：限制的是用户维度，不是商品维度
// 2. 不同用户并行：不同用户的锁不同，不会相互阻塞
// 3. 同一用户串行：同一用户的多次请求串行处理，防止重复下单
```

**重入锁问题分析：**

```java
// 场景：用户疯狂点击
// 请求1：获取锁 lock:order:123，执行业务（查询订单、扣库存、创建订单）
// 请求2：同一用户，获取同一把锁 lock:order:123，等待请求1释放
// 请求3：同一用户，获取同一把锁 lock:order:123，等待请求1释放

// 问题：如果使用可重入锁，请求2和请求3会重入，导致重复下单？

// 答案：不会，因为业务逻辑中有重复下单检查
private void createVoucherOrder(VoucherOrder voucherOrder) {
    Long userId = voucherOrder.getUserId();
    Long voucherId = voucherOrder.getVoucherId();
    
    RLock lock = redissonClient.getLock("lock:order:" + userId);
    boolean isLock = lock.tryLock();  // 非阻塞获取锁
    
    if (!isLock) {
        // 获取锁失败，说明该用户正在处理订单，直接返回
        log.error("不允许重复下单！");
        return;
    }
    
    try {
        // 1. 查询订单（双重检查）
        int count = query().eq("user_id", userId)
                          .eq("voucher_id", voucherId)
                          .count();
        if (count > 0) {
            // 用户已经购买过了
            return;
        }
        
        // 2. 扣减库存
        boolean success = seckillVoucherService.update()
            .setSql("stock = stock - 1")
            .eq("voucher_id", voucherId)
            .gt("stock", 0)
            .update();
        
        if (!success) {
            return;
        }
        
        // 3. 创建订单
        save(voucherOrder);
        
    } finally {
        lock.unlock();
    }
}
```

**为什么不会导致其他用户买不到：**
1. 锁的粒度是用户维度：`lock:order:{userId}`
2. 不同用户的锁不同：用户A的锁不影响用户B
3. 同一用户的请求串行：防止重复下单

**防止同一用户重复下单的措施：**
1. 分布式锁：同一用户的请求串行处理
2. 数据库唯一索引：`UNIQUE KEY uk_user_voucher (user_id, voucher_id)`
3. Redis Set记录：`SADD seckill:order:{voucherId} {userId}`

---

## 第三阶段：Java基础与底层

### 题目15：ThreadPoolExecutor参数

**原题：** 如果让你手写一个ThreadPoolExecutor，那 7 个参数分别是什么？workQueue（工作队列）满了之后，线程池会怎么处理新来的任务？（拒绝策略）

**你的回答：** 核心线程数，最大线程数，非核心线程存活时间，时间单位，工作队列，解决策略，线程池工厂。拒绝策略有callerrunpolicy\abort\abort latest\ajected。

**评判：**
- 优点：基本说出了7个参数
- 不足：参数名称不够准确，拒绝策略名称有误

**标准答案：**

**ThreadPoolExecutor的7个参数：**

```java
public ThreadPoolExecutor(
    int corePoolSize,              // 1. 核心线程数
    int maximumPoolSize,           // 2. 最大线程数
    long keepAliveTime,            // 3. 非核心线程存活时间
    TimeUnit unit,                 // 4. 时间单位
    BlockingQueue<Runnable> workQueue,  // 5. 工作队列
    ThreadFactory threadFactory,   // 6. 线程工厂
    RejectedExecutionHandler handler    // 7. 拒绝策略
)
```

**参数详解：**
1. **corePoolSize**：核心线程数，线程池长期保持的线程数量
2. **maximumPoolSize**：最大线程数，线程池允许的最大线程数量
3. **keepAliveTime**：非核心线程空闲时的最大存活时间
4. **unit**：时间单位（TimeUnit.SECONDS等）
5. **workQueue**：工作队列，存储待执行的任务
6. **threadFactory**：线程工厂，用于创建线程
7. **handler**：拒绝策略，当队列满且线程数达到最大值时的处理策略

**拒绝策略（4种）：**

```java
// 1. AbortPolicy（默认）：抛出异常
new ThreadPoolExecutor.AbortPolicy();
// 抛出 RejectedExecutionException

// 2. CallerRunsPolicy：由调用线程执行
new ThreadPoolExecutor.CallerRunsPolicy();
// 如果线程池未关闭，由提交任务的线程执行

// 3. DiscardPolicy：静默丢弃
new ThreadPoolExecutor.DiscardPolicy();
// 什么都不做，静默丢弃任务

// 4. DiscardOldestPolicy：丢弃最老的任务
new ThreadPoolExecutor.DiscardOldestPolicy();
// 丢弃队列中最老的任务，然后重新提交当前任务
```

**执行流程：**
```
1. 提交任务
   ↓
2. 当前线程数 < corePoolSize？
   ├─ 是 → 创建核心线程执行
   └─ 否 → 3
   ↓
3. 任务入队（workQueue.offer()）
   ├─ 成功 → 等待执行
   └─ 失败（队列满）→ 4
   ↓
4. 当前线程数 < maximumPoolSize？
   ├─ 是 → 创建非核心线程执行
   └─ 否 → 5
   ↓
5. 执行拒绝策略
```

---

### 题目16：Full GC频繁排查

**原题：** 项目上线后，如果发现 Full GC 频繁，可能是什么原因？你会怎么排查？

**你的回答：** 内存泄漏，大对象，新生代老年代比例，有没有大量临时对象，jvm参数，有没有一次性加载大量数据。

**评判：**
- 优点：基本说出了常见原因
- 不足：未说明具体的排查步骤和工具

**标准答案：**

**Full GC频繁的可能原因：**
1. 内存泄漏：对象无法被回收，老年代逐渐填满
2. 大对象：直接进入老年代，占用大量空间
3. 新生代设置过小：Minor GC频繁，对象过早晋升到老年代
4. 老年代设置过小：老年代空间不足，频繁Full GC
5. 大量临时对象：频繁创建大对象，老年代压力大

**排查步骤：**

```bash
# 1. 查看GC日志
java -XX:+PrintGCDetails -XX:+PrintGCDateStamps -Xloggc:gc.log YourApp

# 2. 查看堆内存使用情况
jmap -heap <pid>

# 3. 导出堆转储文件
jmap -dump:format=b,file=heap.hprof <pid>

# 4. 查看对象统计
jmap -histo <pid> | head -20

# 5. 实时监控GC
jstat -gc <pid> 1000 10  # 每1秒输出一次，共10次
```

**使用MAT分析堆转储：**
1. 打开MAT，加载heap.hprof
2. 查看Histogram：找出占用内存大的对象
3. 查看Dominator Tree：找出持有大量内存的对象
4. 查看Leak Suspects：自动检测内存泄漏

**常见问题及解决方案：**

```java
// 问题1：内存泄漏（未关闭资源）
public void processFile() {
    FileInputStream fis = new FileInputStream("file.txt");
    // 忘记关闭，导致内存泄漏
}

// 解决：使用try-with-resources
public void processFile() {
    try (FileInputStream fis = new FileInputStream("file.txt")) {
        // 自动关闭
    }
}

// 问题2：大对象（一次性加载大量数据）
List<String> allData = loadAllData(); // 加载100万条数据

// 解决：分批处理
public void processData() {
    int batchSize = 1000;
    for (int i = 0; i < total; i += batchSize) {
        List<String> batch = loadBatch(i, batchSize);
        processBatch(batch);
    }
}

// 问题3：JVM参数不合理
-Xms512m -Xmx512m  // 堆内存太小

// 解决：调整JVM参数
-Xms2g -Xmx2g                    // 增大堆内存
-XX:NewRatio=2                   // 新生代:老年代 = 1:2
-XX:SurvivorRatio=8              // Eden:Survivor = 8:1
-XX:+UseG1GC                     // 使用G1垃圾回收器
-XX:MaxGCPauseMillis=200        // 最大GC停顿时间
```

---

### 题目17：MySQL索引

**原题：** 在你的医美系统里，"入库单标识" 需要支持筛选，你在设计表结构时，这个字段加索引了吗？如果这个字段是字符串且很长，用什么索引类型（Hash 还是 B+Tree）？为什么？

**你的回答：** 这个是帅选字段，加了索引。很长的话考虑hash索引。

**评判：**
- 优点：知道需要加索引
- 不足：回答有误，MySQL的Hash索引只支持Memory引擎，InnoDB不支持Hash索引

**标准答案：**

**索引设计：**

```sql
-- 入库单表
CREATE TABLE `inbound_order` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `order_no` VARCHAR(64) NOT NULL COMMENT '入库单标识',
  `comp_id` CHAR(36) NOT NULL,
  `create_time` DATETIME NOT NULL,
  -- 其他字段...
  INDEX `idx_order_no` (`order_no`)  -- 普通B+Tree索引
) ENGINE=InnoDB;
```

**Hash索引 vs B+Tree索引：**

| 特性           | Hash索引         | B+Tree索引       |
| -------------- | ---------------- | ---------------- |
| **支持引擎**   | 仅Memory引擎     | InnoDB、MyISAM等 |
| **查询类型**   | 仅等值查询（=）  | 等值、范围、排序 |
| **时间复杂度** | O(1)             | O(log n)         |
| **字符串长度** | 无限制           | 无限制           |
| **适用场景**   | 内存表，等值查询 | 大多数场景       |

**为什么选择B+Tree索引：**
1. InnoDB不支持Hash索引：MySQL的Hash索引仅Memory引擎支持
2. 支持范围查询：`WHERE order_no > 'xxx'`
3. 支持排序：`ORDER BY order_no`
4. 支持前缀索引：`INDEX idx_order_no (order_no(10))`（如果字符串很长）

**字符串很长时的优化：**

```sql
-- 方案1：前缀索引（推荐）
CREATE INDEX idx_order_no ON inbound_order(order_no(20));
-- 只索引前20个字符，节省空间

-- 方案2：使用CRC32/MD5（如果只需要等值查询）
ALTER TABLE inbound_order ADD COLUMN order_no_hash INT;
CREATE INDEX idx_order_no_hash ON inbound_order(order_no_hash);
-- 查询时：WHERE order_no_hash = CRC32('xxx') AND order_no = 'xxx'

-- 方案3：使用全文索引（如果需要模糊查询）
CREATE FULLTEXT INDEX idx_order_no_fulltext ON inbound_order(order_no);
```

---

### 题目18：@Transactional事务失效

**原题：** 如果一个入库方法 A 调用了同一个类里的另一个有@Transactional注解的方法 B，事务会生效吗？为什么？怎么解决？

**你的回答：** 不会。

**评判：**
- 优点：知道事务不会生效
- 不足：未说明原因和解决方案

**标准答案：**

**问题分析：**

```java
@Service
public class InboundService {
    
    public void methodA() {
        // 方法A没有事务
        this.methodB();  // 调用同一个类的方法B
    }
    
    @Transactional
    public void methodB() {
        // 方法B有事务注解，但不会生效
        inboundMapper.insert(order);
    }
}
```

**为什么不会生效：**
1. Spring事务基于AOP代理：`@Transactional`通过代理对象生效
2. 内部调用绕过代理：`this.methodB()`是直接调用，不是代理对象调用
3. 事务不生效：AOP拦截不到内部调用

**解决方案：**

```java
// 方案1：将方法B提取到另一个Service（推荐）
@Service
public class InboundService {
    @Autowired
    private InboundDetailService inboundDetailService;
    
    public void methodA() {
        inboundDetailService.methodB();  // 通过代理对象调用
    }
}

@Service
public class InboundDetailService {
    @Transactional
    public void methodB() {
        // 事务生效
    }
}

// 方案2：方法A也加@Transactional
@Service
public class InboundService {
    @Transactional
    public void methodA() {
        this.methodB();  // 在同一个事务中
    }
    
    public void methodB() {
        // 不需要@Transactional，已经在事务中
    }
}

// 方案3：通过ApplicationContext获取代理对象
@Service
public class InboundService implements ApplicationContextAware {
    private ApplicationContext applicationContext;
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) {
        this.applicationContext = applicationContext;
    }
    
    public void methodA() {
        InboundService proxy = applicationContext.getBean(InboundService.class);
        proxy.methodB();  // 通过代理对象调用
    }
    
    @Transactional
    public void methodB() {
        // 事务生效
    }
}

// 方案4：使用AopContext（需要开启exposeProxy）
@EnableAspectJAutoProxy(exposeProxy = true)
@Service
public class InboundService {
    public void methodA() {
        InboundService proxy = (InboundService) AopContext.currentProxy();
        proxy.methodB();  // 通过代理对象调用
    }
    
    @Transactional
    public void methodB() {
        // 事务生效
    }
}
```

**推荐方案：** 方案1（提取到另一个Service），代码清晰，符合单一职责原则

---

## 第四阶段：系统设计与开放性问题

### 题目19：附近的人功能

**原题：** 假设现在的 "小众点评" 要做一个 **"附近的人"** 功能，展示距离我 1 公里以内的用户。数据库层面：MySQL 里怎么存经纬度？（GeoHash 还是单独字段？）查询层面：如果用 Redis 实现，应该用什么数据结构？（GEO）。算法层面：如果不依赖 Redis，给你一堆经纬度坐标，让你在 Java 内存里找出距离我最近的 K 个点，你会用什么算法？（K-D Tree？或者简单的暴力排序？）

**你的回答：** geoash, geo, （未回答算法）

**评判：**
- 优点：知道GeoHash和Redis GEO
- 不足：未说明具体实现，未回答算法问题

**标准答案：**

**1. 数据库存储经纬度：**

```sql
-- 方案1：单独字段（推荐）
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY,
  `longitude` DECIMAL(10, 7) NOT NULL COMMENT '经度',
  `latitude` DECIMAL(10, 7) NOT NULL COMMENT '纬度',
  INDEX `idx_location` (`longitude`, `latitude`)
) ENGINE=InnoDB;

-- 方案2：GeoHash（适合范围查询）
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY,
  `longitude` DECIMAL(10, 7) NOT NULL,
  `latitude` DECIMAL(10, 7) NOT NULL,
  `geohash` VARCHAR(12) NOT NULL,  -- GeoHash编码
  INDEX `idx_geohash` (`geohash`)
) ENGINE=InnoDB;
```

**GeoHash vs 单独字段：**

| 特性         | 单独字段        | GeoHash         |
| ------------ | --------------- | --------------- |
| **存储**     | 两个DECIMAL字段 | 一个VARCHAR字段 |
| **查询**     | 需要计算距离    | 前缀匹配快速    |
| **精度**     | 精确            | 有精度损失      |
| **适用场景** | 精确查询        | 范围查询        |

**2. Redis GEO实现：**

```java
// Redis GEO数据结构（底层是ZSet）
// Key: user:geo
// Member: userId
// Score: GeoHash编码后的整数

// 添加用户位置
stringRedisTemplate.opsForGeo().add(
    "user:geo", 
    new Point(longitude, latitude), 
    userId.toString()
);

// 查询附近的人（1公里内）
GeoResults<RedisGeoCommands.GeoLocation<String>> results = 
    stringRedisTemplate.opsForGeo().search(
        "user:geo",
        GeoReference.fromCoordinate(myLongitude, myLatitude),
        new Distance(1, Metrics.KILOMETERS),  // 1公里
        RedisGeoCommands.GeoSearchCommandArgs.newGeoSearchArgs()
            .includeDistance()
            .limit(10)  // 返回前10个
    );

// 解析结果
for (GeoResult<RedisGeoCommands.GeoLocation<String>> result : results) {
    String userId = result.getContent().getName();
    Distance distance = result.getDistance();
    // 处理结果
}
```

**3. Java内存算法：**

```java
// 方案1：暴力排序（适合数据量小，<1万）
public List<User> findNearbyUsers(double myLng, double myLat, int k) {
    List<User> allUsers = userService.getAllUsers();
    
    // 计算所有用户的距离并排序
    return allUsers.stream()
        .map(user -> {
            double distance = calculateDistance(
                myLng, myLat, 
                user.getLongitude(), user.getLatitude()
            );
            return new UserWithDistance(user, distance);
        })
        .sorted(Comparator.comparing(UserWithDistance::getDistance))
        .limit(k)
        .map(UserWithDistance::getUser)
        .collect(Collectors.toList());
}

// 计算距离（Haversine公式）
private double calculateDistance(double lng1, double lat1, 
                                 double lng2, double lat2) {
    double R = 6371; // 地球半径（公里）
    double dLat = Math.toRadians(lat2 - lat1);
    double dLng = Math.toRadians(lng2 - lng1);
    double a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
               Math.cos(Math.toRadians(lat1)) * 
               Math.cos(Math.toRadians(lat2)) *
               Math.sin(dLng / 2) * Math.sin(dLng / 2);
    double c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c;
}

// 方案2：K-D Tree（适合数据量大，>1万）
public class KDTree {
    private Node root;
    
    // 构建K-D Tree
    public void build(List<Point> points) {
        root = buildTree(points, 0);
    }
    
    // 查询最近的K个点
    public List<Point> findKNearest(Point target, int k) {
        PriorityQueue<PointDistance> heap = new PriorityQueue<>(
            (a, b) -> Double.compare(b.distance, a.distance)
        );
        search(root, target, k, heap, 0);
        return heap.stream()
            .map(pd -> pd.point)
            .collect(Collectors.toList());
    }
    
    private void search(Node node, Point target, int k, 
                       PriorityQueue<PointDistance> heap, int depth) {
        if (node == null) return;
        
        // 计算距离
        double distance = calculateDistance(node.point, target);
        if (heap.size() < k || distance < heap.peek().distance) {
            heap.offer(new PointDistance(node.point, distance));
            if (heap.size() > k) {
                heap.poll();
            }
        }
        
        // 选择搜索方向
        int axis = depth % 2;
        double diff = axis == 0 ? 
            target.x - node.point.x : 
            target.y - node.point.y;
        
        Node near = diff < 0 ? node.left : node.right;
        Node far = diff < 0 ? node.right : node.left;
        
        search(near, target, k, heap, depth + 1);
        
        // 如果最远距离 > 分割线距离，需要搜索另一侧
        if (heap.size() < k || 
            Math.abs(diff) < heap.peek().distance) {
            search(far, target, k, heap, depth + 1);
        }
    }
}

// 方案3：GeoHash + 内存过滤（推荐，平衡性能和复杂度）
public List<User> findNearbyUsers(double myLng, double myLat, int k) {
    // 1. 计算当前GeoHash
    String myGeohash = GeoHashUtils.encode(myLat, myLng, 6);  // 6位精度约1.2km
    
    // 2. 获取相邻8个GeoHash块（包括自己）
    Set<String> geohashBlocks = getNeighborGeohashes(myGeohash);
    
    // 3. 从这些块中筛选用户
    List<User> candidates = userService.getUsersByGeohashes(geohashBlocks);
    
    // 4. 计算精确距离并排序
    return candidates.stream()
        .map(user -> {
            double distance = calculateDistance(
                myLng, myLat, 
                user.getLongitude(), user.getLatitude()
            );
            return new UserWithDistance(user, distance);
        })
        .filter(ud -> ud.getDistance() <= 1.0)  // 1公里内
        .sorted(Comparator.comparing(UserWithDistance::getDistance))
        .limit(k)
        .map(UserWithDistance::getUser)
        .collect(Collectors.toList());
}
```

**算法选择：**
- 数据量 < 1万：暴力排序（简单）
- 数据量 1万-10万：GeoHash + 内存过滤（平衡）
- 数据量 > 10万：K-D Tree（性能最优）

---

## 总结

以上是各题目的评判与标准答案。建议：
1. 深入理解原理，不只是记忆
2. 结合项目经验，用具体例子说明
3. 承认不足，说明学习计划
4. 展示思考过程，体现解决问题的能力

祝你面试顺利。