## 模块一 Java 基础与语法（22 分）

### 一、基础单选（5×1）

1. 以下关于 Integer 缓存区间与装箱原理，说法错误的是（　）

   A. 默认缓存范围：-128~127，可通过 JVM 参数调整上限

   B. `Integer a = 10;` 底层执行 `Integer.valueOf()`

   C. `new Integer(10)` 会进入缓存池复用对象

   D. 包装类 == 比较未超出缓存时为 true，超出为 false

2. 下列代码运行结果是（　）

```java
public static void main(String[] args) {
    String s1 = "abc";
    String s2 = new String("abc");
    String s3 = s2.intern();
    System.out.println(s1 == s2);
    System.out.println(s1 == s3);
}
```

   A. false true　　B. true false　　C. true true　　D. false false

3. 下列异常分类与处理说法正确的是（　）

   A. Error 必须捕获处理

   B. RuntimeException 编译期强制检查

   C. IOException 属于受检异常

   D. throw 与 throws 作用一致

4. 关于 final、finally、finalize 说法正确的是（　）

   A. final 修饰的方法可被重写

   B. finally 唯一不执行的情况是 JVM 退出

   C. finalize 是 Object 方法，由 GC 确定性调用

   D. final 修饰的集合，内部元素不可修改

5. 方法参数传递机制，说法正确的是（　）

   A. 基本类型传值，引用类型传引用本身

   B. Java 只有值传递一种

   C. 引用类型传参可修改对象地址

   D. 数组传参属于引用传递

### 二、多选（3×2）

1. 以下会导致线程安全问题的代码场景有（　）

   A. 多线程操作 static 变量

   B. 多线程修改同一个集合

   C. 多线程使用 SimpleDateFormat

   D. 多线程使用 ThreadLocal 存各自数据

2. 关于 String、StringBuilder、StringBuffer 正确的是（　）

   A. String 不可变，底层 char [] 被 final 修饰

   B. StringBuilder 线程不安全，性能高

   C. StringBuffer 线程安全，使用 synchronized

   D. + 拼接字符串底层会自动优化为 StringBuilder

3. 下列能正确实现 equals & hashCode 约定的是（　）

   A. 两个对象 equals 为 true，则 hashCode 必须相同

   B. hashCode 相同，equals 一定为 true

   C. 重写 equals 必须重写 hashCode

   D. 实体类属性变更，hashCode 应保持不变

### 三、简答 & 源码（11 分）

1. 解释 Java 不可变对象的设计、优点、实现方式，举例标准不可变类。（4 分）

2. 阐述静态分派与动态分派原理，对应重载与重写的底层实现。（4 分）

3. 写出你理解的：高并发下 HashMap 可能出现的问题（JDK1.7 死链 / JDK1.8 覆盖）。（3 分）

---

## 模块二 集合框架 & 数据结构（26 分）

### 一、多选（4×2）

1. HashMap JDK1.8 结构与扩容说法正确的是（　）

   A. 数组 + 链表 + 红黑树

   B. 树化阈值 8，链表化阈值 6

   C. 扩容是原容量 2 倍，rehash 用高位运算

   D. 1.8 解决了并发下链表成环问题，但仍非线程安全

2. 下列线程安全集合与实现机制对应正确的是（　）

   A. Vector：synchronized 方法

   B. ConcurrentHashMap1.7：Segment 分段锁

   C. ConcurrentHashMap1.8：CAS + synchronized

   D. CopyOnWriteArrayList：写时复制，读无锁

3. 关于 TreeMap / TreeSet 正确的是（　）

   A. 基于红黑树

   B. 元素必须可比较：实现 Comparable 或传入 Comparator

   C. containsKey 时间复杂度 O(logn)

   D. 插入重复 key 会抛异常

4. 下列属于 Queue 体系、支持阻塞功能的是（　）

   A. ArrayDeque　　B. LinkedList　　C. ArrayBlockingQueue　　D. PriorityQueue

### 二、简答（18 分）

1. ArrayList 扩容机制：初始容量、扩容因子、扩容流程、内存复制细节。（4 分）

2. HashMap put 流程完整描述：hash 计算 -> 寻址 -> 覆盖 / 链表 / 树化 -> 扩容判断。（5 分）

3. ConcurrentHashMap1.8 如何保证安全：锁粒度、CAS 用途、synchronized 锁对象、size 统计方式。（5 分）

4. 为什么建议指定集合初始容量？如何计算合理初始值？（4 分）

---

## 模块三 JVM 虚拟机（深度版，30 分）

### 一、单选（5×1）

1. JVM 运行时数据区，下列哪块是线程共享且会 OOM（　）

   A. 虚拟机栈　　B. 本地方法栈　　C. 程序计数器　　D. 堆 & 元空间

2. 垃圾回收判断对象存活算法（　）

   A. 引用计数　　B. 可达性分析　　C. 标记清除　　D. 分代回收

3. 下列不属于 GC Roots 的是（　）

   A. 栈中局部变量引用对象

   B. 类静态属性引用对象

   C. 已不可达的弱引用对象

   D. JNI 引用对象

4. G1 收集器核心特点是（　）

   A. 单代连续内存　　B. 分区 Region、可预测停顿　　C. 追求最大吞吐量　　D. 全程 Stop-The-World

5. 类加载双亲委派，下列说法错误的是（　）

   A. 自底向上检查是否加载，自顶向下尝试加载

   B. 防止核心类被替换

   C. 可以通过破坏双亲实现热部署

   D. 所有类加载器必须严格遵循不可破坏

### 二、多选（4×2）

1. 堆内存溢出 (OOM) 常见原因（　）

   A. 死循环创建大对象

   B. 内存泄漏，对象无法 GC

   C. 栈深度过大 StackOverflow

   D. 静态集合持有大量对象

2. 关于四种引用：强 / 软 / 弱 / 虚 正确（　）

   A. 软引用：内存不足才回收，适合缓存

   B. 弱引用：GC 必回收

   C. 虚引用：必须配合 ReferenceQueue，用于堆外内存管理

   D. 强引用任何时候不会被回收

3. JVM 调优常用参数正确（　）

   A. -Xms -Xmx 堆初始与最大

   B. -XX:MetaspaceSize 元空间

   C. -XX:+UseG1GC 使用 G1

   D. -XX:+HeapDumpOnOutOfMemoryError OOM 自动 dump

4. JIT 编译与优化包含（　）

   A. 逃逸分析　　B. 标量替换　　C. 栈上分配　　D. 锁消除

### 三、简答 & 场景（17 分）

1. 完整对象创建流程：类加载检查 -> 内存分配 -> 初始化 -> 对象头 -> init。（4 分）

2. CMS 收集器运作流程、优缺点、常见问题（Concurrent Mode Failure）。（5 分）

3. 内存泄漏与内存溢出区别，写出 4 种典型 Java 内存泄漏场景。（4 分）

4. 如何定位 OOM：dump 分析、常用工具、排查思路。（4 分）

---

## 模块四 多线程 & 并发 & JUC（36 分）

### 一、单选（6×1）

1. 线程 start() 与 run() 区别正确（　）

   A. run() 启动线程　　B. start() 使线程进入就绪　　C. 可多次调用 start()　　D. run() 由 JVM 隐式调用

2. synchronized 锁升级顺序（　）

   A. 偏向锁 -> 轻量锁 -> 重量锁

   B. 轻量锁 -> 偏向锁 -> 重量锁

   C. 偏向锁 -> 重量锁 -> 轻量锁

   D. 无锁 -> 重量锁

3. volatile 核心作用不包括（　）

   A. 可见性　　B. 禁止指令重排　　C. 原子性　　D. 防止缓存一致性问题

4. AQS 核心队列与实现机制是（　）

   A. 同步队列 CLH 变体，CAS 维护队列

   B. 数组实现锁竞争

   C. 直接使用 synchronized 封装

   D. 基于 ThreadLocal 管理排队

5. ThreadLocal 内存泄漏根源是（　）

   A. 强引用 Entry　　B. key 是弱引用，value 强引用　　C. 线程自动退出　　D. 使用 static ThreadLocal

6. 下列哪项不属于线程池拒绝策略（　）

   A. AbortPolicy　　B. CallerRunsPolicy　　C. DiscardOldestPolicy　　D. TimeoutPolicy

### 二、多选（4×2）

1. 死锁四个必要条件（　）

   A. 互斥　　B. 请求与保持　　C. 不可剥夺　　D. 循环等待

2. 下列 JUC 工具类用途正确（　）

   A. CountDownLatch：减计数，一次性

   B. CyclicBarrier：可循环屏障

   C. Semaphore：控制并发流量

   D. CompletableFuture：异步编排

3. Lock 接口相对于 synchronized 优势（　）

   A. 可响应中断　　B. 可超时获取锁　　C. 可公平 / 非公平　　D. 自动释放锁

4. 下列保证原子性的方式（　）

   A. volatile　　B. CAS　　C. synchronized　　D. AtomicInteger

### 三、简答 & 手写（22 分）

1. 线程池核心 7 参数、工作流程、如何合理设置核心线程数。（5 分）

2. 简述 AQS 原理：state、共享 / 独占、入队出队、唤醒机制。（4 分）

3. 手写一个安全单例（双重检查 DCL），说明为什么要 volatile。（4 分）

4. 手写一个必然死锁代码，并说明如何定位与避免。（4 分）

5. ThreadLocal 原理、使用场景、如何避免内存泄漏。（5 分）

---

## 模块五 IO & NIO & Netty 基础（20 分）

### 一、单选（4×1）

1. BIO/NIO/AIO 说法正确（　）

   A. BIO 非阻塞　　B. NIO 多路复用　　C. AIO 需轮询　　D. NIO 一个线程处理一个连接

2. NIO Buffer 中 flip() 作用（　）

   A. 写模式切换为读模式　　B. 清空缓冲区　　C. 标记位置　　D. 扩容

3. Netty 核心模型基于（　）

   A. 主从 Reactor 多线程　　B. 单线程 BIO　　C. 阻塞 IO　　D. AIO

4. 序列化方案中，不支持跨语言的是（　）

   A. JSON　　B. Protobuf　　C. Java 原生序列化　　D. Hessian

### 二、简答（16 分）

1. BIO、NIO、AIO 对比：阻塞性、连接模型、线程模型、适用场景。（5 分）

2. NIO 三大组件：Channel/Buffer/Selector 作用与关系。（4 分）

3. Java 原生序列化缺点、安全风险、实际项目替代方案。（3 分）

4. Netty 优势（对比原生 NIO）、核心组件：Bootstrap、Channel、Pipeline、Handler。（4 分）

---

## 模块六 MySQL & MyBatis & 索引 & 事务（38 分）

### 一、单选（6×1）

1. InnoDB 索引数据结构（　）

   A. B+ 树　　B. B 树　　C. 哈希　　D. 红黑树

2. InnoDB 默认隔离级别（　）

   A. READ UNCOMMITTED　　B. READ COMMITTED　　C. REPEATABLE READ　　D. SERIALIZABLE

3. RR 级别解决幻读依靠（　）

   A. 行锁　　B. 表锁　　C. MVCC + 间隙锁　　D. 只读事务

4. 下列会导致索引失效的是（　）

   A. 最左前缀匹配　　B. like 'a%'　　C. 索引列运算 / 函数　　D. 覆盖索引查询

5. MyBatis #{} 与 ${} 核心区别（　）

   A. #{} 预编译防注入，${} 字符串拼接

   B. ${} 预编译防注入

   C. 都一样

   D. #{} 无法传入表名

6. MVCC 核心依赖不包括（　）

   A. 隐藏字段 trx_id/roll_pointer　　B. undo log　　C. read view　　D. redo log

### 二、多选（4×2）

1. 事务 ACID 实现依靠（　）

   A. 原子性：undo log

   B. 持久性：redo log

   C. 隔离性：锁 + MVCC

   D. 一致性：由前三者保障

2. 联合索引生效规则正确（　）

   A. 最左前缀原则

   B. 跳过前面列会导致后面失效

   C. 范围查询后列失效

   D. order by 可利用索引排序

3. MyBatis 缓存说法正确（　）

   A. 一级缓存默认开启 SqlSession 级别

   B. 二级缓存 mapper 级别，默认关闭

   C. 更新语句会清空缓存

   D. 多表查询慎用二级缓存

4. MySQL 锁分类正确（　）

   A. 表锁、行锁、意向锁

   B. 间隙锁、临键锁、记录锁

   C. 共享锁 S、排他锁 X

   D. InnoDB 默认行锁，命中索引才会行锁

### 三、简答 & 优化（24 分）

1. B+ 树为什么适合做 MySQL 索引？对比 B 树、红黑树优势。（4 分）

2. MVCC 实现原理：ReadView、隐藏列、undo log、不同隔离级别可见性规则。（5 分）

3. 详细写出 8 种以上索引失效场景。（4 分）

4. 慢 SQL 排查完整流程：开启慢查询、explain 关键字解读、优化手段。（5 分）

5. MyBatis 一级缓存失效场景、#{} 预处理原理。（3 分）

6. 大表优化思路：分库分表、分区、索引、冷热数据分离。（3 分）

---

## 模块七 Redis 缓存 & 分布式问题（26 分）

### 一、单选（5×1）

1. Redis 线程模型（　）

   A. 单线程 Reactor　　B. 多线程锁竞争　　C. 多进程　　D. 纯异步 IO

2. 缓存雪崩原因不包括（　）

   A. 大量 key 同时过期　　B. Redis 宕机　　C. 缓存穿透恶意请求　　D. 重启未预热

3. Redis 数据持久化（　）

   A. RDB 快照，AOF 日志　　B. 只有 RDB　　C. 只有 AOF　　D. 基于 binlog

4. 缓存穿透解决方案（　）

   A. 互斥锁　　B. 布隆过滤器　　C. 过期随机　　D. 队列削峰

5. Redis 分布式锁核心命令（　）

   A. SETNX EXPIRE 合并原子操作　　B. INCR　　C. HSET　　D. LPUSH

### 二、简答（21 分）

1. 缓存穿透、击穿、雪崩区别、场景、解决方案。（6 分）

2. Redis 过期策略：惰性 + 定期，内存淘汰策略 LRU/LFU 等。（4 分）

3. 分布式锁实现：如何保证互斥、防死锁、误删、可重入、锁续期。（5 分）

4. Redis 集群方案：主从、哨兵、Cluster 分片与高可用。（3 分）

5. 如何保证缓存与数据库一致性。（3 分）

---

## 模块八 Spring & SpringBoot 原理（32 分）

### 一、单选（5×1）

1. Spring IOC 容器核心接口（　）

   A. BeanFactory & ApplicationContext　　B. ApplicationEvent　　C. InitializingBean　　D. DisposableBean

2. Spring AOP 底层代理（　）

   A. 接口用 JDK 代理，类用 CGLIB　　B. 全部 CGLIB　　C. 全部 JDK　　D. 静态代理

3. Spring 事务传播行为 REQUIRED 含义（　）

   A. 有事务则加入，无则新建　　B. 必须已有事务否则异常　　C. 新建独立事务　　D. 非事务运行

4. SpringBoot 自动配置核心是（　）

   A. @EnableAutoConfiguration + SPI 机制　　B. @ComponentScan　　C. @Configuration　　D. @Conditional

5. Bean 生命周期顺序正确（　）

   A. 实例化 -> 属性填充 -> 初始化 -> 销毁

   B. 初始化 -> 实例化 -> 属性填充

   C. 实例化 -> 初始化 -> 属性填充

   D. 初始化 -> 销毁 -> 实例化

### 二、多选（4×2）

1. Spring 事务失效场景（　）

   A. 非 public 方法

   B. 同类内部方法调用（this 调用）

   C. 异常被 catch 未抛出

   D. 未被 Spring 管理的类

2. @Transactional 可用参数（　）

   A. propagation 传播行为　　B. isolation 隔离级别　　C. rollbackFor 回滚异常　　D. timeout 超时

3. SpringBoot 常用条件注解（　）

   A. @ConditionalOnClass　　B. @ConditionalOnBean　　C. @ConditionalOnProperty　　D. @ConditionalOnMissingBean

4. Bean 初始化扩展接口（　）

   A. InitializingBean　　B. @PostConstruct　　C. init-method　　D. DisposableBean

### 三、简答（19 分）

1. Spring IOC 完整流程：扫描 -> 解析 -> BeanDefinition -> 实例化前/后 -> 依赖注入 -> 初始化。（5 分）

2. AOP 执行流程：切点匹配、代理创建、通知执行顺序。（4 分）

3. SpringBoot 自动配置原理：spring.factories、@EnableAutoConfiguration、条件装配。（5 分）

4. 循环依赖解决：三级缓存、提前暴露代理对象、为什么不能解决构造器循环。（5 分）

---

## 模块九 消息队列 MQ（RocketMQ/Kafka）（18 分）

### 简答

1. 引入 MQ 优点与缺点：异步、解耦、削峰；缺点：一致性、可用性依赖、消息重复/丢失。（5 分）

2. 如何保证消息不丢失：生产端、Broker 端、消费端。（4 分）

3. 消息重复消费与幂等性解决方案。（4 分）

4. 消息堆积原因与处理思路。（5 分）

---

## 模块十 微服务 & 分布式 & 架构（24 分）

### 简答

1. 服务注册发现原理：心跳、健康检查、故障剔除。（3 分）

2. 服务雪崩：熔断、限流、降级区别与实现。（4 分）

3. 分布式事务方案：2PC、TCC、可靠消息最终一致性、SAGA 适用场景。（5 分）

4. 接口幂等性实现方案：Token、唯一索引、防重表、分布式锁。（4 分）

5. 分布式 ID 方案：UUID、雪花算法、号段模式、优缺点。（4 分）

6. 网关作用：路由、限流、认证、日志、跨域。（4 分）

---

## 模块十一 设计模式 & 代码规范 & 安全（14 分）

1. 单例模式 5 种写法对比：饿汉、懒汉、DCL、静态内部类、枚举。（4 分）

2. 项目常用模式：工厂、策略、模板、适配器、装饰器、代理，各举 Spring/业务场景。（4 分）

3. 常见安全问题：SQL 注入、XSS、CSRF、越权、防刷、敏感信息脱敏。（3 分）

4. 代码规范：异常处理、日志规范、空指针防范、集合使用。（3 分）

---

## 模块十二 计算机网络 & 操作系统（18 分）

### 一、单选（4×1）

1. TCP 三次握手目的（　）

   A. 确认收发正常，初始化序列号　　B. 加速传输　　C. 加密　　D. 压缩

2. TCP 粘包拆包原因（　）

   A. 流式传输无边界　　B. 拥塞控制　　C. 慢启动　　D. 滑动窗口

3. HTTPS 加密方式（　）

   A. 非对称加密交换密钥，对称加密传输　　B. 纯对称　　C. 纯非对称　　D. 明文

4. HTTP 2.0 新特性不包括（　）

   A. 多路复用　　B. 头部压缩　　C. 明文传输　　D. 二进制帧

### 二、简答

1. TCP 三次握手、四次挥手详细流程。（4 分）

2. TCP 可靠性保证：序列号、确认应答、重传、滑动窗口、拥塞控制。（3 分）

3. 进程与线程区别、通信方式、IO 模型。（3 分）

4. HTTP 状态码分类：2xx/3xx/4xx/5xx 典型码。（3 分）
