# 校招笔试模拟题 · 第二套

> 建议限时作答；对答案与错题整理请参照对应查漏补缺文档。

---

## 模块一 Java 基础与语法（22 分）

### 一、基础单选（5×1）

1. 关于 `String s = new String("hello")` 与字符串常量池，说法正确的是（ ）

   A. 该行执行会在堆上创建 1 个对象  
   B. 该行执行会在堆上创建 2 个对象（一个在堆、一个在常量池）  
   C. `s.intern()` 一定返回 s 自身  
   D. 字面量 `"hello"` 只在常量池中存在，不会在堆上出现  

2. 下列代码运行结果是（ ）

```java
public static void main(String[] args) {
    Integer a = 128;
    Integer b = 128;
    System.out.println(a == b);
    Integer c = 127;
    Integer d = 127;
    System.out.println(c == d);
}
```

   A. true true  
   B. false false  
   C. false true  
   D. true false  

3. 关于 Java 异常，说法正确的是（ ）

   A. RuntimeException 的子类必须用 try-catch 或 throws 显式处理  
   B. Error 表示程序可恢复的严重错误，建议捕获后降级  
   C. 受检异常在编译期检查，如 IOException、SQLException  
   D. finally 中的 return 不会覆盖 try 中已确定的返回值  

4. 下列与「Java 只有值传递」一致的说法是（ ）

   A. 引用类型作为参数时，传递的是对象在堆中的地址的副本  
   B. 在方法内可以改变调用方传入的引用变量所指向的对象  
   C. 数组作为参数时属于引用传递，会改变调用方的引用  
   D. 基本类型传的是引用，引用类型传的是值  

5. 关于 hashCode 与 equals，错误的是（ ）

   A. 两个对象 equals 为 true，则 hashCode 必须相等  
   B. hashCode 相等，两个对象不一定 equals 为 true  
   C. 重写 equals 时可以不重写 hashCode  
   D. 若将对象放入 HashSet，必须正确重写 equals 和 hashCode  

### 二、多选（3×2）

1. 下列场景中，存在线程安全风险的有（ ）

   A. 多线程读写同一个 HashMap  
   B. 多线程各自使用 new 出来的 SimpleDateFormat 实例  
   C. 多线程通过 ThreadLocal 存取各自的用户信息  
   D. 多线程对同一 ArrayList 执行 add/remove  

2. 关于 Java 集合与泛型，说法正确的有（ ）

   A. 泛型擦除后，运行时无法获取泛型的具体类型（如 `T` 的真实类型）  
   B. `List<String>` 与 `List<Integer>` 擦除后是同一个 Class  
   C. 泛型可以用于静态方法且静态方法上的类型参数与实例无关  
   D. `Arrays.asList(1,2,3)` 返回的列表支持 add、remove 操作  

3. 下列符合「不可变对象」设计的有（ ）

   A. 类用 final 修饰，字段用 final 修饰，无 setter  
   B. 若字段是引用类型，对外暴露时做防御性拷贝  
   C. 构造完成后，通过反射可以修改 final 字段，因此无法真正不可变  
   D. String、Integer、BigDecimal 是典型的不可变类  

### 三、简答（11 分）

1. 简述静态分派与动态分派的区别，并说明分别对应重载和重写的哪种多态。（4 分）

2. 说明 `Integer.valueOf(100)` 与 `new Integer(100)` 的区别，以及为何推荐用 `equals` 比较包装类。（4 分）

3. 高并发下 HashMap 在 JDK 1.7 和 1.8 分别可能出现什么问题？并发场景应使用什么替代？（3 分）

---

## 模块二 集合框架 & 数据结构（26 分）

### 一、多选（4×2）

1. 关于 ArrayList，说法正确的有（ ）

   A. 默认初始容量为 0，第一次 add 后一般为 10  
   B. 扩容时新容量约为原来的 1.5 倍  
   C. 扩容会申请新数组并拷贝元素，原数组被 GC 回收  
   D. 指定初始容量可避免多次扩容，建议在已知大小时使用  

2. 关于 ConcurrentHashMap（JDK 1.8），正确的有（ ）

   A. 采用 CAS + synchronized 锁桶头节点  
   B. size 通过 baseCount 与 CounterCell 数组累加得到  
   C. 读操作无需加锁，但可能读到正在扩容的中间状态  
   D. 链表长度达到 8 且数组长度≥64 时转为红黑树  

3. 下列属于「阻塞队列」的有（ ）

   A. ArrayBlockingQueue  
   B. LinkedBlockingQueue  
   C. PriorityQueue  
   D. SynchronousQueue  

4. 关于 LinkedHashMap，正确的有（ ）

   A. 在 HashMap 基础上增加双向链表维护插入或访问顺序  
   B. 可实现 LRU 缓存（结合 removeEldestEntry）  
   C. 迭代顺序与插入顺序一致（默认）  
   D. 线程安全，可在多线程下直接使用  

### 二、简答（18 分）

1. 简述 ArrayList 的扩容机制：何时扩容、扩容倍数、大致流程。（4 分）

2. 描述 HashMap 的 put 流程（从 hash 到插入/覆盖、链表/红黑树、扩容判断）。（5 分）

3. 为什么建议为 HashMap、ArrayList 指定初始容量？HashMap 若预计存 1000 个元素，初始容量大致如何设置？（4 分）

4. 简述 ConcurrentHashMap 1.8 如何保证线程安全（CAS、锁粒度、size 思路）。（5 分）

---

## 模块三 JVM 虚拟机（30 分）

### 一、单选（5×1）

1. 下列区域中，线程私有且可能发生 StackOverflowError 的是（ ）

   A. 堆  
   B. 方法区（元空间）  
   C. 虚拟机栈  
   D. 程序计数器  

2. 判断对象是否存活的算法，JVM 采用的是（ ）

   A. 引用计数法  
   B. 可达性分析  
   C. 复制算法  
   D. 分代收集  

3. 下列可作为 GC Roots 的是（ ）

   A. 栈帧中的局部变量表引用的对象  
   B. 已被回收的弱引用指向的对象  
   C. 仅被软引用引用的对象  
   D. 堆中未被任何引用指向的对象  

4. 关于 G1 收集器，说法正确的是（ ）

   A. 将堆划分为多个大小相等的 Region，无分代概念  
   B. 追求全程无 STW  
   C. 不适用于大堆  
   D. 没有「可预测停顿」的设计目标  

5. 破坏双亲委派模型的典型场景是（ ）

   A. 加载系统类库  
   B. 加载用户自定义类  
   C. SPI 机制中由线程上下文类加载器加载驱动类  
   D. 加载 Bootstrap 类  

### 二、多选（4×2）

1. 堆 OOM 的常见原因包括（ ）

   A. 内存泄漏导致无法回收  
   B. 堆空间设置过小  
   C. 递归过深导致栈溢出  
   D. 一次性加载大量数据到内存  

2. 关于 JVM 调优参数，正确的有（ ）

   A. -Xms 与 -Xmx 可设相同，避免运行期扩容  
   B. -XX:MetaspaceSize 设置元空间初始大小  
   C. -XX:+HeapDumpOnOutOfMemoryError 可在 OOM 时自动 dump  
   D. -XX:+UseG1GC 表示使用 G1 收集器  

3. 关于强、软、弱、虚引用，正确的有（ ）

   A. 强引用存在时，对象不会被回收  
   B. 软引用在内存不足时会被回收，适合做缓存  
   C. 弱引用一旦没有强引用指向，下次 GC 必回收  
   D. 虚引用必须与 ReferenceQueue 配合使用  

4. JIT 优化包括（ ）

   A. 逃逸分析  
   B. 标量替换与栈上分配  
   C. 锁消除  
   D. 方法内联  

### 三、简答（17 分）

1. 简述 JVM 中对象创建的完整流程（从类加载检查到执行 `<init>`）。（4 分）

2. 说明 CMS 的四个阶段、优缺点，以及什么是 Concurrent Mode Failure。（5 分）

3. 内存泄漏与内存溢出的区别，并列举 3 种以上典型的 Java 内存泄漏场景。（4 分）

4. 如何定位 OOM？常用工具有哪些？简要说明排查思路。（4 分）

---

## 模块四 多线程 & 并发 & JUC（36 分）

### 一、单选（6×1）

1. 关于 Thread 的 start() 与 run()，正确的是（ ）

   A. 直接调用 run() 会启动新线程  
   B. start() 被调用后，线程进入就绪态，由调度器调度执行 run()  
   C. 同一个 Thread 实例可以多次调用 start()  
   D. run() 执行完毕线程一定进入 TERMINATED  

2. synchronized 锁升级（膨胀）顺序是（ ）

   A. 无锁 → 偏向锁 → 轻量级锁 → 重量级锁  
   B. 无锁 → 轻量级锁 → 偏向锁 → 重量级锁  
   C. 偏向锁 → 轻量级锁 → 无锁 → 重量级锁  
   D. 仅存在重量级锁，无升级  

3. volatile 不能保证的是（ ）

   A. 可见性  
   B. 禁止指令重排序  
   C. 复合操作的原子性（如 i++）  
   D. 单次读写的原子性  

4. AQS 中 state 的含义（ ）

   A. 仅表示是否加锁（0/1）  
   B. 在 ReentrantLock 中表示重入次数，在 Semaphore 中表示许可数  
   C. 由 ThreadLocal 维护  
   D. 只能被一个线程修改  

5. ThreadLocal 可能造成内存泄漏的原因主要是（ ）

   A. key 为强引用，线程不结束则 key 一直存在  
   B. Entry 的 key 是弱引用，被回收后 value 仍被强引用且未 remove  
   C. value 是弱引用，容易被回收  
   D. 必须配合线程池使用才会泄漏  

6. 线程池的拒绝策略中，由「调用者线程」执行任务的是（ ）

   A. AbortPolicy  
   B. CallerRunsPolicy  
   C. DiscardPolicy  
   D. DiscardOldestPolicy  

### 二、多选（4×2）

1. 下列能保证「原子性」的有（ ）

   A. synchronized  
   B. ReentrantLock  
   C. AtomicInteger 的 getAndIncrement  
   D. volatile 修饰的 int 自增  

2. 死锁产生的必要条件包括（ ）

   A. 互斥条件  
   B. 请求与保持  
   C. 不可剥夺  
   D. 循环等待  

3. ReentrantLock 相比 synchronized 的优势有（ ）

   A. 可响应中断  
   B. 可设置超时获取锁  
   C. 可实现公平锁  
   D. 不需要手动释放锁  

4. 关于 JUC 工具，说法正确的有（ ）

   A. CountDownLatch 计数到 0 后不能重置  
   B. CyclicBarrier 可重复使用  
   C. Semaphore 可限制同时访问的线程数  
   D. CompletableFuture 支持异步编排  

### 三、简答 & 手写（22 分）

1. 写出线程池的 7 个核心参数，并简述提交任务后的执行流程（核心线程 → 队列 → 非核心 → 拒绝）。（5 分）

2. 简述 AQS 的原理：state 的作用、独占/共享模式、等待队列与唤醒。（4 分）

3. 手写：双重检查锁（DCL）实现的单例，并说明 instance 为什么要用 volatile。（5 分）

4. 手写：一段必然产生死锁的代码（两线程、两把锁），并说明如何用 jstack 定位以及如何避免。（5 分）

5. ThreadLocal 的实现原理、典型使用场景、使用后为什么要调用 remove？（3 分）

---

## 模块五 IO & NIO & Netty（20 分）

### 一、单选（4×1）

1. 关于 BIO、NIO、AIO，说法正确的是（ ）

   A. BIO 是同步阻塞，一个线程通常对应一个连接  
   B. NIO 是同步非阻塞，一个线程只能处理一个 Channel  
   C. AIO 是同步非阻塞  
   D. NIO 的 Selector 属于 AIO  

2. NIO 中 Buffer 的 flip() 方法作用是（ ）

   A. 清空缓冲区  
   B. 将写模式切换为读模式，limit=position，position=0  
   C. 将 position 置 0，limit 不变  
   D. 扩容  

3. Netty 的线程模型通常基于（ ）

   A. 主从 Reactor 多线程  
   B. 单 Reactor 单线程  
   C. 传统 BIO 多线程  
   D. 纯 AIO  

4. 下列序列化方式中，跨语言、可读性较好的是（ ）

   A. Java 原生序列化  
   B. Protobuf（二进制）  
   C. JSON  
   D. Hessian（二进制）  

### 二、简答（16 分）

1. 对比 BIO、NIO、AIO 的阻塞性、连接模型、线程模型和适用场景。（5 分）

2. 简述 NIO 三大组件 Channel、Buffer、Selector 的作用及它们之间的关系。（4 分）

3. Java 原生序列化有哪些缺点和安全风险？实际项目常用哪些替代方案？（3 分）

4. Netty 相比 JDK 原生 NIO 有哪些优势？写出 Bootstrap、Channel、Pipeline、Handler 的作用。（4 分）

---

## 模块六 MySQL & MyBatis & 索引 & 事务（38 分）

### 一、单选（6×1）

1. InnoDB 主键索引采用的数据结构是（ ）

   A. B+ 树  
   B. B 树  
   C. 哈希表  
   D. 红黑树  

2. MySQL InnoDB 默认的隔离级别是（ ）

   A. READ UNCOMMITTED  
   B. READ COMMITTED  
   C. REPEATABLE READ  
   D. SERIALIZABLE  

3. 在 REPEATABLE READ 下，解决幻读主要依靠（ ）

   A. 仅 MVCC  
   B. 仅行锁  
   C. MVCC + 间隙锁（含临键锁）  
   D. 表锁  

4. 下列哪种写法容易导致索引失效？（ ）

   A. 对索引列使用函数，如 `WHERE YEAR(create_time)=2024`  
   B. 联合索引 (a,b,c) 且条件为 `WHERE a=1 AND b=2 AND c=3`  
   C. `WHERE name LIKE '张%'`  
   D. 覆盖索引查询  

5. MyBatis 中 #{} 与 ${} 的区别，正确的是（ ）

   A. #{} 预编译防 SQL 注入，${} 字符串拼接  
   B. ${} 预编译，#{} 拼接  
   C. 两者等价  
   D. #{} 不能用于 order by 子句  

6. MVCC 实现不依赖（ ）

   A. 隐藏列（如 trx_id、roll_pointer）  
   B. undo log 与版本链  
   C. Read View  
   D. redo log  

### 二、多选（4×2）

1. 事务 ACID 与实现机制对应正确的有（ ）

   A. 原子性：undo log  
   B. 持久性：redo log  
   C. 隔离性：锁 + MVCC  
   D. 一致性：由业务与上述机制共同保障  

2. 关于联合索引 (a, b, c)，正确的有（ ）

   A. 查询条件只有 b、c 时，可能无法利用该索引  
   B. 满足最左前缀时可以利用  
   C. 范围查询（如 a>1）后面的列可能无法用索引  
   D. order by a, b 可以利用该索引排序  

3. MyBatis 一级缓存、二级缓存说法正确的有（ ）

   A. 一级缓存默认开启，作用域为 SqlSession  
   B. 二级缓存为 mapper 级别，默认关闭  
   C. 执行 update/insert/delete 会清空一级缓存  
   D. 多表关联时使用二级缓存需注意数据一致性  

4. MySQL 锁的说法正确的有（ ）

   A. 行锁、表锁、意向锁、间隙锁、临键锁等  
   B. 共享锁(S)、排他锁(X)  
   C. InnoDB 行锁需要命中索引，否则可能锁表  
   D. 间隙锁只在 SERIALIZABLE 下存在  

### 三、简答（24 分）

1. 为什么 B+ 树适合做数据库索引？与 B 树、红黑树相比有什么优势？（4 分）

2. 简述 MVCC 的实现：隐藏列、undo log、Read View、版本链与可见性判断。（5 分）

3. 列举至少 6 种会导致索引失效或无法充分利用索引的场景。（4 分）

4. 慢 SQL 的排查流程：如何开启慢查询、explain 关注哪些字段、常见优化手段。（5 分）

5. MyBatis 一级缓存在哪些情况下会失效？#{} 的预编译原理是什么？（3 分）

6. 大表优化思路：分库分表、分区、索引、冷热分离等（简要说明）。（3 分）

---

## 模块七 Redis 缓存 & 分布式（26 分）

### 一、单选（5×1）

1. Redis 6.0 之前处理命令的线程模型是（ ）

   A. 单线程（命令执行）  
   B. 多线程竞争  
   C. 多进程  
   D. 协程  

2. 缓存雪崩通常指（ ）

   A. 大量 key 同时过期或 Redis 宕机，请求打到 DB  
   B. 热点 key 过期导致瞬时压力  
   C. 查询不存在的数据导致穿透  
   D. 仅指 Redis 宕机  

3. Redis 持久化方式包括（ ）

   A. RDB 与 AOF  
   B. 仅 RDB  
   C. 仅 AOF  
   D. 基于 MySQL binlog  

4. 解决缓存穿透的常见方案是（ ）

   A. 互斥锁  
   B. 布隆过滤器 / 缓存空值  
   C. 仅设置随机过期时间  
   D. 队列削峰  

5. 实现分布式锁时，通常需要（ ）

   A. SET key value NX EX seconds 等原子命令，配合唯一 value 防误删  
   B. 仅 INCR  
   C. 仅 HSET  
   D. 仅 LPUSH  

### 二、简答（21 分）

1. 说明缓存穿透、缓存击穿、缓存雪崩的区别、典型场景及各自解决方案。（6 分）

2. Redis 的过期删除策略（惰性删除、定期删除）与内存淘汰策略（如 LRU、LFU）简要说明。（4 分）

3. 分布式锁如何实现？如何保证互斥、防止死锁、防止误删、可重入与锁续期（可结合 Redisson）？（5 分）

4. Redis 集群方案：主从、哨兵、Cluster 分别解决什么问题？（3 分）

5. 如何保证缓存与数据库的一致性？Cache Aside 策略要点是什么？（3 分）

---

## 模块八 Spring & SpringBoot（32 分）

### 一、单选（5×1）

1. Spring IOC 容器的核心接口是（ ）

   A. BeanFactory、ApplicationContext  
   B. BeanDefinition  
   C. ApplicationEventPublisher  
   D. ResourceLoader  

2. Spring AOP 默认的代理方式是（ ）

   A. 有接口用 JDK 动态代理，无接口用 CGLIB  
   B. 全部 CGLIB  
   C. 全部 JDK 动态代理  
   D. 静态代理  

3. 事务传播行为 REQUIRES_NEW 表示（ ）

   A. 有事务则加入，没有则新建  
   B. 总是新建独立事务，挂起当前事务  
   C. 必须在已有事务中运行  
   D. 以非事务方式运行  

4. SpringBoot 自动配置的核心机制是（ ）

   A. @EnableAutoConfiguration + spring.factories / 自动配置类上的条件注解  
   B. 仅 @ComponentScan  
   C. 仅 @Configuration  
   D. 仅 @Conditional  

5. Bean 生命周期的正确顺序是（ ）

   A. 实例化 → 属性填充 → 初始化 → 使用 → 销毁  
   B. 初始化 → 实例化 → 属性填充  
   C. 实例化 → 初始化 → 属性填充  
   D. 属性填充 → 实例化 → 初始化  

### 二、多选（4×2）

1. 可能导致 @Transactional 失效的有（ ）

   A. 方法非 public  
   B. 同类内部方法调用（this 调用），未经过代理  
   C. 异常被 catch 未抛出  
   D. 类未被 Spring 管理（如 new 出来的对象）  

2. @Transactional 可配置的参数有（ ）

   A. propagation  
   B. isolation  
   C. rollbackFor  
   D. timeout  

3. SpringBoot 中常用的条件注解有（ ）

   A. @ConditionalOnClass  
   B. @ConditionalOnBean  
   C. @ConditionalOnProperty  
   D. @ConditionalOnMissingBean  

4. Bean 初始化阶段可用的扩展方式有（ ）

   A. InitializingBean#afterPropertiesSet  
   B. @PostConstruct  
   C. init-method  
   D. DisposableBean#destroy（销毁阶段）  

### 三、简答（19 分）

1. 简述 Spring IOC 的完整流程：扫描、解析 BeanDefinition、实例化、属性注入、初始化。（5 分）

2. 简述 AOP 的执行流程：切点匹配、代理创建、通知执行顺序（@Around 与 @Before/@After 等）。（4 分）

3. SpringBoot 自动配置原理：@EnableAutoConfiguration、spring.factories、条件装配的作用。（5 分）

4. Spring 如何解决循环依赖？三级缓存分别存什么？为什么构造器注入的循环依赖无法用三级缓存解决？（5 分）

---

## 模块九 消息队列 MQ（18 分）

### 简答

1. 引入 MQ 有哪些优点和缺点？（5 分）

2. 如何保证消息不丢失？从生产者、Broker、消费者三端说明。（4 分）

3. 消息重复消费的原因及幂等性解决方案。（4 分）

4. 消息堆积的常见原因及处理思路。（5 分）

---

## 模块十 微服务 & 分布式（24 分）

### 简答

1. 服务注册与发现的基本原理：心跳、健康检查、故障剔除。（3 分）

2. 服务雪崩与熔断、限流、降级的区别及常见实现方式。（4 分）

3. 分布式事务常见方案：2PC、TCC、可靠消息最终一致性、SAGA 的适用场景。（5 分）

4. 接口幂等的实现方案：Token、唯一索引、防重表、分布式锁等。（4 分）

5. 分布式 ID 方案：UUID、雪花算法、号段模式及其优缺点。（4 分）

6. 网关的主要作用：路由、限流、认证、日志、跨域等。（4 分）

---

## 模块十一 设计模式 & 安全 & 规范（14 分）

1. 单例的几种写法：饿汉、懒汉、DCL、静态内部类、枚举，并对比线程安全与懒加载。（4 分）

2. 工厂、策略、模板方法、适配器、代理在 Spring 或业务中的典型应用各举一例。（4 分）

3. 常见 Web 安全问题：SQL 注入、XSS、CSRF、越权、防刷、敏感信息脱敏的防护思路。（3 分）

4. 代码规范：异常处理、日志、空指针防范、集合使用等方面写出 3 条以上建议。（3 分）

---

## 模块十二 计算机网络 & 操作系统（18 分）

### 一、单选（4×1）

1. TCP 三次握手的主要目的是（ ）

   A. 确认双方收发能力、协商序列号  
   B. 加密  
   C. 压缩  
   D. 加速  

2. TCP 粘包/拆包的根本原因是（ ）

   A. TCP 是流式协议，无消息边界  
   B. 拥塞控制  
   C. 滑动窗口  
   D. 慢启动  

3. HTTPS 的加密方式是（ ）

   A. 非对称加密协商密钥，对称加密传输数据  
   B. 全程对称加密  
   C. 全程非对称加密  
   D. 不加密  

4. HTTP/2 的特性包括（ ）

   A. 多路复用、头部压缩、二进制分帧  
   B. 明文传输  
   C. 仅支持短连接  
   D. 无状态  

### 二、简答

1. 简述 TCP 三次握手与四次挥手的流程及 TIME_WAIT 的作用。（4 分）

2. TCP 如何保证可靠性？序列号、确认应答、重传、滑动窗口、拥塞控制各起什么作用？（3 分）

3. 进程与线程的区别；进程间通信的常见方式；常见 IO 模型。（3 分）

4. HTTP 状态码 2xx、3xx、4xx、5xx 各举 2 个典型码并说明含义。（3 分）

---

*做完后对答案、整理错题请参照对应的查漏补缺文档。*
